# 当前记忆状态快照

> 日期: 2026-06-19  
> 分支: `feature/v6.2`  
> 当前浏览器地址: `http://127.0.0.1:8765/index.html?qa=materialsP1#materials`  
> 用途: 保存当前协作上下文、用户偏好、已知风险和下一步工作依据。

## 1. 用户最新意图

用户认为当前项目与原预期出现较大出入，希望先做文档同步和 Git 提交，而不是继续盲目扩展页面。用户要求：

- 更新一版项目规划描述，说明我当前理解的未来项目方向。
- 写一份当前记忆状态快照文档。
- 完成 Git 提交，形成当前状态检查点。

## 2. 用户明确偏好

- 项目是学科知识学习类网站，知识内容必须真实可信。
- 可以使用网络资料或爬取辅助完善内容，但最终页面表达必须面向学习者，而不是面向开发过程。
- 公开页面不能暴露内部工作过程，例如调试、复核记录、规划任务、内部质量标签等。
- 如果页面里出现类似「审查情况」的外露内容，应撤销或改写为正常学习内容。
- 用户希望我明确自己在做什么，并能把项目方向讲清楚。

## 3. 当前项目理解

项目当前已经从单一「工科实验室」扩展为「星序 Astra」多星系学习平台：

- 主站: `index.html`
- 代码空间子站: `codevis/index.html`
- 文档目录: `doc/`
- 模板目录: `muban/`
- 共享配置: `shared/js/config.js`
- 静态预览服务: `server/dev-static-server.mjs`
- 当前本地预览端口: `127.0.0.1:8765`

当前主站包含传统核心学科与扩展学科两层：

- 核心学科: 数学、物理、化学、算法、生物。
- 扩展方向: 地球与宇宙科学、工程应用、数据科学、信息技术、材料、人文可视化。

我的当前判断是：扩展方向应暂时收束为「候选正式模块」，需要经过内容可信度、交互价值和页面一致性验收后再完全纳入正式方向。

## 4. 最近完成的关键工作

近期围绕总体完善目标已经做过以下方向的工作：

- 新增或完善多个扩展页: 宇宙、数据科学、信息技术、材料、人文、工程。
- 人文页补充了文本证据链、方法边界和公开参考资料。
- 材料页补充了显微图像到材料判断的路径、Hall-Petch 适用范围提示和移动端 canvas 收缩修复。
- 多次扫描公开页面，确认不再出现用户提醒的内部过程类文案。
- 用本地静态服务验证过当前材料页可访问。

## 5. 当前工作树状态

截至本快照，工作树存在大量未提交改动，包括：

- 主入口 `index.html`
- 共享配置、路由、模块选择器、service worker
- 多个核心学科模块脚本和 CSS
- 新增扩展页目录: `pages/cosmos/`、`pages/datascience/`、`pages/engineering/`、`pages/humanities/`、`pages/infotech/`、`pages/materials/`
- 新增多个算法、生物、化学、数学、物理补充模块
- 本次新增文档: `doc/07-项目方向规划快照-20260619.md`、`doc/08-记忆状态快照-20260619.md`

这些变更范围较大，因此本次提交应被视为「方向同步和当前工作快照」，不是最终完成版。

## 6. 已知验证信息

最近一次材料页验证信息：

- URL: `http://127.0.0.1:8765/index.html?qa=materialsP1#materials`
- 静态请求返回: `200`
- `node --check pages/materials/materials-lab.js` 通过
- `node --check shared/js/main.js` 通过
- `node --check sw.js` 通过
- 桌面浏览器检查: active page 为 `page-materials`
- 移动端 390px 检查: 无横向溢出，canvas 宽度收进容器
- 浏览器日志: 无 error/warn
- 交互验证: 材料页「快速凝固」「退火长大」按钮可以更新晶粒尺寸和说明面板
- `npm run build` 不适用，因为项目根目录没有 `package.json`

## 7. 当前风险

- 项目方向已经从原本更聚焦的工科实验室扩张为多学科星序平台，可能超出用户最初预期。
- 工作树改动范围很大，需要通过文档和提交先冻结当前状态，再决定回收或继续扩展。
- 现有部分旧文档在终端中显示编码异常，后续应优先用 UTF-8 工具重新整理文档，而不是盲目大段覆盖。
- 公开页面目前已扫描过内部过程词，但后续新增内容仍需继续执行扫描。
- 当前没有 npm 构建体系，验证主要依赖静态服务、脚本语法检查和浏览器检查。

## 8. 下一步建议

建议用户先确认项目路线：

1. 继续走「星序 Astra 多学科平台」路线。
2. 回收为更聚焦的「工科实验室」路线。
3. 保留星序品牌，但把扩展方向降级为未来星图，不进入主学习路径。

在路线确认前，不建议继续大规模新增页面。更合适的下一步是：

- 对当前页面做文案和资料来源清单。
- 明确哪些扩展方向正式保留。
- 给每个正式方向补学习目标、模型边界和参考资料。
- 统一移动端与实验台布局。


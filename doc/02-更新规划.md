# 星序 Astra — 后续更新计划

> **制定日期**: 2026-04-13 | **最后更新**: 2026-07-07 | **状态**: v6.5 后端化首阶段 review 报告已回流；REV-01 active 草稿数据库级抗并发、REV-02 closed 作业复盘入口、REV-03 direct join 口径、REV-04 前七批后端修复、R-21 用户名数据库级 normalized key 唯一约束、BE-12 缺陷记录外部 issue 链接、审计日志链式哈希、审计链完整性校验、审计日志 JSON/CSV 明细导出、报表摘要导出、审计高频候选摘要、审计留存预检、本地审计归档包导出/Manifest 校验和导出/摘要行为审计留痕、BE-08 知识快照调度数据库租约/自动心跳/运行列表/健康摘要与协作式取消、BE-03/BE-05 禁用用户会话撤销、BE-03 活动会话列表/单会话撤销与会话设备标识/last_seen 追踪/节流、BE-03/BE-05 管理员密码重置、BE-03 用户自助密码重置令牌与密码重置 token 留存清理脚本已在 `houduan` 落地
>
> **文档边界**：本文只记录下一阶段仍需推进、仍需验收或仍需决策的事项。已完成版本、历史修复、验证记录和阶段沉淀统一进入 [`03-发布历史.md`](03-发布历史.md)；长期开发规则进入 [`01-开发者手册.md`](01-开发者手册.md)；后端架构、三端平台和数据模型细节进入 [`07-后端优化与设计.md`](07-后端优化与设计.md)。
>
> **维护规则**：完成项不要长期留在本文。每轮完成后，应把结果、验证证据和剩余风险同步到发布历史，再把本文压缩回“下一步”。
>
> **Review 前快照**：2026-07-05 建议以 `houduan@c9a2b41` 作为团队整体 review 锚点；功能实现锚点为 `c32d3d5 V6.5.22 内容脚本 sandbox 契约`。记录时工作区干净，当前可以暂停开发进入 review。
>
> **Review 报告回流**：2026-07-06 已读取 `review` 分支 `doc/review-code-audit-report-20260706.md`。本轮只同步结论与后续任务，不合并或覆盖 `review` 分支代码修复；后续在 `houduan` 上按优先级选择性吸收。
>
> **最新快照**：2026-07-06，REV-04 后端候选已在 `houduan` 分批收口至中文路径迁移测试：正式内容初始化覆盖中文目录/中文 SQLite 文件名，内容发布链路覆盖中文 slug 的创建、发布、公开读取、后台搜索与版本过滤；真实 MySQL/部署联调风险转入 BE-10/BE-12。
>
> **账号安全快照**：2026-07-06，R-21 已新增 `users.normalized_username` 与 `login_attempts.normalized_username` 数据库唯一约束，注册/admin bootstrap 并发唯一冲突返回 `409`；历史重复用户迁移前需要人工清洗，真实 MySQL 落库验证仍归入 BE-10/BE-12。
>
> **审计与缺陷治理快照**：2026-07-07，BE-12 已新增 `BugRecord.external_issue_provider/external_issue_id/external_issue_url`，管理端创建/更新会修剪字段，列表关键字搜索覆盖 issue 元数据，`admin.bug.create/update` 快照记录链接变化；新写入 `AuditLog` 会生成 `prev_hash/current_hash` 应用层 SHA-256 链式哈希，查询与 JSON/CSV 明细导出均保留哈希字段；审计日志已新增 `GET /api/admin/audit-logs/export` JSON 明细导出、`GET /api/admin/audit-logs/export.csv` CSV 明细导出、`GET /api/admin/audit-logs/report` JSON 报表摘要、`GET /api/admin/audit-logs/report.csv` CSV 报表摘要、`GET /api/admin/audit-logs/high-frequency` 高频候选摘要、`GET /api/admin/audit-logs/retention-plan` 留存预检和 `GET /api/admin/audit-logs/chain-integrity` 链完整性校验，均复用审计日志筛选或时间窗边界；明细导出默认剥离 `snapshot_json`，显式 `include_snapshot=true` 才返回快照；CSV 明细响应返回 `X-Audit-Export-*` 元数据头并中和表格公式前缀；报表按 action/resource_type/actor_role/event_result/failure_reason 聚合，返回 `X-Audit-Report-*` 元数据头；高频摘要默认最近 24 小时，按 action、actor/action、ip/action、resource/action 和 failure_reason 聚合候选，成功后写入 `admin.audit.high_frequency`，只记录筛选、时间窗、阈值、总量和维度命中数；留存预检按 `ASTRA_AUDIT_LOG_RETENTION_DAYS` 或显式 `retention_days/before` 计算 cutoff、归档候选、临期数量、聚合桶和哈希链边界，成功后写入 `admin.audit.retention_plan`；链完整性校验复用 `app.services.audit_chain` 重算 `current_hash`、检查相邻 `prev_hash`、把历史空 hash 标记为 partial，并写入不含 issue 明细的 `admin.audit.chain_integrity` 摘要；`scripts.archive_audit_logs` 已可按配置、`--retention-days` 或 `--before` 生成 JSONL/CSV 归档文件与 Manifest，记录 SHA-256、导出数量、截断状态、链边界和 hash-chain 重算状态，并支持 `--verify` 复验；归档脚本默认只读，不删除源表、不写新的审计日志、不提供 WORM 或外部锚定；正式外部 issue 自动创建/双向同步、外部对象存储/WORM、第三方时间戳、外部锚定、删除/清理策略和正式告警/处置策略仍归入 BE-12 后续。
>
> **会话治理快照**：2026-07-07，BE-03/BE-05 已补齐禁用用户会话撤销，BE-03 已补齐当前用户活动会话列表、单会话撤销、会话设备标识与 last_seen 追踪/节流：`GET /api/auth/sessions` 只返回未撤销、未过期会话并标记当前会话，返回 best-effort `device_label`、登录 user-agent 和 `last_seen_at`；有效鉴权默认 300 秒内同一 IP 哈希不重复写入 `AuthSession.last_seen_at`，窗口过期、IP 哈希变化、缺失 last_seen 或 `ASTRA_SESSION_LAST_SEEN_UPDATE_SECONDS=0` 时刷新；`DELETE /api/auth/sessions/{id}` 仅允许撤销自己的活动会话且写入 `auth.session.revoke` 审计；用户自助重置 token 已补齐，真实投递/MFA、强设备绑定、长期会话策略和真实 MySQL 并发/事务验证仍归入 BE-03/BE-10/BE-12。
>
> **账号治理快照**：2026-07-07，BE-03/BE-05 已补齐管理员协助密码重置，BE-03 已补齐用户自助密码重置令牌与离线留存清理脚本：`POST /api/auth/password-reset/request` 泛化响应并按账号哈希/IP 哈希冷却，生产环境不回传 token；`POST /api/auth/password-reset/confirm` 行锁消费一次性 token，重置密码后撤销会话、清理登录失败桶并写入脱敏审计；`scripts.cleanup_password_reset_tokens` 默认 dry-run，显式 `--apply` 才按已用或已过期终态 token 删除，摘要不返回用户名、IP 哈希、user-agent 或 token hash。真实邮件/短信/MFA 投递、生产调度/演练、强设备绑定、长期会话策略和真实 MySQL 并发/事务验证仍归入 BE-03/BE-10/BE-12。

> **知识快照调度快照**：2026-07-07，BE-08 已补齐知识快照调度数据库租约、自动心跳、运行列表、健康摘要和运行取消首轮：`knowledge_snapshot_runs` 新增 scheduler lease owner/token/expires/heartbeat 元数据，FastAPI 进程内调度器与 `scripts.rebuild_knowledge_snapshots` CLI 均先抢占同一 `run_key` 租约再重算；未过期 running 窗口跳过，过期租约可被新 worker 接管，长重算循环会按 `ASTRA_KNOWLEDGE_SNAPSHOT_SCHEDULER_HEARTBEAT_SECONDS` 通过 owner/token guard 自动续租，失去租约或被 admin 协作式取消的旧 worker 会抛出 `SnapshotRunLeaseLost` 并停止；`GET /api/admin/knowledge-snapshot-runs` 可分页过滤 run，`GET /api/admin/knowledge-snapshot-runs/health` 可汇总 stale running、lease expiring、claimable、retryable/exhausted failed 和 problem runs，`POST /api/admin/knowledge-snapshot-runs/{id}/cancel` 可将 pending 或带 scheduler lease 的 running run 标记为 `cancelled` 并清空租约，响应不返回 `scheduler_lease_token`。外部任务队列、正式告警、requeue、队列堆积治理和真实 MySQL 并发取消演练仍归入 BE-08/BE-10 后续。

---

## 一、当前规划焦点

v6.5 的核心方向是把星序从“静态前端站点 + 轻量静态服务”推进到“可登录、可管理、可生成内容的教学平台”。

当前只保留影响下一步判断的现状：

1. 现有 C++ 服务主要承担静态托管、健康检查、服务器信息和占位 API。
2. 主站教学内容仍大量由前端静态页面、配置和脚本直接承载。
3. 项目已新增 Python 后端首切片，并建立本地账号、密码策略、登录失败锁定、用户、活动会话列表与单会话撤销、会话设备标识与 last_seen 追踪/节流、管理员密码重置、用户自助密码重置令牌、密码重置 token 留存清理脚本、禁用用户会话撤销、用户名大小写规范化、normalized key 数据库唯一约束、必填文本修剪后校验、学校、班级、班级加入申请审批与管理端队列、会话、课程、作业、学习事件、作业提交、作业只读复盘、跨班级提交唯一性、学生资源状态可见性、批改、积分流水、个人进度摘要、知识状态/班级规则统计、个人/班级知识快照、知识快照周期重算脚本/运行记录/进程内调度器/数据库租约防重入/自动心跳/运行列表/健康摘要与协作式取消、管理端 API、学校/班级深度统计、管理端列表分页搜索、管理端内容页数据库侧分页、缺陷记录外部 issue 链接、待批改队列、审计元数据、认证事件审计、审计日志链式哈希、审计链完整性校验、审计日志 JSON/CSV 明细导出、报表摘要导出、审计高频候选摘要、审计留存预检、本地审计归档包导出/Manifest 校验与导出/摘要行为审计留痕、学校/班级/课程访问控制服务层、跨范围权限矩阵测试、内容 seed 启动初始化与读取无副作用边界、正式内容初始化入口、ContentDraft 草稿与脚本审核、脚本静态分析风险等级、脚本 sandbox 契约、公开 render 脚本 manifest 脱敏、脚本历史版本 rollback 重审门禁、草稿编辑、提交/退回/撤回工作流、内容发布/版本记录/追加式回滚、发布/回滚冲突 409、内容页 current 指针、草稿 base version/hash、active key 数据库唯一约束、版本 previous 链、发布元数据、管理端版本 JSON path diff 与 semantic 富语义摘要、API no-store 缓存边界、部署预检、部署 smoke 门禁和前端 opt-in schema 渲染试点；真实邮件/短信/MFA 重置投递、强设备绑定、真实 iframe/worker 执行隔离、CSP 强制、脚本资产 allowlist/SRI、知识快照外部告警/任务队列/requeue/队列堆积治理、真实服务部署验证、管理端正式 UI、审计外部对象存储/WORM/外部锚定/删除清理策略、正式高频告警/处置策略、正式外部 issue 双向同步、密码重置 token 清理生产调度/演练和真实 MySQL 联调仍未闭环。
4. 后端生成页面已从工科试验室 `physics/energy-conservation` 开始试点，因为它已有较强模块化基础和页面模板参考。
5. 未来新增教学内容不应继续只靠前端静态页面扩张，应逐步转向后端内容协议和页面生成。

### Review 后承接原则（2026-07-05）

- 当前后端重构按整体目标估算约完成 70%；后端 API、数据模型、内容协议、审计和回归测试首轮已成型，但生产化、真实运行隔离、多实例与三端 UI 仍是后续大块。
- 团队 review 期间可以中断开发；review 后若出现 API、数据模型、迁移、权限、安全或内容协议的大改，应先更新本文和 `07-后端优化与设计.md` 的任务/架构边界。
- review 后的完成项继续沉淀到 `03-发布历史.md`，本文只保留仍需推进、仍需验收或仍需决策的事项。
- review 分支中的代码修复不得自动视为 `houduan` 已完成项；需要重新评估、选择性迁入、复测并按 `V+版本号` 提交。

### Review 报告回流待办（2026-07-06）

| 编号 | 优先级 | 归属 | 事项 | 验收标准 |
| ---- | ------ | ---- | ---- | -------- |
| REV-04 | P1 | BE-01 / BE-03 / BE-04 / BE-05 / BE-06 / BE-07 | 后端候选修复已分批收口 | 跨班级提交唯一性、后台内容页数据库侧分页、必填文本修剪后校验、用户名大小写规范化、发布冲突 409、学生资源可见性、中文路径迁移测试和中文 slug 回归已在 `houduan` 落地；后续只保留真实 MySQL/部署联调等 BE-10/BE-12 风险 |
| REV-05 | P1 | BE-09 / FE | 评估吸收 review 分支前端与离线修复 | 分批处理模块生命周期清理、deep link 规范化、跨页跳转不抢 hash、License 本地渲染、Service Worker 安装/离线缓存和 `codevis` 离线 shell |
| REV-06 | P1 | QA / FE | 将可复用前端浏览器测试入口纳入 `houduan` | 接受后补 `npm run test:frontend`、`test:frontend:online`、`test:frontend:offline` 文档和 CI/本地验证口径 |

---

## 二、v6.5 优先级任务

| 编号 | 优先级 | 状态 | 任务 | 验收标准 |
| ---- | ------ | ---- | ---- | -------- |
| BE-01 | P0 | 进行中 | 建立 Python + MySQL 后端工程骨架 | 已新增 FastAPI 后端、配置、数据库探针、Alembic 迁移、健康检查、测试入口和部署 smoke；仍需本机/部署环境 MySQL 实例联调 |
| BE-02 | P0 | 首轮完成 | 定义后端内容协议草案 | 已将 `physics/energy-conservation` 页面 JSON 持久化到 `content_pages`，并通过 Pydantic schema 返回；内容 seed 已迁到开发/测试初始化，读取接口保持 published 只读；正式内容初始化入口可在迁移和预检通过后显式创建/修复内置内容页版本，并要求 active admin 归因、脚本引用确认和冲突保护；已新增 ContentDraft 草稿、管理员草稿队列、脚本审核、脚本静态分析风险等级、脚本 sandbox 契约、公开 render 脚本 manifest 脱敏、脚本历史版本 rollback 重审门禁、草稿编辑、submit/request-changes/withdraw 工作流、active key 数据库唯一约束、发布元数据、发布、不可变版本记录、内容页 current 指针、草稿 base version/hash、版本 previous 链、版本 JSON path diff、semantic 富语义摘要和追加式回滚；已被 BE-09 首轮试点消费，真实 iframe/worker 执行隔离、CSP 强制、脚本资产 allowlist/SRI、稳定 sectionId/sourceId 身份和更细 diff 脱敏策略仍待推进 |
| BE-03 | P0 | 进行中 | 本地账号注册与登录 | 已支持本地账号注册、登录、退出、当前用户接口、活动会话列表、单会话撤销、会话设备标识、last_seen 追踪/节流、管理员密码重置、用户自助密码重置令牌、密码重置 token 留存清理脚本、角色字段、密码强度策略、登录失败持久化锁定、用户名 normalized key 数据库唯一约束、生产 cookie secure 标记、过期会话撤销、禁用用户会话撤销和登录/退出/密码重置审计；公开注册仅允许 teacher / student，admin 初始化走管理端 bootstrap；仍需更细权限、真实邮件/短信/MFA 投递、强设备绑定、长期会话策略、清理脚本生产调度/演练和真实 MySQL 迁移验证 |
| BE-04 | P0 | 进行中 | 学校、班级与资源边界 | 已支持教师建校建班、legacy/direct join 直接加入、join request 审批流、班级教师或管理员查看/审批/拒绝申请、管理端跨班级队列审批、审批后生成学校/班级成员关系、direct join 收口 pending 申请、普通业务端点访问控制 helper 服务层收口、禁用用户旧会话撤销和跨范围权限矩阵首批测试；仍需异常关系处理、管理端统计权限拆分、多角色/内容发布等更细权限矩阵 |
| BE-05 | P1 | 进行中 | 管理端基础 | 已支持首个 admin 初始化、用户/学校/班级/内容页/内容草稿基础查看、用户角色/状态维护、管理员密码重置、禁用用户会话撤销与 `revoked_sessions` 审计快照、全站统计摘要、待审加入申请计数、待脚本审核计数、学校/班级深度统计、管理端列表分页搜索、内容页数据库侧分页搜索、加入申请队列、草稿队列、待批改队列、审计日志查询和缺陷清单 `BugRecord` 基础维护及外部 issue 链接元数据；管理端正式 UI、审计日志前端视图、权限矩阵和前端统计视图仍待推进 |
| BE-06 | P1 | 进行中 | 教师端基础 | 已支持教师在学校/班级范围内创建课程、挂接班级、创建课程单元和作业，查看提交并批改生成积分流水；同一作业挂接多个班级时提交按班级 scope 区分；closed 作业后教师/admin 提交列表和批改视角保持可用；学生管理、积分规则配置和教师端 UI 仍待推进 |
| BE-07 | P1 | 进行中 | 学生端基础 | 已支持学生通过班级范围查看 published 课程、published 单元与 active 作业、按班级提交作业、进入 published 课程/单元内作业只读复盘、记录学习事件、查看个人积分流水和按学生可见资源过滤的个人进度摘要；closed/archived 作业不可再次提交但可查看本人题目、成绩和反馈；个人首页、正式作业中心 UI 和学习反馈入口仍待推进 |
| BE-08 | P1 | 进行中 | 学习事件与规则统计 | 已支持 append-only 学习事件、个人进度摘要、`/api/knowledge/me` 个人知识状态、`/api/classes/{id}/knowledge` 教师班级聚合、个人/班级知识快照、按日/周窗口重算快照的脚本、运行记录、FastAPI lifespan 进程内调度器、失败窗口尝试次数上限、基于 `knowledge_snapshot_runs.run_key` 的数据库租约防重入、长重算自动心跳、管理端运行列表、健康摘要和 pending/带租约 running run 协作式取消；健康摘要可观察 stale running、lease expiring、claimable、retryable/exhausted failed 与 problem runs；学生个人 progress/knowledge/snapshot 仅计入 published 课程、published 单元和 active 作业，周期 user snapshot 跳过 hidden course，class snapshot 保持管理口径；仍需外部告警、任务队列/requeue/队列堆积治理、单元/概念细分、真实 MySQL 取消演练和正式三端 UI |
| BE-09 | P1 | 首轮完成 | 前端渲染壳接入 | 已新增 `shared/js/backend-content.js` 和 `shared/css/backend-content.css`，通过 `?backendSchema=1` + `apiBase=` 或 `astra-api-base` 试点渲染 `physics/energy-conservation` 学习任务卡；默认静态页面保持回退 |
| BE-10 | P2 | 进行中 | 部署迁移方案 | 已补充 API no-store 缓存边界、数据库/Alembic 部署预检、部署 smoke 脚本和 `04-部署指南` 的静态站 + Python API 共存、反向代理、预检、回滚路径；仍需真实 MySQL、真实反向代理和服务注册实机验证 |
| BE-11 | P2 | 未启动 | 代码空间 / AI 助教 / OJ 二期预留 | 作业模型预留代码提交、AI 助教和 OJ 判题记录扩展位，但本阶段不作为首批闭环 |
| BE-12 | P2 | 进行中 | 风险与缺陷管理 | 已新增 `BugRecord` 持久化、admin 维护入口和外部 issue provider/id/url 链接字段，`admin.bug.*` 审计快照会记录链接变化；`AuditLog` 已覆盖 admin、学校、班级、课程、单元、作业、提交、批改、内容草稿创建、脚本审核、登录成功/失败/锁定和退出审计，并带 request_id、IP 哈希、user-agent、结果、失败原因和 `prev_hash/current_hash` 应用层链式哈希；审计链完整性校验已提供 `GET /api/admin/audit-logs/chain-integrity` 和 `app.services.audit_chain` 复用服务，可重算 current hash、检查相邻 prev hash、报告历史空 hash 与截断状态，并以 `admin.audit.chain_integrity` 记录摘要；审计日志 JSON/CSV 明细导出已支持复用筛选、默认剥离快照、显式快照导出、`limit/truncated` 截断标记、哈希字段保留和 CSV 公式前缀中和，并以 `admin.audit.export` 反向记录导出格式、筛选与摘要；审计报表摘要已按 action/resource_type/actor_role/event_result/failure_reason 聚合，并以 `admin.audit.report` 记录格式、筛选和 bucket 数量；审计高频候选摘要已按 action、actor/action、ip/action、resource/action 和 failure_reason 聚合，并以 `admin.audit.high_frequency` 记录不含候选明细的摘要；审计留存预检已按配置或查询参数计算候选、临期数量、聚合桶和哈希链边界，并以 `admin.audit.retention_plan` 记录 observe-only 摘要；`scripts.archive_audit_logs` 已能生成本地 JSONL/CSV 归档包与 Manifest，并校验文件 SHA-256、记录数和 hash-chain 状态；后续仍需文档化风险系统迁移、正式外部 issue 自动创建/双向同步、外部对象存储/WORM、第三方时间戳、外部锚定、删除/清理策略和正式告警/处置策略 |

---

## 三、风险与缺陷入口

详细清单和证据位置见 [`07-后端优化与设计.md`](07-后端优化与设计.md#九当前-bug--风险统计)。本文只保留规划层摘要。

| 分类 | 数量 | 处理方向 |
| ---- | ---- | -------- |
| BE 后端能力缺口 | 3 | BE-01、BE-02、BE-05、BE-08 和 BE-12 的 MySQL 联调、内容脚本真实执行隔离/CSP/资产 allowlist、管理端 UI、审计外部对象存储/WORM/外部锚定/删除清理策略/正式高频告警与处置策略、权限矩阵、知识快照告警/任务队列/队列堆积治理和真实服务部署验证持续处理 |
| Review 回流待办 | 2 | 前端离线修复选择性吸收和前端浏览器测试入口纳入；active 草稿数据库级抗并发、closed 作业只读复盘、direct join 口径、跨班级提交唯一性、后台内容页数据库侧分页、必填文本修剪后校验、用户名大小写规范化、发布冲突 409、学生资源可见性、中文路径迁移测试和中文 slug 回归已完成并迁入发布历史 |
| SEC 安全与隐私 | 3 | CORS、学生数据权限边界、审计留存策略、强设备绑定和长期会话治理继续先行设计 |
| FE 缓存、路由与状态迁移稳定性 | 6 | 后端 schema 试点前配套整理缓存、脚本 ready 检测、本地状态迁移与 API 缓存边界 |
| QA / DOC 未闭环项 | 2 | 后续按体验专项或内容审校专项处理，不在本轮展开 |
| OPS 开发服务与部署 | 1 | BE-10 已完成文档、本地预检和部署 smoke 首轮；真实 MySQL/反代/服务注册仍需实机验证 |

---

## 四、本阶段不做事项

1. 不立即删除当前 C++ 静态服务。
2. 不一次性重写全部前端页面。
3. 不把学生访问历史、知识状态或作业数据存入普通前端缓存作为正式方案。
4. 不让教师自定义脚本绕过审核或安全讨论直接公开执行。
5. 不在首阶段把代码空间运行记录、AI 助教和 OJ 判题做成完整作业闭环，只预留模型接口。
6. 不在本文展开已完成 v6.3 / v6.4 历史细节；需要查历史时看 `03-发布历史.md`。

---

## 五、验收方式

| 阶段 | 最低验收 |
| ---- | -------- |
| 后端骨架 | API 本地启动、MySQL 连接可用、健康检查通过、pytest 可运行；部署预检可验证数据库和 Alembic head，部署 smoke 可验证表结构与 `/api/health` |
| 内容协议 | 工科试验室试点页面 schema 有样例、有校验、有前端消费路径；正式内容初始化可显式创建/修复内置内容页及版本；教师/管理员可创建草稿，系统会记录脚本静态分析风险和 sandbox 摘要，管理员可审核脚本，公开 render 会剥离原始脚本引用并返回不可执行 manifest，脚本历史版本 rollback 必须新建草稿重审；草稿可在 `draft` / `changes_requested` 状态编辑并重算脚本策略，草稿可提交/退回/撤回，管理员可发布已提交草稿并写入不可变版本，草稿绑定 base 版本，同一作者同一目标页只允许一个 active 草稿且由数据库唯一约束兜底，当前页保留 current 指针，版本保留 previous 链，版本可 diff 且带 semantic 摘要，回滚会追加新版本；未发布草稿不会进入公开 render |
| 登录权限 | 本地账号可注册登录；管理员、教师、学生角色可区分；弱密码拒绝、失败锁定、管理员密码重置、用户自助密码重置令牌、密码重置 token 留存清理、活动会话列表/单会话撤销、会话设备标识、last_seen 追踪/节流、禁用用户会话撤销和越权访问有测试覆盖 |
| 学校班级 | 教师和学生可在学校内通过 legacy/direct join 直接加入，也可提交 join request 进入审批流；班级教师或管理员可审批申请；普通业务端点统一复用访问控制服务层，教师可看到所属学校和相关班级 |
| 三端雏形 | 管理端、教师端、学生端各有最小可用入口；学生端作业中心需保留 closed / archived 历史复盘只读路径 |
| 管理端统计 | 学校/班级深度统计和待批改队列可由 admin 读取，后续正式 UI 再单独验收 |
| 管理端列表 | 用户、学校、班级、加入申请、内容页、审计日志和缺陷清单统一返回 `items/total/limit/offset/next_offset`，并覆盖关键字搜索或状态/范围过滤 |
| 规则统计 | 个人知识状态和教师班级聚合可按班级/课程读取；个人/班级知识快照可按班级、课程、粒度和时间窗分页读取；周期重算脚本和进程内调度器可按日/周窗口重建快照并记录运行结果，且通过数据库租约、自动心跳、管理端健康摘要和协作式取消降低多 worker 重复执行与失控长任务风险；外部告警、任务队列、requeue、队列堆积治理与 UI 另行验收 |
| 前端接入 | 工科试验室试点页面可通过 opt-in 后端 schema 渲染，默认静态回退不破坏 |
| 文档收尾 | 完成项迁入 `03-发布历史.md`，本文只保留下一阶段事项 |

---

## 六、已确认决策

| 编号 | 决策 | 影响 |
| ---- | ---- | ---- |
| D-01 | 第一批后端生成页面选择工科试验室实验页 | 复用既有模块化特色和页面模板，降低试点风险 |
| D-02 | 登录先采用简单本地账号注册 + 登录，并对接 MySQL | 暂不引入强身份验证；先完成基础用户体系，密码策略和失败锁定先落在数据库状态表 |
| D-03 | 需要学校/组织层级；教师和学生都在学校内选择加入班级，并支持 direct join 与加入申请审批并存 | 管理端、教师端、学生端都要围绕学校和班级建立资源边界；后续 UI 不应把审批流表现成唯一加入路径 |
| D-04 | 教师自定义页面原则上允许脚本，但具体脚本需视情况讨论和审核 | 先搭建扩展框架，为后续更多用户群体辅助开发预留能力 |
| D-05 | 作业首阶段暂不完整接入代码空间运行记录 | 代码空间偏代码测试；后续二期规划 AI 助教和 OJ 作业模块 |
| D-06 | 学生画像先采用规则统计 | 当前已按作业频次、数据量、完成率、正确率和积分做在线聚合，并新增个人/班级聚合快照、周期重算运行记录、进程内调度器、数据库租约防重入、自动心跳和管理端健康摘要；后续再补外部告警、任务队列、单元/概念细分或模型分析 |

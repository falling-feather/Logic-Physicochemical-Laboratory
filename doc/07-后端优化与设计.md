# 星序 Astra — 后端优化与三端平台设计

> **制定日期**: 2026-07-03 | **最后更新**: 2026-07-08 | **状态**: v6.5 后端化设计草案；`houduan` 已接入 FastAPI 骨架、内容协议持久化、内容 seed 启动初始化与读取无副作用边界、正式内容初始化入口、ContentDraft 草稿、脚本审核、脚本静态分析风险等级、脚本 sandbox 契约、脚本资产 allowlist/SRI 静态门禁、发布版本绑定外部脚本资产镜像、管理端脚本资产供应链清单、内容脚本镜像一致性审计、内容脚本远端漂移扫描、内容脚本扫描 run 台账与远端漂移告警候选摘要、内容脚本远端漂移扫描调度/CLI observe-only 首轮、内容脚本扫描 run 健康/队列摘要、内容脚本远端漂移告警 outbox 人工复核入队与状态流转、内容脚本 CDN host 信任治理首轮、公开 render 脚本 manifest 脱敏与沙箱执行契约头、稳定 `sectionId/sourceId` 内容身份、脚本历史版本 rollback 重审门禁、草稿编辑、提交/退回/撤回工作流、active 草稿数据库唯一约束、内容发布/版本记录/追加式回滚、发布/回滚冲突 409、内容页 current 指针、草稿 base version/hash、版本 previous 链、发布元数据、管理端版本 JSON path diff 敏感预览脱敏与带显式稳定 ID 字段的 semantic 富语义摘要、前端 opt-in schema 试点、本地认证安全基线、活动会话列表与单会话撤销、会话设备标识与 last_seen 追踪/节流、管理员密码重置、用户自助密码重置令牌、密码重置 token 留存清理脚本、禁用用户会话撤销、用户名大小写规范化与 normalized key 数据库唯一约束、必填文本修剪后校验、学校/班级成员列表、学生 membership 状态维护、admin-only 教师 membership 状态治理、班级教师 membership 转让/批量治理、学校/班级加入申请审批与管理端队列、学校/班级/课程访问控制服务层、跨范围权限矩阵测试、课程/作业/学习事件/提交批改/学生侧只读复盘/跨班级提交唯一性/学生资源状态可见性/积分统计、知识状态/班级规则统计、个人/班级知识快照、知识快照周期重算运行记录、进程内调度器、数据库租约防重入与自动心跳、管理端知识快照运行列表/健康摘要/调度积压摘要、告警候选摘要、告警 outbox 人工复核台账、状态流转、队列摘要与批量复核、协作式取消与手动 requeue、管理端 API、学校/班级深度统计、管理端列表分页搜索、管理端内容页数据库侧分页、待批改队列、缺陷记录外部 issue 链接、审计元数据、认证事件审计、审计日志链式哈希、审计链完整性校验、审计日志 JSON/CSV 明细导出、报表摘要导出、审计高频候选摘要、审计留存预检、本地审计归档包导出/Manifest 校验与导出/摘要行为审计留痕、API no-store 缓存边界、部署预检 MySQL/`utf8mb4` 门禁与部署 smoke 门禁；2026-07-06 已回流 `review` 分支审查报告结论但未合并 review 代码修复
> **最新回归**: 2026-07-07，BE-04/BE-05/BE-06/BE-08 已继续补齐同校非本班教师隔离、teacher direct join 审批收口、班级教师 membership 转让/批量治理、课程结构作者权限、课程协作者 editor 首轮、课程 owner 转让首轮、学校/班级统计 scoped 读取、待批改队列 teacher scope、班级成员列表、学生 membership 状态维护、admin-only 教师 membership 状态治理与 assignment 级积分规则首轮：班级成员列表、学生状态维护、课程挂接班级、指定班级提交列表、待批改队列、提交批改、学习事件查询、积分流水、学生进度和班级深度统计均要求对应班级 active teacher membership；同校非本班教师不能通过 legacy direct join 自助成为班级 teacher，必须提交 join request 并由本班 active teacher 或 admin 审批；学校深度统计要求对应学校 active teacher/admin membership；课程单元、作业创建和 assignment 级积分规则维护要求课程创建者、active editor 或全局 admin，协作者管理和课程 owner 转让仅限课程创建者或全局 admin；普通班级教师可批量维护本班 student membership、按规则批改自己班级提交，但不能批量维护 teacher membership，也不能改写非自己课程或未授权课程的共享作业规则；全局 admin 可维护、批量维护和转让 teacher membership，但禁用班级最后一个 active teacher 会返回 `409`；教师不带 `class_id` 查看作业提交、待批改队列、学习事件或积分流水时只返回自己任教班级内的数据，全局 admin 保留跨范围治理能力，全站 `/api/admin/stats` 与待批改 `status=graded` 治理视图仍保持 admin-only。BE-10 已为 `scripts.deploy_preflight --require-mysql` 补齐 MySQL 方言与 `utf8mb4` 字符集/排序规则门禁；真实 MySQL DSN、网络路径、部署账号权限、反向代理和服务注册仍归入 BE-10/BE-12 实机验证。
> **账号不变量**: 2026-07-06，R-21 已新增 `users.normalized_username` 与 `login_attempts.normalized_username` 数据库唯一约束，账号查找和登录锁定桶均由 normalized key 兜底；历史重复用户迁移前必须人工清洗。
> **审计与缺陷治理快照**: 2026-07-07，BE-12 已新增 `BugRecord.external_issue_provider/external_issue_id/external_issue_url`，管理端创建/更新会修剪字段，列表关键字搜索覆盖 issue 元数据，`admin.bug.create/update` 快照记录链接变化；新写入 `AuditLog` 会保存 `prev_hash/current_hash` 应用层 SHA-256 链式哈希，查询与 JSON/CSV 明细导出均保留哈希字段；审计日志已新增 `GET /api/admin/audit-logs/export` JSON 明细导出、`GET /api/admin/audit-logs/export.csv` CSV 明细导出、`GET /api/admin/audit-logs/report` JSON 报表摘要、`GET /api/admin/audit-logs/report.csv` CSV 报表摘要、`GET /api/admin/audit-logs/high-frequency` 高频候选摘要、`GET /api/admin/audit-logs/retention-plan` 留存预检和 `GET /api/admin/audit-logs/chain-integrity` 链完整性校验，复用审计筛选或时间窗边界；明细导出默认剥离 `snapshot_json`，显式 `include_snapshot=true` 才返回快照；CSV 明细响应附带 `X-Audit-Export-*` 元数据头并中和表格公式前缀；报表按 action/resource_type/actor_role/event_result/failure_reason 聚合，CSV 报表附带 `X-Audit-Report-*` 元数据头；高频摘要默认最近 24 小时，按 action、actor/action、ip/action、resource/action 和 failure_reason 聚合候选，成功后写入 `admin.audit.high_frequency`，只记录筛选、时间窗、阈值、总量和维度命中数，不记录候选明细；留存预检默认按 `ASTRA_AUDIT_LOG_RETENTION_DAYS=365` 计算 cutoff，也支持 `retention_days` 或 `before` 预览，返回归档候选、临期数量、聚合桶和哈希链边界，并写入 `admin.audit.retention_plan` 摘要；链完整性校验复用 `app.services.audit_chain`，重算 `current_hash`、检查相邻 `prev_hash`、把历史空 hash 标记为 partial，并写入不含 issue 明细的 `admin.audit.chain_integrity` 摘要；`scripts.archive_audit_logs` 已支持本地 JSONL/CSV 归档包和 Manifest 校验，Manifest 记录 SHA-256、导出数量、截断状态、链边界和 hash-chain 重算状态；外部 issue 自动创建/双向同步、对象存储/WORM、第三方时间戳、外部锚定、删除/清理策略和正式告警/处置策略仍待推进。
> **会话治理快照**: 2026-07-07，认证依赖已拆出 `AuthContext(user, session)` 以支持当前会话识别；`GET /api/auth/sessions` 返回当前用户未撤销、未过期会话，标记 `is_current` 并返回 best-effort `device_label`、登录 user-agent 和 `last_seen_at`；有效鉴权会按 `ASTRA_SESSION_LAST_SEEN_UPDATE_SECONDS` 节流刷新 `AuthSession.last_seen_at/last_seen_ip_hash`；admin 禁用用户和 admin 密码重置都会撤销目标用户未撤销 `AuthSession`。BE-03 已新增用户自助密码重置令牌与离线留存清理脚本：请求泛化响应、按账号哈希/IP 哈希冷却、生产不回传 token，确认阶段行锁消费一次性 token，成功后撤销会话并清理登录失败桶；`scripts.cleanup_password_reset_tokens` 默认 dry-run，显式 `--apply` 才删除已用或已过期终态 token，摘要不返回用户名、IP 哈希、user-agent 或 token hash。强设备绑定、长期会话策略、并发禁用/登录事务语义、生产调度/演练和真实 MySQL 验证仍归入后续治理；真实邮件/短信/MFA 投递暂列 `P4 / 最低优先级 / 暂缓`。
> **知识快照调度快照**: 2026-07-08，BE-08 已在 `knowledge_snapshot_runs` 上新增 scheduler lease owner/token/expires/heartbeat 元数据，并新增 `admin_alert_outbox_entries` 作为告警 outbox 人工复核台账；FastAPI 进程内调度器和 `scripts.rebuild_knowledge_snapshots` CLI 统一先抢占 `run_key` 租约再重算，未过期 running 窗口跳过，过期租约可接管，长重算循环会按配置间隔以 owner/token guard 自动续租，成功/失败释放使用 token guard 防止旧 worker 覆盖新状态；管理端已新增 `GET /api/admin/knowledge-snapshot-runs` 运行列表、`GET /api/admin/knowledge-snapshot-runs/health` 健康摘要、`GET /api/admin/knowledge-snapshot-runs/queue` 调度积压摘要、`GET /api/admin/knowledge-snapshot-runs/alerts` 告警候选摘要、`POST /api/admin/knowledge-snapshot-runs/alerts/outbox` 告警候选显式 outbox 入队、`GET /api/admin/alert-outbox` outbox 列表、`GET /api/admin/alert-outbox/queue` outbox 队列摘要、`PATCH /api/admin/alert-outbox/reviews` 批量人工复核、`POST /api/admin/knowledge-snapshot-runs/{id}/cancel` 协作式取消和 `POST /api/admin/knowledge-snapshot-runs/{id}/requeue` 手动重排；健康摘要汇总 stale running、lease expiring、claimable、retryable/exhausted failed 和 problem runs，积压摘要区分 dispatchable now、claimable by lease rule、manual requeue 与 blocked runs，告警候选摘要复用 health/queue 视角输出 severity/action hint，outbox 按 source/run/code/action dedupe 幂等刷新 `pending_review` 人工复核项，队列摘要只读聚合 pending/planned/queued/terminal、stale/due 和 severity bucket，批量复核仅按显式 ID all-or-nothing 写入人工状态且不返回 payload/备注正文，取消会将 pending 或带 scheduler lease 的 running run 标记为 `cancelled`、清空租约且不返回 `scheduler_lease_token`；requeue 会将 failed、cancelled 或过期带租约 running run 重置为 pending，调度器会扫描 pending run 并重新抢占执行；无租约 legacy running、active running 和 success run 返回 `409`。当前 alerts/outbox/queue/reviews 仍是 observe-only 管理面，不投递外部消息、不自动 requeue/cancel/清理；正式外部告警投递、外部任务队列、队列执行治理和真实 MySQL 并发取消/重排演练仍待推进。
> **内容协议治理快照**: 2026-07-08，BE-02 已新增 `ASTRA_CONTENT_SCRIPT_ALLOWED_HOSTS`、`policy_context_hash`、稳定 `sectionId/sourceId` 身份、diff 稳定身份响应、diff 敏感预览、外部脚本资产下载/SRI 校验、校验证据 metadata、发布版本绑定的外部脚本资产镜像、管理端脚本资产供应链清单、内容脚本镜像一致性审计、内容脚本远端漂移扫描、内容脚本扫描 run 台账与远端漂移告警候选摘要、内容脚本远端漂移扫描调度/CLI observe-only 首轮、内容脚本扫描 run 健康/队列摘要、内容脚本远端漂移告警 outbox 人工复核入队、内容脚本 CDN host 信任治理首轮、公开 render 沙箱执行契约头、后端 sandbox embed 描述符、sandbox HTML 首轮、后端受控本地脚本资产端点、后端 bootstrap JS 端点和 sandbox HTML 响应级 CSP nonce：外部 `scriptUrl/scriptSrc` 默认阻断，配置 host 后仍需显式 `https://`、无 query/fragment、合法 SRI、`crossorigin=anonymous` 和管理员脚本审核；管理员批准外部脚本和发布已审核草稿时会下载资产并按声明 SRI 比对字节，下载失败、SRI mismatch 或发布前 CDN 字节漂移会阻断流程，默认下载器不跟随重定向；发布成功后，外部脚本字节以当前 `page_version_id + sandbox_id + reference_value_sha256` 写入 `content_script_assets`；管理端 `GET /api/admin/content/script-assets` 提供只读、分页、可过滤的镜像资产清单，按内容页、版本、sandbox、引用 hash、镜像字节 hash、策略版本与发布时间观察供应链状态，响应和审计均不返回原始 CDN URL、完整 SRI 或 `content_bytes`；管理端 `GET /api/admin/content/script-assets/mirror-audit` 提供只读离线一致性审计，复核当前 published schema 与 `content_script_assets` 的版本绑定、source/integrity 元数据、本地镜像字节 SHA-256/大小/SRI、重复引用和缺失镜像，写入 `admin.content_script_asset.mirror_audit` 聚合审计摘要，响应与审计只返回 host/hash/问题码，不返回原始 CDN URL、完整 SRI 或镜像字节；管理端 `POST /api/admin/content/script-assets/remote-drift-scan` 提供显式确认的小批量远端漂移扫描，按当前远端字节与发布时镜像 SHA-256、大小和声明 SRI 比对，写入 `admin.content_script_asset.remote_drift_scan` 聚合审计摘要，响应与审计不返回原始 CDN URL、完整 SRI、远端字节或异常明细；`GET /api/admin/content/script-assets/remote-drift-scan-runs`、`/health`、`/queue` 与 `GET /api/admin/content/script-assets/remote-drift-alerts` 提供 run 台账、failed/stale/lease expiring/current bucket/manual review/blocked 候选摘要和只读 action hint，`POST /api/admin/content/script-assets/remote-drift-alerts/outbox` 可将候选以 `content_script_asset_scan_run_alert` source type 幂等写入本地 `pending_review` 人工复核台账；这些响应、payload 和审计不返回 `scheduler_lease_token`、原始 CDN URL、完整 SRI、远端字节、异常原文或 `content_bytes`；默认关闭的调度器和必须显式确认外网的 CLI 只提供非交互 observe-only 触发入口，不修改 host policy、不替换镜像、不下线或重发布内容、不投递外部告警；管理端 `GET/PATCH /api/admin/content/script-host-policies` 合并已观测 host、配置 allowlist host 与持久化 policy，维护 `trusted/watch/blocked` 状态、原因与审阅人，`blocked` host 会阻断草稿创建/编辑、脚本审核、发布和回滚，`trusted/watch` 不绕过 allowlist/SRI/审核/发布前下载校验；`script_integrity_verified` 与 `script_integrity_mismatch` finding 会在 metadata 中保留资产 SHA-256、字节大小、SRI token 数量和匹配算法；allowlist 配置变化会触发审核/发布前重分析；公开 render 剥离脚本 URL、SRI/crossorigin 元数据和 sandbox 原始字段，仅返回带稳定 `sandboxId` 的 `scriptManifest`，其中 `scriptManifest.sandbox` 会按 `network=none/same-origin` 派生稳定 CSP、返回 enforcement/capabilities 并对 unsafe sandbox 防御性降级，`GET /api/render/page/{slug}` 会返回 `X-Astra-Content-Script-*` 稳定契约头，并在可执行且全页唯一的 manifest 中返回 `scriptManifest.embed`，用于描述同源 iframe `src/sandbox/referrerPolicy`、opaque origin、messageProtocol、capabilities 和 assetCount；`GET /api/render/script-sandboxes/{sandbox_id}/page/{slug}` 会按已发布私有 schema 生成 sandbox HTML，每次响应生成不可预测 nonce，将响应级 `script-src` 收紧为 nonce source，bootstrap `<script>` 携带同一 nonce，并对 missing、blocked、歧义和缺失镜像资产 fail closed；`GET /api/render/script-sandboxes/{sandbox_id}/bootstrap/page/{slug}` 会生成受控资产 URL 列表、`bootstrap-v1`、ready/error/postMessage 契约，并通过 `document.currentScript.nonce` 把 HTML nonce 传给动态资产脚本；`GET /api/render/script-sandboxes/{sandbox_id}/assets/{asset_sha256}/page/{slug}` 会按 published slug、当前 page version、sandboxId 和引用 SHA-256 返回受控根内本地 JS 或版本绑定外部镜像字节，防止 schema 直接选择任意同源路径或外部 CDN；新写入草稿、发布和内置初始化必须带稳定 section/source ID，semantic diff 优先按稳定 ID 识别章节和来源，并在 section/source 变更项显式返回 before/after 稳定 ID；管理端版本 diff 的 raw changes 与 semantic field/prop changes 会对 token/key/secret/script/sandbox/integrity/crossorigin 等敏感字段返回结构化 redaction preview，非敏感字段保持原值。前端实际 iframe 编排、实时监控、生产外部告警/自动处置、自动信任/封禁策略和浏览器自动化隔离证明仍待推进。
>
> **V6.6.19 内容脚本镜像增量**: 外部脚本资产镜像第一刀已落地为“发布版本绑定镜像”，新增 `content_script_assets` 持久化表和 `app.services.content_script_assets` 服务。发布成功后，外部脚本字节以 `page_version_id + sandbox_id + reference_value_sha256` 绑定当前版本；render 阶段只读该绑定资产，不联网、不代理任意 CDN、不暴露原始 URL。BE-02 剩余设计焦点收窄为前端 iframe 编排、完整 CDN 供应链治理、浏览器自动化隔离证明和生产试运行硬化。
> **V6.6.20 sandbox embed 描述符增量**: public render 已为可执行且非歧义的 `scriptManifest` 注入 `embed` 描述符，稳定提供 iframe `src/sandbox/referrerPolicy`、messageProtocol、capabilities 和 assetCount；descriptor 不含 nonce、原始脚本 URL/路径值、SRI/crossorigin、bootstrap URL、asset URL、镜像资产 metadata 或数据库 id。BE-02 剩余设计焦点收窄为前端真实 iframe 生命周期编排、postMessage 状态接入、浏览器自动化隔离证明、完整 CDN 供应链治理和生产镜像审计/告警。
> **V6.6.21 知识快照告警候选摘要增量**: 管理端新增 `GET /api/admin/knowledge-snapshot-runs/alerts`，从 health/queue 报告派生 stale running、lease expiring、retryable/exhausted failed、pending backlog、manual requeue 和 blocked 候选，按 critical/warning/info 返回 action hint，并以 `admin.knowledge_snapshot_run.alert_report` 记录不含候选明细和敏感字段的审计摘要。该端点只定义 UI/外部告警前置契约，不发送邮件/短信/Webhook，不接入 broker，不自动处置 run。
> **V6.6.22 脚本资产供应链清单增量**: 管理端新增 `GET /api/admin/content/script-assets`，只读分页查看已发布版本绑定的 `content_script_assets`，支持 slug、host、sandbox、page/version、发布人、策略版本、策略 hash、资产 hash、引用 hash、时间窗和关键字筛选；响应提供 source host、source URL hash、引用 hash、镜像字节 hash、大小、策略版本和发布时间，不暴露原始 CDN URL、完整 SRI 或镜像字节；成功查询写入 `admin.content_script_asset.inventory` 聚合审计摘要。该端点是供应链清单/观察面，不做 CDN 实时扫描、自动信任裁决、外部告警或资产处置。
> **V6.6.23 内容脚本镜像一致性审计增量**: 管理端新增 `GET /api/admin/content/script-assets/mirror-audit`，只读扫描当前 published 内容页和当前版本 schema，按 `page_version_id + sandbox_id + reference_value_sha256` 复核镜像绑定、source/integrity 元数据、本地镜像字节 SHA-256/大小/SRI 和重复引用；响应返回 issue code、severity、host、引用 hash、source URL hash、asset hash 和版本定位信息，并写入 `admin.content_script_asset.mirror_audit` 脱敏聚合审计。该端点不联网、不访问远端 CDN、不发送外部告警、不自动修复/删除/重发资产。
> **V6.6.24 内容脚本远端漂移扫描增量**: 管理端新增 `POST /api/admin/content/script-assets/remote-drift-scan`，显式 `confirm_external_network=true` 后按 `limit/offset` 小批量扫描当前 published 外部脚本引用，重新读取远端当前字节并与发布时镜像 SHA-256、大小和声明 SRI 比对；响应返回 drift/unavailable/too-large 等脱敏 issue，并写入 `admin.content_script_asset.remote_drift_scan` 聚合审计。该端点不是定时实时监控，不自动信任/封禁 host，不替换镜像、不删除资产、不下线或重发布内容，不发送外部告警。
> **V6.6.25 内容脚本 CDN host 信任治理增量**: 新增 `content_script_host_policies` 与 `app.services.content_script_host_policies`，管理端通过 `GET/PATCH /api/admin/content/script-host-policies` 维护 source host 的 `trusted/watch/blocked` 状态、原因、审阅人和审阅时间；列表合并已观测脚本镜像 host、环境 allowlist host 和持久化 policy。`blocked` host 会在草稿创建/编辑、脚本审核、发布和回滚前 fail closed；`trusted/watch` 不绕过 `ASTRA_CONTENT_SCRIPT_ALLOWED_HOSTS`、SRI、`crossorigin=anonymous`、管理员脚本审核或发布前下载校验。
> **V6.6.26 内容脚本扫描 run 台账增量**: 新增 `content_script_asset_scan_runs` 与 `app.services.content_script_asset_scan_runs`，手动远端漂移扫描成功后写入 `remote_drift` run key、触发来源、触发人、筛选条件、totals、issue code/severity 聚合、限量脱敏 issue 摘要和 `alert_status`；管理端通过 `GET /api/admin/content/script-assets/remote-drift-scan-runs` 分页读取台账，通过 `GET /api/admin/content/script-assets/remote-drift-alerts` 从最近 run 派生 observe-only severity/action hint。本轮不做定时扫描、外部告警投递、自动封禁/信任、镜像替换或内容重发布。
> **V6.6.27 内容脚本漂移调度/CLI 增量**: `content_script_asset_scan_runs` 支持 `running/failed/success` 生命周期、可空创建人、`attempt_count`、唯一 `run_key` 和 scheduler lease owner/token/expires/heartbeat 元数据；新增默认关闭的 `app.services.content_script_asset_scan_scheduler.ContentScriptRemoteDriftScheduler` 与必须显式 `--confirm-external-network` 的 `scripts.scan_content_script_asset_remote_drift` CLI。管理端 run 列表不返回 `scheduler_lease_token`，告警候选摘要会纳入 failed run 与 stale running run。该能力仍是 observe-only 非交互触发入口，不是实时监控、生产外部告警、自动 host policy 裁决、镜像替换或内容重发布系统。
> **V6.6.28 内容脚本扫描 run 健康/队列摘要增量**: 新增 `GET /api/admin/content/script-assets/remote-drift-scan-runs/health` 与 `/queue`，从 `content_script_asset_scan_runs` 和调度配置只读派生 health/queue 观察面，覆盖 failed、stale running、lease expiring、legacy running、warning/critical issue run、当前 scheduler bucket、manual review 与 blocked run；新增聚合审计 `admin.content_script_asset.remote_drift_scan_run.health_report` 和 `admin.content_script_asset.remote_drift_scan_run.queue_report`。响应与审计不返回 `scheduler_lease_token`、原始 CDN URL、完整 SRI、远端字节或 issue 明细；不触发扫描、不投递外部告警、不自动 requeue/cancel/封禁 host、不替换镜像、不下线或重发布内容。
> **V6.6.30 内容脚本告警 outbox 增量**: 新增 `POST /api/admin/content/script-assets/remote-drift-alerts/outbox`，显式 `confirm_observe_only=true` 后将远端漂移告警候选以 `content_script_asset_scan_run_alert` 写入 `admin_alert_outbox_entries`。dedupe key 绑定 run key、候选来源、code/action 与 host/hash/asset hash 定位信息，重复入队只刷新 `seen_count/last_seen_at/payload_hash`；payload 只保留脱敏 run 摘要、source host、source URL hash、引用 hash、镜像/远端 SHA-256 和远端大小，不保存原始 CDN URL、完整 SRI、远端字节、异常原文、`content_bytes` 或 `scheduler_lease_token`。该能力仍是本地人工复核台账，不投递外部告警、不自动封禁 host、不替换镜像、不下线或重发布内容。
> **V6.6.31 告警 outbox 人工复核状态流转增量**: 新增 `PATCH /api/admin/alert-outbox/{id}`，请求体需 `confirm_manual_review=true`，可将知识快照与内容脚本告警 outbox 条目人工标记为 `pending_review/planned/queued/suppressed/cancelled`，并记录 `reviewed_by_user_id/reviewed_at/review_note`；重复入队只刷新观察计数与 payload hash，不覆盖已人工复核状态。复核审计 `admin.alert_outbox.review` 仅保存脱敏状态摘要，不记录 payload、备注正文、原始 CDN URL、完整 SRI、远端字节、异常原文、`content_bytes` 或 `scheduler_lease_token`；仍不外部投递、不接入 broker、不自动 requeue/cancel/封禁 host、不替换镜像、不下线或重发布内容。
> **V6.6.32 告警 outbox 队列摘要增量**: 管理端新增 `GET /api/admin/alert-outbox/queue`，从 `admin_alert_outbox_entries` 只读派生 queue report，覆盖 pending_review、planned、queued、suppressed、cancelled、stale pending review、due planned/queued、severity bucket 和限量条目，并以 `admin.alert_outbox.queue_report` 写入聚合审计。该端点不返回 payload、复核备注、原始 CDN URL、完整 SRI、远端字节、异常原文、`content_bytes` 或 `scheduler_lease_token`；`queued` 仍是人工复核状态，不代表外部 broker 队列，不触发投递或自动处置。
> **V6.6.33 告警 outbox 批量人工复核增量**: 管理端新增 `PATCH /api/admin/alert-outbox/reviews`，仅接受显式 ID 列表、目标状态和 `confirm_manual_review=true`，最多 100 条，重复 ID 422、缺失 ID 404 且 all-or-nothing；响应使用不含 payload/备注正文的瘦身队列条目，审计 `admin.alert_outbox.bulk_review` 只记录 ID、计数和 source/event/severity/status 分布。该端点不外部投递、不接 broker、不自动 requeue/cancel、不封禁 host、不替换镜像、不下线或重发布内容。
>
> **关联文档**: [`02-更新规划.md`](02-更新规划.md) · [`01-开发者手册.md`](01-开发者手册.md) · [`04-部署指南.md`](04-部署指南.md)
>
> **文档边界**: 本文只描述后端优化、前后端协作方式、登录与三端角色体系、数据模型、阶段路线和当前风险统计。具体实现代码、部署命令和已完成版本记录仍分别归入开发者手册、部署指南和发布历史。

---

## 一、背景判断

当前星序已经形成三个主要内容面：

| 内容面 | 当前状态 | 后端化影响 |
| ------ | -------- | ---------- |
| 工科试验室 | 主站内的成熟教学星系，保留数学、物理、化学、算法、生物等二级学科和大量交互实验，且已有较强模块化基础和页面模板参考 | 作为第一批后端生成页面试点，把实验元信息、学习路径、题库、进度和访问历史从前端常量中抽离 |
| 代码空间 | `/codevis/` 独立子站，已有多语言运行时抽象 | 首阶段不纳入完整作业闭环；后续二期再接入 AI 助教、OJ 作业模块、代码提交和判题记录 |
| 未来星系 | 正式产品内容保留，已完成基础 UI 外壳和学习框架 | 需要继续模块化，避免继续用静态长页面堆叠内容 |

目前项目更接近“静态前端应用 + 轻量 C++ 静态文件服务器”。后端只承担静态托管、健康检查、服务器信息和占位 API，并没有登录、用户、课程、班级、作业、内容管理、权限管理或持久化学习记录。

下一阶段的关键不是简单增加几个接口，而是把前端内容模块化、归一化，让后端能够生成、组合和发布页面结构。前端应逐渐从“内容写死在页面里”转向“稳定渲染壳 + 后端内容协议 + 可复用组件”。

---

## 二、目标与原则

### 2.1 总目标

1. 建立 Python + MySQL 后端框架，逐步替代当前 C++ 静态服务承担业务逻辑。
2. 将页面、课程、实验、题目、学习路径、作业和统计数据统一建模。
3. 添加登录、用户管理和权限控制，形成管理员端、教师端、学生端三端体系。
4. 支持从后端生成页面配置、课程配置和学习任务，而不是只依赖前端静态部署渲染。
5. 保持现有主站可访问，采用渐进迁移，不一次性打断已有页面。

### 2.2 设计原则

| 原则 | 说明 |
| ---- | ---- |
| 内容数据化 | 星系、学科、实验、章节、题目、来源和学习任务都应进入结构化数据模型 |
| 前端壳稳定 | 前端保留路由、组件、动画和交互实验能力，但页面结构由后端协议驱动 |
| Python 优先 | C++ 继续作为当前历史实现保留，业务后端优先使用 Python 降低维护成本 |
| 权限前置 | 登录、角色、学校、班级、课程、作业和学生数据从第一阶段就纳入权限设计 |
| 可回滚迁移 | 后端生成页面先从新路由或试点页面开始，保留静态页面作为回退 |
| 教学可审计 | 教师操作、管理端内容发布、学生学习事件和积分变化都需要可追踪 |
| 隐私保护 | 学生访问历史、知识状态和作业表现属于敏感学习数据，默认最小暴露 |

### 2.3 已确认产品口径

| 编号 | 决策 | 设计影响 |
| ---- | ---- | -------- |
| D-01 | 后端生成页面第一试点选择工科试验室实验页 | 优先复用既有模块化实验结构和页面模板，降低 schema 试点风险 |
| D-02 | 登录先采用简单本地账号注册 + 登录，并对接 MySQL | 暂不做强身份验证、统一身份认证或复杂实名体系 |
| D-03 | 需要学校/组织层级 | 教师可看到所属学校及相关班级；教师和学生都需要在学校内选择加入班级 |
| D-04 | 教师自定义页面原则上允许脚本，但具体脚本需视情况讨论和审核 | 内容扩展框架需要预留脚本能力、审核状态、安全标记和降级策略 |
| D-05 | 代码空间作业记录放到后续二期 | 当前作业先围绕页面、实验、题目和提交；AI 助教与 OJ 判题后续接入 |
| D-06 | 学生画像先采用规则统计 | 当前只按作业频次、数据量、完成率、正确率等百分比统计，后续再考虑模型分析 |

---

## 三、推荐技术路线

### 3.1 后端语言与框架

建议采用 Python 作为后续主业务后端：

| 层级 | 推荐方案 | 说明 |
| ---- | -------- | ---- |
| Web 框架 | FastAPI | 类型友好、OpenAPI 自动生成、适合教学平台 API 逐步扩展 |
| 数据模型 | Pydantic + SQLModel 或 SQLAlchemy | 统一接口校验、数据库映射和文档生成 |
| 数据库 | MySQL | 用户、学校、班级、课程、作业、学习事件和权限关系需要关系型模型 |
| 迁移工具 | Alembic | 记录数据库结构演进 |
| 鉴权 | 简单本地账号注册 + 登录，优先服务端 Session Cookie | 早期降低接入成本，不引入强身份验证 |
| 权限 | RBAC + 学校/班级/课程资源范围检查 | 角色不够，还要校验用户是否属于学校、班级或课程范围 |
| 后台任务 | APScheduler / Celery / RQ 视规模选择 | 规则统计刷新、内容生成等任务可异步执行；当前先以 FastAPI lifespan 进程内调度器承接 BE-08 日/周重算基础，并以数据库租约、自动心跳、管理端健康摘要、调度积压摘要、告警候选摘要、告警 outbox 人工复核台账/状态流转/队列摘要/批量复核、协作式取消和手动 requeue 降低多副本重复执行与失控长任务风险；外部队列、正式告警投递和队列执行治理仍按规模后续接入 |
| 缓存 | Redis 可选 | 登录 Session、统计缓存、生成结果缓存 |
| 测试 | pytest + httpx + Playwright | API、权限、页面生成、前端渲染都要进入回归 |

### 3.2 与现有 C++ 服务的关系

短期不建议立即删除 C++ 服务。更稳妥的路线是：

1. 保留 `server/main.cpp` 作为历史静态服务与本地部署方案。
2. 新增 Python 后端目录，例如 `backend/`，先提供独立 API。
3. 前端通过配置切换 API base URL。
4. 待 Python 后端稳定后，再决定是否把静态托管也交给 Python、Nginx 或其他部署层。

### 3.3 当前落地基线（2026-07-07）

`houduan` 分支已新增 `backend/` 作为 Python 业务后端首切片：

> **Review 前架构快照**：团队整体 review 建议以 `houduan@c9a2b41` 为锚点，功能切片锚点为 `c32d3d5 V6.5.22 内容脚本 sandbox 契约`。该状态不是最终完成态，但已经是干净、可解释、可回滚的阶段性基线；review 后若改动 API 契约、数据库迁移、权限边界、内容协议或脚本安全策略，应同步更新本文、`02-更新规划.md` 和 `03-发布历史.md`。
>
> **Review 报告回流边界**：2026-07-06 已只读审阅 `review` 分支审查报告。本 `houduan` 文档只吸收结论与后续任务，不声明 review 分支代码修复已经进入 `houduan`；后续代码吸收必须重新在 `houduan` 上实现或选择性迁入并复测。

> **后端阶段读数**：截至 `V6.5.65 内容脚本沙箱执行契约`，按后端 API 与核心业务闭环估算，后端首阶段约完成 75%；按可上线生产级完整三端系统估算，整体约完成 55% 至 60%。当前已经具备阶段性后端 review 价值，但生产试运行仍依赖真实 MySQL/部署联调、内容脚本浏览器级执行隔离、正式三端 UI、前端回归自动化和审计/调度生产化。

> **v6.6.X 项目后端开发收束计划**：后续约 20 到 60 个小版本集中在 `6.6.X` 内推进；`V6.6.0` 作为中版本开篇，`V6.6.1` ~ `V6.6.12` 聚焦 MySQL/部署/权限矩阵第一组生产化，`V6.6.13` 收掉 teacher direct join 审批缺口，`V6.6.14` 已完成班级教师 membership 转让/批量治理首轮，`V6.6.15` 已完成脚本 sandbox HTML 首轮，`V6.6.16` 已完成后端受控本地脚本资产端点，`V6.6.17` 已完成后端 sandbox bootstrap 端点，`V6.6.18` 已完成 sandbox HTML 响应级 CSP nonce，`V6.6.19` 已完成发布版本绑定的外部脚本资产镜像，`V6.6.20` 已完成后端 sandbox embed 描述符，`V6.6.21` 已完成知识快照告警候选摘要，`V6.6.22` 已完成脚本资产供应链清单，`V6.6.23` 已完成内容脚本镜像一致性审计，`V6.6.24` 已完成内容脚本远端漂移扫描，`V6.6.25` 已完成内容脚本 CDN host 信任治理首轮，`V6.6.26` 已完成内容脚本扫描 run 台账首轮，`V6.6.27` 已完成内容脚本漂移调度/CLI observe-only 首轮，`V6.6.28` 已完成内容脚本扫描 run 健康/队列摘要，`V6.6.29` 已完成知识快照告警 outbox 人工复核台账首轮，`V6.6.30` 已完成内容脚本远端漂移告警 outbox 人工复核入队，`V6.6.31` 已完成告警 outbox 人工复核状态流转，`V6.6.32` 已完成告警 outbox 队列摘要，`V6.6.33` 已完成告警 outbox 批量人工复核；后续从 `V6.6.34` 起继续推进前端真实 iframe 编排、浏览器隔离自动化验证、实时监控、生产告警/自动处置与生产试运行级后端，`V6.6.35` ~ `V6.6.45` 聚焦三端 UI 接入，`V6.6.46` ~ `V6.6.60` 聚焦系统硬化。邮件/短信/MFA 投递进入 `V6.6.P4 / 暂缓池`，当前为 `P4 / 最低优先级 / 暂缓`。

| 范围 | 当前状态 | 剩余缺口 |
| ---- | -------- | -------- |
| FastAPI 应用 | 已建立 `app.main:create_app()`、统一 `/api` 前缀、OpenAPI 入口、Admin / Auth / Users / Schools / Classes / Courses / LearningEvents / Submissions / Points / Progress / Knowledge / Content / Render 路由 | 后续继续拆分统计服务层和更细 RBAC |
| 数据库连接 | 已建立 SQLAlchemy 数据库探针、P0/P1 ORM 模型和 Alembic 迁移至 `20260708_0035`；默认目标为 MySQL URL，测试使用 SQLite 覆盖；`deploy_preflight --require-mysql` 会报告并校验 MySQL 方言、数据库/连接字符集和排序规则 | 需要本机/部署环境 MySQL 实例和迁移落库验证 |
| 健康检查 | `GET /api/health` 返回服务、版本、环境和数据库状态；部署预检脚本可检查数据库连通、Alembic head、MySQL 方言和 `utf8mb4` 字符集/排序规则；部署 smoke 可复用预检并检查表结构集合、MySQL 方言门禁和 `/api/health` 运行态 | 需要纳入正式进程管理、日志和外部可观测性 |
| 登录用户 | `POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/sessions`、`DELETE /api/auth/sessions/{id}`、`POST /api/auth/password-reset/request`、`POST /api/auth/password-reset/confirm`、`GET /api/users/me` 已形成本地闭环；公开注册仅允许 teacher / student；注册、admin bootstrap、admin 密码重置和自助密码重置确认共享密码强度策略，`users.normalized_username` 兜底大小写变体唯一性；登录失败进入持久化锁定；当前用户可列出/撤销自己的活动会话；自助重置请求泛化响应并按账号哈希/IP 哈希冷却，生产环境不回传 token，确认阶段行锁消费一次性 token，成功后撤销会话、清理登录失败桶；`scripts.cleanup_password_reset_tokens` 可按留存窗口 dry-run 或显式 `--apply` 清理已用/已过期终态 token；登录成功/失败/锁定、退出、单会话撤销和密码重置写入审计 | 需要强设备绑定、长期会话策略、更细 RBAC、清理脚本生产调度/演练和真实 MySQL 迁移验证；真实邮件/短信/MFA 投递标记为 `P4 / 最低优先级 / 暂缓` |
| 学校班级 | `POST /api/schools`、`POST /api/classes`、`POST /api/classes/{id}/join`、`GET /api/classes/{id}/members`、`PATCH /api/classes/{id}/members/{membership_id}`、`PATCH /api/classes/{id}/members/batch-status`、`POST /api/classes/{id}/teachers/transfer`、`POST/GET/PATCH /api/classes/{id}/join-requests`、`GET/PATCH /api/admin/class-join-requests` 支持学校/班级范围、student legacy/direct join、teacher join request 审批、班级成员列表、学生 membership 状态维护/批量维护、admin-only 教师 membership 状态治理/批量治理/转让、direct join 收口 pending 申请、班级教师审批和管理端加入申请队列；普通业务端点已通过 `app.services.access_control` 复用学校、班级和课程可见性判断，并补充外部学生/教师跨范围访问拒绝测试；同校非本班教师不能通过 direct join 自助成为班级 teacher，不能读取该班成员列表、维护学生状态、挂接该班课程、读取该班提交、待批改队列、批改该班提交、读取该班学习事件、积分流水、学生进度、班级深度统计或改写他人课程结构/共享作业规则；学校深度统计要求对应学校 teacher/admin scope；禁用用户旧会话撤销已由 admin 用户状态维护收口 | 需要复杂归属异常处理、移除/转班、批量导入、多角色/批量协作者、班级专属作业模型、多角色/内容发布和管理端治理队列等更细权限矩阵 |
| 课程作业事件 | `GET/POST /api/courses`、课程挂班、课程 owner 转让、课程协作者 editor、课程单元、作业创建、`GET/POST /api/learning-events`、作业提交、学生侧作业复盘、教师批改、积分流水、assignment 级积分规则、个人进度摘要、个人/班级规则统计、个人/班级知识快照、日/周周期重算运行记录、进程内调度器、数据库租约防重入、自动心跳、管理端运行健康摘要、调度积压摘要、告警候选摘要、告警 outbox 人工复核台账/状态流转/队列摘要/批量复核、协作式取消和手动 requeue 已形成首轮闭环；同一作业挂接多个班级时提交按 `assignment/student/class` 区分，教师提交列表、批改、学习事件、积分流水和学生进度按 class teacher scope 收束，课程单元、作业创建和作业积分规则维护按 course creator / active editor / global admin scope 收束；课程 owner 转让要求原 owner 或全局 admin 且目标为同校 active teacher/admin；批改积分按“规则目标积分 - 当前 submission 已入账 assignment_grade 积分”写入差额流水；学生只读取 published 课程、published 单元和 active 作业，published 课程/单元内 closed / archived 作业不可再次提交但仍可只读查看本人提交、成绩和反馈；学生个人 progress/knowledge/snapshot 使用同一可见性口径，教师/班级聚合保持管理口径 | 需要真实服务部署验证、知识快照正式外部告警投递/任务队列/队列执行治理、正式三端 UI、多角色/批量协作者、班级专属作业模型、班级专属积分覆盖规则、更细权限矩阵和关闭作业后统计分母口径确认 |
| 管理端 API | `/api/admin/bootstrap`、用户管理、管理员密码重置、学校/班级/内容页/内容草稿/内容版本历史查看、内容脚本资产供应链清单、内容脚本镜像一致性审计、内容脚本远端漂移扫描、内容脚本扫描 run 台账/健康摘要/队列摘要与远端漂移告警候选摘要、内容脚本远端漂移告警 outbox 入队、知识快照告警 outbox 入队与 outbox 列表/队列摘要/批量复核、全站统计、学校/班级深度统计、管理端列表分页搜索、内容页数据库侧分页搜索、加入申请队列、草稿队列、待批改队列、`BugRecord` 缺陷清单与外部 issue 链接字段、`GET /api/admin/audit-logs`、`GET /api/admin/audit-logs/export`、`GET /api/admin/audit-logs/export.csv`、`GET /api/admin/audit-logs/report`、`GET /api/admin/audit-logs/report.csv`、`GET /api/admin/audit-logs/retention-plan`、`GET /api/admin/audit-logs/chain-integrity` 和 `GET /api/admin/audit-logs/high-frequency` 已形成首轮后端入口；`GET /api/admin/content/script-assets` 只读查看发布版本绑定脚本镜像清单，`GET /api/admin/content/script-assets/mirror-audit` 只读复核当前 published schema 与镜像绑定、本地字节 hash/大小/SRI 和重复引用，`POST /api/admin/content/script-assets/remote-drift-scan` 小批量触发远端漂移扫描并写入 `content_script_asset_scan_runs`，`GET /api/admin/content/script-assets/remote-drift-scan-runs`、`/health`、`/queue` 与 `GET /api/admin/content/script-assets/remote-drift-alerts` 提供只读台账、租约状态、failed/stale/lease expiring/current bucket/manual review/blocked 候选摘要和 action hint，`POST /api/admin/content/script-assets/remote-drift-alerts/outbox` 需显式 observe-only 确认并以 `content_script_asset_scan_run_alert` 写入 `pending_review` 人工复核项，响应、payload 与审计都不暴露 `scheduler_lease_token`、原始 CDN URL、完整 SRI、异常原文或镜像/远端字节；`POST /api/admin/knowledge-snapshot-runs/alerts/outbox` 需显式 observe-only 确认，按 dedupe key 写入 `pending_review` 人工复核项，`GET /api/admin/alert-outbox` 可筛选本地 outbox，`GET /api/admin/alert-outbox/queue` 可只读聚合 pending/planned/queued/terminal、stale/due 和 severity bucket，`PATCH /api/admin/alert-outbox/reviews` 可按显式 ID all-or-nothing 批量写入人工状态且不返回 payload/备注正文，相关入口都不外部投递、不自动处置；全站统计和治理类入口保持 admin-only，学校/班级深度统计已拆为全局 admin 或对应 school/class teacher scope 读取，待批改队列已拆为 admin 全局视图和教师本班 `submitted/returned` 视图，`status=graded` 仍为 admin-only；`scripts.archive_audit_logs` 已提供本地审计归档包导出与 Manifest 复验；用户状态置为 `disabled` 时撤销未撤销会话并在 `admin.user.update` 快照记录 `revoked_sessions`；密码重置会撤销目标用户未撤销会话、清理登录失败桶并写入 `admin.user.password_reset`；关键组织/教学/内容写操作、认证事件、导出行为、高频摘要行为、链完整性校验行为、留存预检行为、脚本资产清单查询、镜像一致性审计、远端漂移扫描、扫描 run 列表、扫描 run 健康/队列摘要、远端漂移告警候选摘要和告警 outbox 入队/列表/复核/队列摘要/批量复核已进入 `AuditLog`，并带 request_id、IP 哈希、user-agent、结果、失败原因和 `prev_hash/current_hash` 链式哈希元数据 | 需要正式管理端 UI、正式外部 issue 同步、对象存储/WORM/第三方时间戳/外部锚定、删除/清理策略、正式告警/处置策略和管理端治理队列等更细权限矩阵 |
| 内容协议 | `content_pages` 已持久化首个工科试验室实验页 schema，并维护当前 schema hash、current version 指针和发布元数据；开发/测试初始化阶段显式 seed 样例；`scripts.init_content_pages` 可在迁移和预检通过后正式创建/修复内置内容页与版本，要求 active admin 发布归因、脚本引用确认、稳定 `sectionId/sourceId`、差异冲突保护和活跃草稿升级阻断，且已覆盖中文目录/中文 SQLite 文件名下的迁移与初始化回归；`GET /api/content/pages/*` 与 `GET /api/render/page/{slug}` 只读 published 当前记录并通过 Pydantic 校验后返回，且公开响应会剥离原始脚本引用、SRI/crossorigin 元数据和 sandbox 原始字段并保留带稳定 `sandboxId` 的 `scriptManifest`，manifest sandbox 会按 `network=none/same-origin` 派生稳定 CSP、返回 enforcement/capabilities 并对 unsafe sandbox 防御性降级，render API 会返回 `X-Astra-Content-Script-*` 稳定契约头；可执行且全页唯一的 manifest 会带 `embed` 描述符，脱敏提供 iframe `src/sandbox/referrerPolicy`、messageProtocol、capabilities、assetCount 和 opaque origin 语义，blocked/ambiguous manifest 不生成可执行 embed；`GET /api/render/script-sandboxes/{sandbox_id}/page/{slug}` 会按已发布私有 schema 生成 sandbox HTML，每次响应生成运行时 nonce，把响应级 `script-src` 收紧为 nonce source，bootstrap `<script>` 携带同一 nonce，并对 missing、blocked、歧义和缺失镜像资产 fail closed；`GET /api/render/script-sandboxes/{sandbox_id}/bootstrap/page/{slug}` 会按 published slug 与 sandboxId 生成受控资产 URL 列表、`bootstrap-v1`、ready/error/postMessage 契约，并把 HTML nonce 传给动态资产脚本；`GET /api/render/script-sandboxes/{sandbox_id}/assets/{asset_sha256}/page/{slug}` 会按 published slug、当前 page version、sandboxId 和引用 SHA-256 返回受控根内本地 JS 或版本绑定外部镜像字节，响应 `application/javascript`、引用 hash、镜像字节 hash 和 `nosniff`；`GET /api/admin/content/script-assets` 可观察发布版本绑定的脚本镜像清单，按 host、sandbox、版本、策略与 hash 维度分页筛选，响应用 source URL hash 替代原始 CDN URL；`GET /api/admin/content/script-assets/mirror-audit` 可离线复核当前 published schema 与 `content_script_assets` 的版本绑定、source/integrity 元数据、本地字节 SHA-256/大小/SRI 和重复引用，定位缺失镜像、陈旧绑定和字节证据不一致问题；`POST /api/admin/content/script-assets/remote-drift-scan` 可在 admin 显式确认后小批量读取远端当前字节并与发布镜像 hash/大小/SRI 比对，定位 CDN 静默替换、不可用或超大响应等候选问题，并写入可追溯的 scan run 台账；`GET /api/admin/content/script-assets/remote-drift-scan-runs`、`/health`、`/queue` 与 `GET /api/admin/content/script-assets/remote-drift-alerts` 可只读查看 run 聚合、租约状态、failed/stale/lease expiring/current bucket/manual review/blocked 候选和告警候选，不外部投递、不自动处置；`ContentScriptRemoteDriftScheduler` 默认关闭，`scripts.scan_content_script_asset_remote_drift` 必须显式确认外部网络访问，二者都不修改 host policy、不替换镜像、不下线或重发布内容；`GET/PATCH /api/admin/content/script-host-policies` 可维护 CDN source host 的 `trusted/watch/blocked` 治理状态，列表合并已观测镜像 host、环境 allowlist host 与持久化 policy，`blocked` 会在草稿创建/编辑、脚本审核、发布和回滚前 fail closed，`trusted/watch` 不绕过 allowlist/SRI/审核/发布前下载校验；`content_drafts` 支持教师/管理员创建草稿、绑定 base version/hash、active key 数据库唯一约束、记录脚本静态分析结果、policy context hash 和 sandbox 摘要、管理员脚本审核、作者不可自审、`draft` / `changes_requested` 草稿编辑、submit/request-changes/withdraw 工作流、管理员发布已提交草稿并回填发布元数据；新建/编辑/发布草稿要求稳定 `sectionId/sourceId`，编辑草稿会重算 schema hash 与脚本策略、清空旧脚本审核且不自动 rebase；外部脚本默认阻断，allowlist 后仍需 `https`/无 query fragment/SRI/`crossorigin=anonymous`，审核批准和发布阶段会后端下载资产并按声明 SRI 比对字节，下载失败、SRI mismatch、host policy blocked 或发布前 CDN 漂移会阻断，发布成功后会写入当前版本绑定的外部脚本资产镜像；脚本历史版本 rollback 会被要求新建草稿重审；`content_page_versions` 记录不可变 schema_hash、版本快照和 explicit previous 链，管理端可做同 slug 版本 JSON path diff 与带显式稳定 ID 字段的 semantic 富语义摘要，raw changes 与 semantic field/prop changes 会对敏感字段返回 redaction preview，sections/sources 优先按稳定 ID 识别，回滚采用追加新版本而非改写历史；中文 slug 已覆盖创建、发布、百分号编码公开读取、后台搜索和版本过滤 | 需要前端实际 iframe 编排、实时监控、生产外部告警/自动处置、自动信任/封禁策略和浏览器自动化隔离证明 |
| 前端接入 | `shared/js/backend-content.js` 以 opt-in 方式读取后端 schema，并在 `physics/energy-conservation` 静态实验主体上方渲染学习任务卡；默认不启用，保留静态回退；Python API 统一返回 `Cache-Control: no-store`，降低业务 JSON 被静态缓存污染的风险 | 需要扩展组件类型、API 错误提示、离线策略和更多页面回归 |
| 测试入口 | `python -m pytest backend` 已覆盖健康检查、API no-store、部署预检、部署 smoke、schema 成功与 404、正式内容初始化、中文数据库路径迁移/初始化、内容草稿/编辑/提交/退回/撤回/脚本审核/发布/中文 slug 公开读取与管理查询/发布冲突 409/版本 JSON path diff/semantic 摘要/回滚/回滚冲突 409、认证、弱密码拒绝、登录失败锁定、管理员密码重置、用户自助密码重置、密码重置 token 留存清理、活动会话列表与单会话撤销、会话设备标识、last_seen 刷新与节流、用户名大小写规范化、normalized key 数据库唯一约束、重复用户名迁移阻断、重复登录失败桶迁移清理、过期会话撤销、禁用用户会话撤销、认证事件审计、必填文本修剪后校验、审计元数据过滤、审计链式哈希、审计链完整性校验、admin bootstrap/用户管理/统计/分页搜索/内容页数据库侧分页/缺陷清单外部 issue 链接/审计日志查询、JSON/CSV 明细导出、报表摘要导出与导出审计留痕/学校班级深度统计/加入申请队列/草稿队列/待批改队列、学校班级、班级成员列表、学生 membership 状态维护、admin-only 教师 membership 状态治理、direct join 与加入申请审批并存、课程作业学习事件、提交批改积分统计、跨班级提交唯一性、同校非本班教师隔离、学生资源状态可见性、closed/archived 作业复盘只读语义、组织/教学/内容关键写操作审计、知识状态/班级聚合、个人/班级快照幂等和越权访问；in-app browser 已验证 opt-in schema 卡片渲染 | 需要 MySQL 集成测试、更多权限矩阵和前端渲染回归自动化 |

> **管理端 outbox 复核契约**：`GET /api/admin/alert-outbox` 提供本地告警 outbox 查询，`GET /api/admin/alert-outbox/queue` 提供只读队列摘要，`PATCH /api/admin/alert-outbox/reviews` 提供显式 ID 批量人工复核，`PATCH /api/admin/alert-outbox/{id}` 负责单条人工复核状态流转；复核请求必须带 `confirm_manual_review=true`，状态限定为 `pending_review/planned/queued/suppressed/cancelled`，列表、队列摘要和复核审计均只保存脱敏摘要，不触发外部投递或自动处置。

### 3.4 Review 报告映射（2026-07-06）

| 审查结论 | 设计影响 | `houduan` 后续状态 |
| -------- | -------- | ---------------- |
| active 草稿仍缺数据库级抗并发 | 内容协议不能只依赖应用层查询保护活跃草稿唯一性 | 已在 `houduan` 落地：`active_key` + `(author_user_id, target_slug, active_key)` 唯一约束；迁移前若已有重复 active 草稿需人工清理 |
| closed 作业不应从学生侧完全消失 | 学生端需要区分“当前作业”和“历史/复盘只读入口” | 已在 `houduan` 落地：`GET /api/assignments/{id}/review` 仅开放学生侧，只读返回题目、本人提交、成绩和反馈；closed / archived 不可再次提交，教师/admin 管理视角仍走 submissions/grade |
| 直接加入班级被确认保留 | 组织边界需要同时支持 legacy direct join 与 join request 审批流 | 已在 `houduan` 文档化：`POST /api/classes/{id}/join` 继续服务学生直接加入、admin 治理、受控导入/邀请码或旧 UI 兼容；teacher 角色的普通教师加入必须走 `join-requests` 审批流；后续 UI 不应暗示审批流是唯一入口，也不应让普通教师绕过审批成为班级 teacher |
| review 分支修复了多类后端输入、权限、并发、迁移测试问题 | 这些属于后端不变量：修剪后必填、用户名规范、资源状态可见性、跨班级唯一性、发布冲突 409、内容页 DB 侧分页 | P1，跨班级提交唯一性、内容页数据库侧分页、必填文本修剪后校验、用户名大小写规范化、发布冲突 409 和学生资源可见性已在 `houduan` 分批落地；其余按批次选择性吸收并复测 |
| review 分支修复了前端生命周期、路由、Service Worker 和离线测试问题 | BE-09 需要从“单页 opt-in smoke”升级为可复用浏览器回归入口 | P1，评估吸收 `npm run test:frontend*` 与离线 E2E |

---

## 四、目标架构

### 4.1 分层结构

| 层级 | 责任 |
| ---- | ---- |
| 前端渲染壳 | 路由、组件、动画、实验交互、响应式布局和局部降级 |
| 内容协议层 | 定义页面 JSON、实验卡片、章节、题目、来源、任务和组件属性 |
| 后端 API 层 | 用户、课程、班级、作业、进度、积分、内容、统计和管理接口 |
| 生成服务层 | 根据课程、角色、班级、学习状态生成页面结构和学习任务 |
| 数据层 | 用户、角色、内容、课程、作业、学习事件、积分流水、审计日志 |
| 运营管理层 | 管理端、教师端、学生端的控制台和统计视图 |

### 4.2 后端生成页面的基本思路

前端不再直接假定某个页面一定由固定 HTML 与固定 JS 组成，而是读取后端页面配置：

```json
{
  "slug": "physics/energy-conservation",
  "galaxy": "englab",
  "subject": "physics",
  "title": "能量守恒",
  "layout": "experiment-page",
  "sections": [
    {
      "type": "hero",
      "title": "能量守恒",
      "summary": "观察动能、势能和内能之间的转换"
    },
    {
      "type": "experiment",
      "experimentId": "energy-conservation",
      "props": {
        "defaultFriction": 0.1
      }
    },
    {
      "type": "assessment",
      "questionSetId": "physics-energy-basic"
    }
  ]
}
```

前端负责解释 `layout`、`sections` 和组件类型，后端负责根据课程、角色、班级和发布状态生成这些结构。

---

## 五、核心数据模型草案

| 模型 | 关键字段 | 说明 |
| ---- | -------- | ---- |
| User | id, username, display_name, password_hash, status | 本地账号注册与登录用户 |
| AuthSession | user_id, token_hash, device_label, user_agent, expires_at, last_seen_at, last_seen_ip_hash, revoked_at, created_at | 登录会话；当前已支持活动会话列表、当前会话识别、best-effort 设备摘要、last_seen 追踪/节流与单会话撤销 |
| LoginAttempt | username, failure_count, locked_until, last_failed_at | 登录失败限流和短时锁定状态；当前默认 15 分钟窗口内失败 5 次锁定 15 分钟 |
| Role | admin, teacher, student | 基础角色，可扩展为多角色 |
| Permission | action, resource | 管理端精细权限 |
| School | id, name, region, status | 学校/组织层级 |
| SchoolMembership | school_id, user_id, role, status | 用户与学校关系 |
| ClassGroup | school_id, name, grade, term, status | 学校内班级 |
| ClassMembership | class_id, user_id, role, status | 教师和学生加入班级后的关系；teacher membership 支持 admin-only 状态维护、批量治理与转让，student membership 支持班级教师或 admin 状态维护/批量维护 |
| ClassJoinRequest | school_id, class_id, user_id, role, status, message, requested_by_user_id, reviewed_by_user_id, reviewed_at, review_note | 教师/学生选择加入班级的申请与审批记录；同一班级/用户/角色唯一，支持 pending / approved / rejected；普通教师加入 teacher role 必须走该审批路径，legacy direct join 命中 pending 时会同步转 approved，避免后台残留假待审 |
| Course | school_id, creator_user_id, title, summary, status | 课程集合；当前 `creator_user_id` 同时承担 current owner 语义，owner 转让会改写该字段，后续若需保留原始创建者再拆 `owner_user_id` |
| CourseCollaborator | course_id, user_id, role, status | 课程协作者；首轮仅支持 active/inactive `editor`，协作者管理限课程创建者或全局 admin |
| CourseUnit | course_id, order, page_slug, experiment_id | 课程内学习单元 |
| Assignment | class_id, course_id, title, due_at, policy, point_rule_json, future_code_mode | 作业布置；`point_rule_json` 存储 assignment 级积分规则，`NULL` 表示使用默认规则；预留代码空间/OJ 二期扩展 |
| Submission | assignment_id, student_id, class_id, content, status, score, feedback | 作业提交与批改；唯一性按 `assignment_id + student_id + class_id` 收口，学生侧复盘入口可按 `class_id` 定位本人提交，closed / archived 作业保持只读历史可见 |
| ProgressEvent | user_id, page_slug, event_type, payload, created_at | 访问、完成、练习、运行等学习事件 |
| KnowledgeStat | user_id, class_id, course_id, concept_id/unit_id, frequency, sample_size, percent, evidence | 基于频次、数据量和百分比的规则统计；当前首轮以在线响应 schema 返回，后续再决定是否物化表 |
| UserKnowledgeSnapshot | user_id, class_id, course_id, granularity, period_start, period_end, rule_version, aggregate fields, knowledge_stats_json | 学生个人知识状态物化首轮；只允许本人读取/重算，同一用户/班级范围/课程范围/粒度/时间窗/规则版本幂等更新 |
| ClassKnowledge | class_id, course_id, students_total, students_active, completion_percent, accuracy_percent, evidence | 教师班级学情聚合；当前首轮在线计算并可写入班级快照 |
| ClassKnowledgeSnapshot | class_id, course_id, granularity, period_start, period_end, rule_version, aggregate fields, knowledge_stats_json | 班级知识状态周期物化首轮；同一班级/课程范围/粒度/时间窗/规则版本幂等更新，不保存原始提交正文或学习事件 payload |
| KnowledgeSnapshotRun | run_key, granularity, period_start, period_end, trigger_source, status, started_at, finished_at, scheduler_lease_owner, scheduler_lease_token, scheduler_lease_expires_at, scheduler_heartbeat_at, attempt_count, counts, error_message, metadata_json | 日/周周期重算的运行记录；同一窗口使用 run_key 幂等复用并以数据库租约防重入，只保存状态、尝试次数、租约元数据、计数、错误类型和聚合元数据，不保存原始提交正文或学习事件 payload |
| AdminAlertOutboxEntry | source_type, source_id, source_key, event_code, severity, action_hint, status, dispatch_mode, delivery_target, external_delivery, dedupe_key, payload_hash, payload_json, first_seen_at, last_seen_at, seen_count, reviewed_by_user_id, reviewed_at, review_note | 管理端告警 outbox 人工复核台账；承接知识快照告警候选与内容脚本远端漂移告警候选，按 source/run/code/action 与 host/hash/asset hash 定位信息幂等去重，只保存脱敏摘要；管理员可通过 `PATCH /api/admin/alert-outbox/{id}` 标记 `pending_review/planned/queued/suppressed/cancelled`，重复入队不覆盖已复核状态，默认 `external_delivery=false` 与 `dispatch_mode=manual_review` |
| PointLedger | user_id, delta, reason, source_id | 积分流水，禁止只存总分；作业批改使用 `assignment_grade` 原因并写入规则目标与当前 submission 已入账积分的差额 |
| ContentPage | slug, schema, status, version, schema_hash, current_version_id, published_by_user_id, published_at | 后端生成页面当前快照；公开 content/render 只返回 `published`，发布和回滚都会更新 current 指针与发布元数据 |
| ContentDraft | author_user_id, target_slug, title, status, active_key, schema_json, schema_hash, base_version_id, base_schema_hash, allow_script, script_risk_level, script_analysis_json, script_review_status, script_reviewed_by_user_id, script_reviewed_at, script_review_note, submitted_at, withdrawn_at, change_requested_by_user_id, change_requested_at, change_request_note, published_page_id, published_version_id, published_by_user_id, published_at | 教师或管理员的内容草稿；当前支持创建、绑定当前 published base 版本、管理端队列、`draft` / `changes_requested` 草稿编辑、脚本静态分析、脚本审核、`draft -> submitted -> changes_requested -> submitted -> published`、活跃草稿撤回和发布元数据回填；`active_key='active'` 与 `(author_user_id, target_slug, active_key)` 唯一约束保证同一作者同一目标页只有一个活跃草稿，撤回/发布后置空以保留历史；编辑会重算 schema hash 与脚本策略并清空旧脚本审核，但不改变 base version/hash；脚本草稿按风险进入 pending，非脚本草稿为 not_required，作者不可自审 |
| ContentPageVersion | page_id, slug, version, schema_hash, schema_json, source_draft_id, restored_from_version_id, previous_version_id, published_by_user_id, published_at, note | 内容发布历史；发布和回滚只追加版本，`previous_version_id` 指向发布/回滚前当前版本，回滚会生成新的当前版本，不改写历史版本 |
| AuditLog | actor_user_id, actor_role, action, resource, resource_type, resource_id, school_id, class_id, event_result, failure_reason, request_id, client_ip_hash, user_agent, request_method, request_path, prev_hash, current_hash, snapshot_json, created_at | 管理、发布、权限变更、认证事件和关键教学操作审计；已覆盖 admin bootstrap、用户更新、BugRecord 维护、学校/班级创建、班级加入、加入申请创建/审批、课程创建、课程挂班、单元/作业创建、作业提交、批改、内容草稿创建、草稿编辑、脚本审核、内容发布、内容回滚、登录成功/失败/锁定和退出；客户端 IP 仅保存带盐哈希，内容审计不保存完整 schema 或脚本正文；`prev_hash/current_hash` 是应用层链式哈希，不等同外部不可篡改账本 |
| BugRecord | title, category, severity, status, source, external_issue_provider, external_issue_id, external_issue_url | 缺陷与风险管理；外部 issue 字段仅保存引用元数据，不调用外部系统 API |

---

## 六、三端功能设计

### 6.1 管理端

管理端是全站最高层级，重点是“看全局、控权限、管内容、审风险”。

| 模块 | 功能 |
| ---- | ---- |
| 用户与角色 | 创建、停用、重置用户；分配管理员、教师、学生角色 |
| 学校与班级 | 管理学校/组织；查看学校内班级、教师归属、学生归属、加入申请和异常关系 |
| 内容中心 | 管理星系、学科、课程、实验、页面配置、题库和来源索引 |
| 发布审核 | 审核教师自定义页面、课程包、作业模板、脚本能力和公开内容 |
| 全站统计 | 访问量、课程完成率、实验使用率、错误率、缓存命中和活跃用户 |
| 风险与缺陷 | 汇总 bug、权限风险、内容口径风险和待回归项目 |
| 审计日志 | 查看登录、权限变更、内容发布、删除、批量导入和关键教学操作记录 |

### 6.2 教师端

教师端面向课堂与教学组织，重点是“管班级、布置任务、看学生、定制内容”。

| 模块 | 功能 |
| ---- | ---- |
| 学校视图 | 查看自己所属学校，以及学校内可见的其他班级 |
| 班级管理 | 在学校内选择加入或管理班级、导入学生、分组、查看活跃状态 |
| 课程进度 | 设置课程路径、安排单元顺序、查看班级整体进度 |
| 作业布置 | 从实验、页面、题库或自定义内容生成作业 |
| 批改反馈 | 查看提交、打分、写评语、退回修改 |
| 积分管理 | 按完成、参与、优秀提交、补交等规则调整积分 |
| 页面定制 | 为班级增添说明、任务卡、资料链接、阶段性专题页；脚本能力需进入审核流程 |
| 学情分析 | 查看基于作业频次、数据量、完成率、正确率等百分比形成的规则统计 |

### 6.3 学生端

学生端面向学习体验，重点是“知道学什么、做什么、哪里薄弱、下一步去哪”。

| 模块 | 功能 |
| ---- | ---- |
| 学校与班级 | 选择学校并加入班级，作为后续课程、作业和统计的范围 |
| 个人首页 | 今日任务、课程进度、待交作业、积分和最近访问 |
| 课程管理 | 查看已加入课程、学习路径、完成状态和推荐下一步 |
| 实验记录 | 保存访问历史、实验参数、运行记录和关键观察 |
| 作业中心 | 查看作业、提交答案、接收反馈、补交或重做 |
| 知识状态 | 展示基于作业频次、数据量、完成率和正确率的规则统计 |
| 个性化分析 | 暂不引入模型分析；先根据规则统计生成基础学习建议 |

---

## 七、API 模块草案

| 模块 | 示例接口 | 说明 |
| ---- | -------- | ---- |
| Auth | `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/logout`, `POST /api/auth/password-reset/request`, `POST /api/auth/password-reset/confirm`, `GET /api/auth/sessions`, `DELETE /api/auth/sessions/{id}`, `GET /api/users/me` | 本地账号注册、登录、退出、用户自助密码重置 token、活动会话列表、会话设备摘要、last_seen 追踪/节流、单会话撤销和当前用户；注册/重置拒绝弱密码，连续失败登录返回 `429` 与 `Retry-After`；重置请求泛化响应并冷却，生产不回传 token，确认成功后撤销会话并清理登录失败桶；登录/退出/单会话撤销/密码重置写入审计 |
| Admin | `POST /api/admin/bootstrap`, `GET /api/admin/users`, `PATCH /api/admin/users/{id}`, `POST /api/admin/users/{id}/password-reset`, `GET /api/admin/stats`, `GET /api/admin/schools/{id}/stats`, `GET /api/admin/classes/{id}/stats`, `GET/PATCH /api/admin/class-join-requests`, `GET /api/admin/content/drafts`, `GET /api/admin/submissions/pending`, `GET /api/admin/knowledge-snapshot-runs`, `GET /api/admin/knowledge-snapshot-runs/health`, `GET /api/admin/knowledge-snapshot-runs/queue`, `GET /api/admin/knowledge-snapshot-runs/alerts`, `POST /api/admin/knowledge-snapshot-runs/{id}/cancel`, `POST /api/admin/knowledge-snapshot-runs/{id}/requeue`, `GET /api/admin/audit-logs`, `GET /api/admin/audit-logs/export`, `GET /api/admin/audit-logs/export.csv`, `GET /api/admin/audit-logs/report`, `GET /api/admin/audit-logs/report.csv`, `GET /api/admin/audit-logs/retention-plan`, `GET /api/admin/audit-logs/chain-integrity`, `GET /api/admin/audit-logs/high-frequency`, `GET/POST/PATCH /api/admin/bugs` | 管理员初始化、用户管理、密码重置、全站/学校/班级统计、加入申请队列、内容草稿队列、待批改队列、知识快照运行列表/健康摘要/调度积压摘要/告警候选摘要/协作式取消/手动 requeue、审计日志查询/JSON/CSV 明细导出、报表摘要、留存预检、链完整性校验、高频候选摘要和缺陷清单；全站统计仍 admin-only，学校/班级深度统计分别要求 school teacher scope / class teacher scope，待批改队列对教师仅返回本人任教班级内 `submitted/returned` 提交且 `status=graded` 仍 admin-only；缺陷清单可保存外部 issue provider/id/url 并纳入搜索与审计快照；列表统一返回 `items/total/limit/offset/next_offset`，知识快照管理响应不返回 `scheduler_lease_token` 或 `metadata_json`，告警候选摘要只返回 severity/action hint 且审计不记录候选明细，用户置为 `disabled` 或被重置密码时会撤销未撤销会话并记录影响面，审计日志支持 request_id/event_result/failure_reason 过滤和 `prev_hash/current_hash` 查看，链完整性校验可报告 current hash mismatch、prev hash mismatch、历史空 hash 和截断状态，明细导出默认不含快照但保留哈希字段，CSV 响应附导出元数据头并中和表格公式前缀，成功导出会记录 `admin.audit.export` 或 `admin.audit.report`，留存预检会记录 `admin.audit.retention_plan`，链完整性校验会记录 `admin.audit.chain_integrity`，高频摘要会记录 `admin.audit.high_frequency`；离线 CLI `scripts.archive_audit_logs` 可生成本地归档文件和 Manifest 复验 |
| Schools | `GET /api/schools`, `POST /api/schools`, `GET /api/schools/{id}/classes` | 学校/组织与班级列表；`school.create` 已审计 |
| Classes | `GET /api/classes`, `POST /api/classes`, `POST /api/classes/{id}/join`, `GET /api/classes/{id}/members`, `PATCH /api/classes/{id}/members/{membership_id}`, `PATCH /api/classes/{id}/members/batch-status`, `POST /api/classes/{id}/teachers/transfer`, `POST/GET/PATCH /api/classes/{id}/join-requests` | 班级与教师/学生加入关系、班级成员列表、学生 membership 状态维护/批量维护、admin-only 教师 membership 状态治理/批量治理/转让、student legacy/direct join、teacher join request 审批、加入申请和审批；`class.create` / `class.join` / `class.member.status.update` / `class.member.status.batch_update` / `class.teacher.transfer` / `class.join.request.*` 已审计 |
| Courses | `GET /api/courses`, `POST /api/courses`, `POST /api/courses/{id}/classes`, `PATCH /api/courses/{id}/owner`, `GET/POST /api/courses/{id}/collaborators`, `PATCH /api/courses/{id}/collaborators/{collaborator_id}`, `GET/POST /api/courses/{id}/units` | 课程管理、课程挂班、owner 转让、课程协作者与课程单元；课程单元创建要求课程创建者、active editor 或全局 admin，owner 转让和协作者管理要求课程创建者或全局 admin；`course.create` / `course.class.attach` / `course.owner.transfer` / `course.collaborator.create/update` / `course.unit.create` 已审计 |
| Assignments | `GET /api/courses/{id}/assignments`, `POST /api/courses/{id}/units/{unit_id}/assignments` | 作业布置与查询；作业创建要求课程创建者、active editor 或全局 admin；`assignment.create` 已审计 |
| Submissions | `POST /api/assignments/{id}/submissions`, `GET /api/assignments/{id}/review`, `GET /api/assignments/{id}/submissions`, `PATCH /api/submissions/{id}/grade` | 学生提交、学生侧只读复盘、教师查看提交与批改；提交唯一性按班级 scope 区分，批改按 assignment 积分规则写入差额流水，`submission.create` / `submission.grade` 已审计，复盘入口不写审计、不生成学习事件 |
| Progress | `POST /api/learning-events`, `GET /api/learning-events`, `GET /api/progress/me`, `GET /api/progress/users/{id}` | 学习事件记录、班级范围查询和个人进度摘要；教师查询要求本班 teacher scope |
| Points | `GET /api/points/ledger`, `GET/PATCH /api/points/assignments/{id}/rule` | 个人或班级范围积分流水；教师查询要求本班 teacher scope；assignment 级积分规则学校 teacher/admin 可读，课程创建者、active editor 或全局 admin 可写，默认规则为 `enabled=true`、`points_per_score=1`、`max_points=null` |
| Knowledge | `GET /api/knowledge/me`, `POST /api/knowledge/me/snapshots`, `GET /api/knowledge/me/snapshots`, `GET /api/classes/{id}/knowledge`, `POST /api/classes/{id}/knowledge/snapshots`, `GET /api/classes/{id}/knowledge/snapshots` | 规则统计后的知识状态与个人/教师班级快照；周期重算脚本和 FastAPI lifespan 调度器按日/周窗口批量重算并写入运行记录，调度/CLI 路径通过数据库租约、自动心跳、管理端健康摘要、调度积压摘要、告警候选摘要、协作式取消和手动 requeue 防止同一窗口重复执行或旧 worker 覆盖新状态 |
| Content | `GET /api/content/pages/{slug}`, `POST /api/content/drafts`, `GET /api/content/drafts/{id}`, `PATCH /api/content/drafts/{id}`, `POST /api/content/drafts/{id}/submit`, `POST /api/content/drafts/{id}/withdraw`, `POST /api/content/drafts/{id}/request-changes`, `PATCH /api/content/drafts/{id}/script-review`, `POST /api/content/drafts/{id}/publish`, `POST /api/content/page-versions/{id}/rollback`, `GET /api/admin/content/page-versions`, `GET /api/admin/content/page-versions/{id}/diff` | 页面配置、草稿编辑、脚本静态分析、脚本审核、提交/退回/撤回、发布、版本历史、JSON path schema diff、semantic 摘要和追加式回滚；草稿不进入公开 render，发布后才进入当前 published 快照；默认 diff 沿 `previous_version_id` 链，草稿发布校验 base version/hash 与脚本 policy |
| Render | `GET /api/render/page/{slug}` | 后端生成前端可渲染页面结构 |
| Code Future | `POST /api/code-assignments`, `GET /api/oj/submissions` | 二期预留：AI 助教、OJ 作业和代码运行记录 |
| Bugs | `GET /api/bugs`, `POST /api/bugs` | 缺陷与风险清单 |

---

## 八、阶段路线

| 编号 | 优先级 | 阶段 | 目标 | 验收标准 |
| ---- | ------ | ---- | ---- | -------- |
| BE-01 | P0 | Python + MySQL 后端骨架 | 建立 `backend/`、FastAPI、MySQL 连接、配置、健康检查、测试入口、部署预检和部署 smoke | 本地可启动 `/api/health`，pytest 可运行；部署预检可检查 Alembic head、MySQL 方言和 `utf8mb4` 字符集/排序规则，部署 smoke 可检查表结构和健康状态 |
| BE-02 | P0 | 工科试验室内容协议试点 | 定义页面 schema、组件类型、稳定 section/source 身份、实验引用、课程单元结构、正式初始化和草稿编辑/审核/提交/退回/撤回/发布/回滚入口 | 能生成一个工科试验室实验页 JSON，并由前端试点读取；迁移后可显式初始化内置内容页版本；教师/管理员可创建和编辑草稿，管理员可审核脚本、sandbox 契约和外部脚本 allowlist/SRI 契约，审核/发布阶段可下载校验外部脚本并保留资产指纹 metadata，退回草稿、发布已提交草稿、查看版本、对比版本、按稳定 `sectionId/sourceId` 读取 semantic 变更摘要并追加式回滚；公开 render 不暴露原始脚本引用、SRI/crossorigin 或 sandbox 原始字段，并返回 manifest enforcement/capabilities、后端 sandbox embed 描述符与 `X-Astra-Content-Script-*` 执行契约头 |
| BE-03 | P0 | 本地注册登录与用户模型 | 用户表、密码哈希、登录态、当前用户接口、活动会话列表、会话设备摘要、last_seen 追踪/节流、单会话撤销、管理员密码重置、用户自助密码重置令牌、密码策略和失败锁定 | 支持账号注册/登录/退出，区分管理员、教师、学生；弱密码和连续失败登录可被阻断，用户可查看设备摘要与最后活跃时间，并撤销自己的单个活动会话；管理员可协助重置密码，用户可通过一次性 token 自助重置密码 |
| BE-04 | P0 | 学校、班级与资源范围 | RBAC + 学校/班级/课程/内容归属校验 | 学生可在学校内通过 legacy/direct join 直接加入，也可提交 join request；普通教师加入 teacher role 必须走 join request 审批，admin/受控导入路径保留 direct join；班级教师或管理员可查看成员列表、维护/批量维护学生 membership 状态和审批申请，全局 admin 可维护、批量维护和转让 teacher membership 且不能禁用最后一个 active teacher；管理端可跨班级治理申请；普通业务端点复用访问控制服务层；教师不能访问非范围内学生数据，同校非本班教师不能通过 direct join 自助成为班级 teacher，也不能查看该班成员列表、维护学生状态或处理该班课程、提交、待批改队列、学习事件、积分流水、学生进度、班级深度统计或课程结构；学校深度统计要求对应学校 teacher/admin scope |
| BE-05 | P1 | 管理端基础 | 学校、用户、角色、密码重置、班级、加入申请、内容页、内容草稿、缺陷清单的基础视图 | 管理端能查看并维护核心对象 |
| BE-06 | P1 | 教师端基础 | 所属学校、可见班级、学生、课程路径、作业布置 | 教师能在学校/班级范围内查看成员列表、维护/批量维护学生 membership `active/inactive`，并布置基于页面或实验的作业；全局 admin 可转让或批量治理班级 teacher membership，课程 owner 转让和协作者管理要求课程创建者或全局 admin，课程结构写入和 assignment 级积分规则维护要求课程创建者、active editor 或全局 admin；同一作业挂接多个班级时按班级查看待批改队列、提交并批改，批改按规则目标积分与当前 submission 已入账 `assignment_grade` 积分差额写入流水；成员列表、学生状态维护、查看/批改、学习事件、积分流水和学生进度要求本班 teacher scope；关闭作业后仍可通过提交列表和批改接口查看历史 |
| BE-07 | P1 | 学生端基础 | 选择学校、加入班级、个人首页、课程进度、访问历史、作业中心 | 学生能看到自己的班级 published 课程、published 单元与 active 作业；同一作业在不同班级可分别提交，同班级重复提交返回 409；published 课程/单元内 closed / archived 作业可进入只读复盘，不可再次提交 |
| BE-08 | P1 | 学习事件与规则统计 | 记录访问、实验启动、题目提交、作业提交 | 后端可按作业频次、数据量和百分比生成个人/班级统计；教师查询学习事件、积分流水和学生进度要求本班 teacher scope；个人/班级聚合可按时间窗写入幂等快照；周期脚本和进程内调度器可按日/周窗口重算并记录运行结果，且通过数据库租约、自动心跳、管理端健康摘要、调度积压摘要、告警候选摘要、告警 outbox 人工复核台账/状态流转/队列摘要/批量复核、协作式取消和手动 requeue 降低多 worker 重复执行风险；alerts/outbox/queue/reviews 只读或本地复核，不代表正式外部告警或自动处置完成 |
| BE-09 | P1 | 前端壳接入 | Router 或页面加载器支持后端 schema | 已完成 1 个工科试验室试点页面 opt-in 后端 schema 消费；后续扩展更多组件和回归自动化 |
| BE-10 | P2 | 部署迁移 | Python 后端接入部署文档、API 缓存边界、部署预检、部署 smoke 与反向代理 | 已完成 no-store、部署预检、MySQL/`utf8mb4` 生产门禁、部署 smoke 和静态站 + API 共存文档首轮；生产环境仍需真实 MySQL、反向代理和服务注册验证 |
| BE-11 | P2 | 代码空间 / AI 助教 / OJ 二期预留 | 为代码提交、判题记录和 AI 助教预留数据扩展 | 不阻塞首阶段作业闭环，后续可接入代码空间 |
| BE-12 | P2 | 风险与缺陷管理 | 将当前文档化风险迁入可维护的 `BugRecord` 或 issue 系统，并持续完善审计导出、链完整性校验、留存预检、归档、高频候选摘要、正式告警/处置和防篡改策略 | 风险可按分类、优先级、状态、证据位置、规划归属和外部 issue 引用维护；关键组织/教学/认证事件可按 action/resource/school/class/request_id/result/reason 查询并以 JSON/CSV 导出、聚合为报表摘要、生成链完整性报告、生成留存预检或生成高频候选摘要，默认不暴露快照；审计明细保留应用层链式哈希，导出/摘要/链完整性校验/留存预检行为自身会进入审计 |

---

## 九、当前 bug / 风险统计

本节是基于 2026-07-03 的工作区只读扫描、主智能体复核和已有文档记录整理出的初始基线。它不是最终 bug 库，后续应迁入正式 `BugRecord` 或 issue 系统。

### 9.1 汇总

| 分类 | 数量 | 当前判断 | 后续处理 |
| ---- | ---- | -------- | -------- |
| BE 后端能力缺口 | 3 | C++ 后端只有基础静态服务与占位 API；Python 业务后端已有 P0/P1 首轮闭环，权限矩阵已补同校非本班教师的教学/学情横向隔离和课程结构作者权限，内容脚本 sandbox HTML、本地脚本资产端点、后端 bootstrap JS 端点、响应级 CSP nonce、发布版本绑定外部脚本镜像、后端 sandbox embed 描述符、脚本资产供应链清单、镜像一致性审计、手动远端漂移扫描、扫描 run 台账/远端漂移告警候选摘要、默认关闭的调度器/必须确认外网的 CLI、扫描 run 健康/队列摘要、内容脚本远端漂移告警 outbox 入队与 CDN host policy blocked gate 已落地，知识快照与内容脚本已有告警候选摘要、outbox 入队/列表、人工复核状态流转、队列摘要和批量复核，但 MySQL 联调、前端真实 iframe 编排、内容脚本实时监控/生产告警/自动处置/自动信任封禁、正式管理端 UI、审计真实归档/外部锚定/正式高频告警与处置策略、课程协作者/班级专属作业等更多角色矩阵、知识快照正式外部告警投递/外部任务队列/队列执行治理和更细统计物化仍未完成 | BE-01、BE-02、BE-05、BE-08 与 BE-12 |
| Review 回流专项 | 2 | review 分支报告已确认若干后续任务与候选修复；active 草稿数据库级抗并发、closed 作业学生侧只读复盘、direct join 文档口径、跨班级提交唯一性、后台内容页数据库侧分页、必填文本修剪后校验、用户名大小写规范化、发布冲突 409、学生资源可见性、中文路径迁移测试和中文 slug 回归已在 `houduan` 重新落地；剩余集中在前端离线/生命周期修复与浏览器测试入口纳入 | FE / QA / BE-10 / BE-12 |
| SEC 安全与隐私 | 3 | 历史 C++ 服务 CORS 全放行、前端 cookie 元数据保护弱；Python 业务后端已有密码策略、失败锁定、管理员密码重置、用户自助密码重置令牌、密码重置 token 留存清理脚本、活动会话撤销、会话设备摘要、last_seen 追踪/节流、禁用用户会话撤销、认证审计和基础边界，但学生数据权限、审计留存策略、强设备绑定和长期会话治理仍需更多矩阵回归；真实重置投递/MFA 暂列 `P4 / 最低优先级 / 暂缓` | BE-03、BE-04、BE-08 |
| FE 缓存、路由与状态迁移稳定性 | 6 | Service Worker、HTTP fallback、脚本 ready 检测、hash 自恢复、本地状态迁移和 API 缓存边界存在风险 | BE-08 / BE-09 前配套修复 |
| QA / DOC 未闭环项 | 2 | 未来星系拆分、移动端与旧学习体验审查仍有后续项 | 后续体验专项或内容审校专项继续跟踪 |
| OPS 开发服务与部署 | 1 | 本地 dev static server 只承担静态访问；BE-10 已补共存部署文档、no-store、MySQL/`utf8mb4` 预检门禁和部署 smoke，但真实 MySQL/反代/服务注册仍未实机验证 | BE-10 |

### 9.2 具体清单

| 编号 | 分类 | 优先级 | 问题 | 证据位置 | 规划归属 |
| ---- | ---- | ------ | ---- | -------- | -------- |
| R-01 | SEC | P0 | 当前 C++ 服务默认 CORS `*`，后续接入登录后不能沿用 | `server/main.cpp` CORS 默认头 | BE-03 / BE-04 |
| R-02 | BE | P0 | `/api/health`、`/api/info`、`/api/eval` 缺少鉴权与用户上下文 | `server/main.cpp` API 路由 | BE-03 |
| R-03 | BE | P0 | `/api/eval` 是占位实现，不能作为真实作业或代码运行后端 | `server/main.cpp` eval 注释与固定返回 | BE-01 / BE-08 |
| R-04 | BE | P0 | 用户、登录失败状态、学校、班级、班级加入申请、会话、课程、作业、学习事件、作业提交、积分流水、个人/班级知识快照、知识快照运行记录、进程内调度器、调度数据库租约、自动心跳、运行健康摘要、调度积压摘要、告警候选摘要、告警 outbox 人工复核台账/状态流转/队列摘要/批量复核、协作式取消、手动 requeue、BugRecord 和 AuditLog 已有首轮持久化；BugRecord 已可保存外部 issue 引用元数据，AuditLog 已具备应用层链式哈希；知识状态、班级聚合、管理端学校/班级深度统计、待批改队列、禁用用户会话撤销、关键组织/教学写操作审计、认证事件审计、审计日志 JSON/CSV 明细导出、报表摘要、链完整性校验、留存预检、高频候选摘要和导出/摘要行为审计已有在线响应/查询路径，但正式外部 issue 双向同步、知识快照正式外部告警投递/外部任务队列/队列执行治理、真实审计归档执行、外部锚定/WORM 和正式高频告警/处置策略仍缺少正式闭环 | `backend/app/models/` 当前模型范围 | BE-05 至 BE-08 / BE-12 |
| R-05 | SEC | P0 | 学生访问历史和知识状态已建立后端读取边界，但前端 localStorage/cache meta 仍不能扩展为正式敏感数据存储 | `shared/js/router.js` cache meta 现状 | BE-04 / BE-08 |
| R-06 | FE | P1 | Service Worker 安装失败只跳过等待，缺少可观测降级策略 | `sw.js` install catch | BE-09 |
| R-07 | FE | P1 | 未版本化静态资源 stale-while-revalidate，可能出现旧资源继续命中 | `sw.js` static asset strategy | BE-09 |
| R-08 | FE | P1 | HTTP fallback 预热使用 `force-cache`，在版本遗漏时会放大旧资源风险 | `shared/js/main.js` warmup fetch | BE-09 |
| R-09 | FE | P1 | 脚本已存在但缺 marker 时直接 resolve，可能跳过初始化检查 | `shared/js/router.js` script loader | BE-09 |
| R-10 | SEC | P1 | cookie 解析和写入只适合前端回访元数据，不适合登录态或学生数据 | `shared/js/router.js` cookie helpers | BE-03 |
| R-11 | FE | P2 | hash 自恢复固定 250ms 轮询，复杂路由下有抖动和主线程压力风险 | `shared/js/router.js` hash reconcile | BE-09 |
| R-12 | QA/DOC | P2 | 未来星系仍有总览拆分、专题路由、视觉系统深化等后续项 | `doc/03-发布历史.md` v6.3 条目中的“仍需后续”记录 | 后续体验专项 |
| R-13 | QA | P2 | 旧体验审查中仍有移动端提示、题库、学习反馈等可改进项 | `doc/06-实验体验与信度审查报告-20260606.md` | 后续体验专项 |
| R-14 | OPS | P2 | `server/dev-static-server.mjs` 只适合作本地静态服务，不承担正式业务后端 | `server/dev-static-server.mjs` | BE-10 |
| R-15 | FE/API | P1 | 学习进度、收藏、评分和测验成绩从 localStorage 迁移到后端时，需要处理未登录、离线、冲突合并和幂等写入 | `shared/js/learning-progress.js`、`shared/js/experiment-favorites.js`、`shared/js/experiment-rating.js`、`shared/js/experiment-quiz.js` | BE-08 / BE-09 |
| R-16 | FE/API | P1 | Python API 已统一返回 `Cache-Control: no-store`，但 Service Worker 与 HTTP fallback 仍主要面向静态资源；后续前端 API 错误 UX、离线策略和真实反向代理缓存策略仍需回归 | `backend/app/main.py`、`sw.js`、`shared/js/main.js` | BE-09 / BE-10 |
| R-17 | BE/DATA | P1 | 同一作者同一目标页 active 草稿唯一性已通过 active key 与数据库唯一约束补齐；剩余风险是迁移前若存在重复 active 草稿会阻断升级，且真实 MySQL 落库仍需部署环境验证 | `backend/alembic/versions/20260706_0019_content_draft_active_key.py`、`backend/tests/test_content_drafts.py` | BE-01 / BE-10 / BE-12 |
| R-18 | BE/PRODUCT | P1 | closed / archived 作业学生侧只读复盘入口已补齐；剩余问题是正式学生端作业中心 UI、历史列表入口和关闭作业后统计分母口径尚未最终产品化 | `backend/app/api/endpoints/submissions.py`、`backend/tests/test_course_learning_loop.py`、`backend/tests/test_access_control.py` | BE-06 / BE-07 / BE-08 |
| R-19 | BE/DOC | P2 | `/api/classes/{class_id}/join` 已明确为 legacy/direct join 兼容入口，且普通教师 teacher role 自助直入已收口到 join request 审批；剩余风险是正式 UI 仍需按学生 direct join、教师申请和 admin/导入路径选择入口并避免误导文案 | `backend/app/api/endpoints/classes.py`、`backend/README.md`、`doc/01-开发者手册.md` | BE-04 / FE |
| R-20 | QA/FE/BE | P1 | review 分支已有多项后端/前端修复候选；跨班级提交唯一性、后台内容页数据库侧分页、必填文本修剪后校验、用户名大小写规范化、发布冲突 409、学生资源可见性、中文路径迁移测试和中文 slug 回归已在 `houduan` 分批吸收；剩余后续集中在前端离线/生命周期修复与浏览器测试入口 | `review` 分支审查报告 | FE / QA |
| R-21 | BE/DATA | P1 | 用户名大小写规范化已由 `users.normalized_username` 与 `login_attempts.normalized_username` 数据库唯一约束兜底，注册/admin bootstrap 并发唯一冲突返回 `409`，登录失败大小写变体共享 normalized 锁定桶；剩余风险是历史库若存在大小写重复用户会阻断 `20260706_0022` 迁移，需要上线前人工清洗，并用 `deploy_preflight --require-mysql` 和真实 MySQL 回归验证字符集/排序规则行为 | `backend/app/models/user.py`、`backend/alembic/versions/20260706_0022_user_normalized_username.py`、`backend/tests/test_auth.py`、`backend/tests/test_deploy_preflight.py` | BE-03 / BE-10 / BE-12 |
| R-22 | BE/DATA | P1 | 内容发布/回滚冲突已通过 `(slug, version)` 与 `source_draft_id` 唯一约束和 `IntegrityError -> 409` 转换收口；剩余风险是真实 MySQL 并发事务、历史重复 `source_draft_id` 清洗和更细 constraint name 分类仍需部署前验证 | `backend/app/api/endpoints/content.py`、`backend/alembic/versions/20260706_0021_content_publish_conflicts.py` | BE-01 / BE-10 / BE-12 |
| R-23 | BE/SEC | P1 | 学生资源可见性已通过 access control helper、学生端列表/事件/提交/复盘过滤、个人 progress/knowledge/snapshot 可见性过滤和周期 user snapshot 跳过 hidden course 收口；剩余风险是历史库旧 hidden snapshot/event 的迁移清理、前端入口提示和真实 MySQL 联调仍需跟进 | `backend/app/services/access_control.py`、`backend/app/api/endpoints/courses.py`、`learning_events.py`、`submissions.py`、`progress.py`、`knowledge.py`、`backend/app/services/knowledge_snapshot_runs.py` | BE-04 / BE-06 / BE-07 |
| R-24 | BE/OPS | P1 | 中文路径迁移测试已覆盖 SQLite 中文目录/中文数据库文件名下的 Alembic 迁移、正式初始化 CLI、JSON 输出与 current version 写入，并覆盖中文 slug 经百分号编码后的发布和读取链路；生产字符集/排序规则已有 `deploy_preflight --require-mysql` 门禁，剩余风险是真实 MySQL DSN 百分号编码、网络路径、服务账号目录权限和实机部署联调验证 | `backend/tests/test_content_initialization.py`、`backend/tests/test_content_publication.py`、`backend/tests/test_deploy_preflight.py` | BE-10 / BE-12 |

### 9.3 首轮实现状态

- BE-01 已完成工程骨架、配置、数据库探针、Alembic 迁移至 `20260708_0035`、健康检查、部署预检、MySQL/`utf8mb4` 生产门禁、部署 smoke 和 pytest 入口；MySQL 实例联调仍待推进。
- BE-02 已完成 `physics/energy-conservation` 内容协议样例持久化、开发/测试初始化 seed、正式内容初始化入口、published 只读 schema 返回、ContentDraft 草稿创建、草稿编辑、脚本静态分析风险等级、脚本 sandbox 契约、脚本资产 allowlist/SRI 静态门禁、审核/发布阶段后端下载 SRI 校验、脚本资产校验证据 metadata、发布版本绑定的外部脚本资产镜像、管理端脚本资产供应链清单、内容脚本镜像一致性审计、管理端手动远端漂移扫描、内容脚本扫描 run 台账与远端漂移告警候选摘要、内容脚本远端漂移扫描调度/CLI observe-only 首轮、内容脚本扫描 run 健康/队列摘要、内容脚本远端漂移告警 outbox 人工复核入队、内容脚本 CDN host 信任治理首轮、公开 render 脚本 manifest 脱敏与沙箱执行契约头、后端 sandbox embed 描述符、稳定 `sectionId/sourceId` 内容身份、sandbox HTML 首轮、后端受控本地脚本资产端点、后端 bootstrap JS 端点、sandbox HTML 响应级 CSP nonce、脚本历史版本 rollback 重审门禁、脚本审核、submit/request-changes/withdraw 工作流、active key 数据库唯一约束、管理员发布、发布元数据、不可变版本记录、内容页 current 指针、草稿 base version/hash、版本 previous 链、管理端版本 JSON path diff、带显式稳定 ID 字段和敏感预览脱敏的 semantic 富语义摘要和追加式回滚；首轮前端消费路径由 BE-09 接入，前端实际 iframe 编排、实时监控、生产镜像告警/自动处置、自动信任/封禁策略和浏览器自动化隔离证明仍待推进。
- BE-03 已完成本地注册、登录、退出、当前用户接口、活动会话列表、单会话撤销、会话设备摘要、last_seen 追踪/节流、管理员密码重置、用户自助密码重置令牌、密码重置 token 留存清理脚本、角色字段、密码强度策略、登录失败持久化锁定、生产 cookie secure 标记、过期会话撤销、禁用用户会话撤销和登录/退出/单会话撤销/密码重置审计；公开注册仅允许 teacher / student；真实邮件/短信/MFA 投递暂列 `P4 / 最低优先级 / 暂缓`，强设备绑定、长期会话策略、清理脚本生产调度/演练和更细 RBAC 仍待推进。
- BE-04 已完成学校、班级、成员关系、班级成员列表、学生 membership 状态维护/批量维护、admin-only 教师 membership 状态治理/批量治理/转让、student legacy/direct join、teacher join request 审批流、普通教师 teacher direct join 审批收口、direct join 收口 pending 申请、班级教师/管理员查看成员/维护学生状态与审批、管理端跨班级队列审批、拒绝后重新申请、普通业务端点访问控制 helper 服务层收口、禁用用户旧会话撤销、跨范围权限矩阵首批测试、同校非本班教师教学/学情隔离、课程结构作者权限、课程协作者 editor 首轮、课程 owner 转让首轮、学校/班级深度统计 scoped 读取和待批改队列 teacher scope；异常关系处理、移除/转班、批量导入、多角色/批量协作者、班级专属作业、多角色/内容发布和管理端治理队列等更细权限矩阵仍待推进。
- BE-05 已完成首个 admin 初始化、用户列表/角色状态维护、管理员密码重置、禁用用户会话撤销与 `revoked_sessions` 审计快照、学校/班级/内容页/内容草稿基础查看、全站统计摘要、待审加入申请计数、待脚本审核计数、学校/班级深度统计 scoped 读取、管理端列表分页搜索、内容页数据库侧分页搜索、加入申请队列、草稿队列、待批改队列 teacher scope、`BugRecord` 缺陷清单、外部 issue 链接元数据和审计日志查询后端首轮；正式管理端 UI、审计日志前端视图、加入申请/内容草稿/知识快照/审计等治理队列权限矩阵和前端统计视图仍待推进。
- BE-06 已完成教师创建课程、挂接班级、课程协作者 editor 首轮、课程 owner 转让首轮、创建课程单元和作业、查看班级成员列表、维护/批量维护学生 membership `active/inactive`、查看待批改队列/提交、批改反馈并生成积分流水、assignment 级积分规则配置的后端首轮；`POST /api/classes/{id}/teachers/transfer` 支持全局 admin 将源 active class teacher 转给同校 active teacher/admin，目标 class membership 不存在则创建、inactive 则恢复；`PATCH /api/classes/{id}/members/batch-status` 支持成员状态 all-or-nothing 批量治理，普通班级教师仅可处理 student membership，全局 admin 可处理 teacher membership 且最终不能让 active teacher 归零；`PATCH /api/courses/{id}/owner` 支持课程创建者或全局 admin 将 owner 转给同校 active teacher/admin membership 用户，并在目标原本是 active editor 时将其 collaborator 状态置为 `inactive`；`CourseCollaborator`、`GET/POST /api/courses/{id}/collaborators` 与 `PATCH /api/courses/{id}/collaborators/{collaborator_id}` 支持课程创建者或全局 admin 把同校 active teacher/admin membership 用户设为 `editor` 或置为 `inactive`，active editor 可创建课程单元/作业和维护作业积分规则但不能管理协作者或转让 owner；`assignments.point_rule_json = NULL` 表示默认 `enabled=true`、`points_per_score=1`、`max_points=null`，`GET/PATCH /api/points/assignments/{id}/rule` 支持读取/维护规则，维护要求课程创建者、active editor 或全局 admin 并写入 `assignment.point_rule.update` 审计；批改使用“规则目标积分 - 当前 submission 已入账 assignment_grade 积分”写入差额流水，避免重复批改累计膨胀并支持封顶/禁用后的反向校正；课程单元/作业创建、作业积分规则维护要求课程创建者、active editor 或全局 admin，班级成员列表、学生状态维护、课程挂接班级、指定班级提交列表、待批改队列、批改、学习事件、积分流水和学生进度要求本班 teacher scope，同校非本班/非作者/非协作者教师不再具备该班操作权、课程结构写权或共享作业规则维护权；同一作业可挂接多个班级并按班级区分提交；closed 作业后教师/admin 提交列表和批改视角仍保持可用；移除/转班、批量导入、监护人/联系方式档案、班级专属积分覆盖规则、多角色/批量协作者和教师端 UI 仍待推进。
- BE-07 已完成学生基于班级范围查看 published 课程、published 单元和 active 作业、按班级提交作业、进入 published 课程/单元内作业只读复盘、记录学习事件、查看个人积分流水和按学生可见资源过滤的个人进度摘要的后端入口；closed / archived 作业不可再次提交但可查看本人题目、提交、成绩和反馈；个人首页、正式作业中心 UI 和学习反馈入口仍待推进。
- BE-08 已完成 append-only 学习事件记录、教师按班级查询、提交/批改计数、积分合计、个人进度摘要、`/api/knowledge/me` 个人知识状态、`/api/knowledge/me/snapshots` 个人快照、`/api/classes/{id}/knowledge` 教师班级聚合、`/api/classes/{id}/knowledge/snapshots` 班级知识快照、`scripts.rebuild_knowledge_snapshots` 日/周窗口重算、`knowledge_snapshot_runs` 运行记录、`attempt_count` 重试上限、FastAPI lifespan 进程内调度器、基于数据库租约的多 worker 防重入、自动心跳首轮、管理端运行列表、健康摘要、调度积压摘要、告警候选摘要、告警 outbox 人工复核台账/状态流转/队列摘要/批量复核、协作式取消和手动 requeue；教师学习事件、积分流水和学生进度查询已收束到本班 teacher scope；学生个人 progress/knowledge/snapshot 仅计入 published 课程、published 单元和 active 作业，周期 user snapshot 跳过 hidden course，class snapshot 保持管理口径；正式外部告警投递、外部任务队列、队列执行治理、单元/概念细分、真实 MySQL 取消/重排演练和正式 UI 仍待推进。
- BE-09 已完成 `backendSchema=1` opt-in adapter、`apiBase=` / `astra-api-base` 本地 API 指向、后端学习任务卡渲染和静态页面默认回退；更多组件类型、API 失败 UX、移动端回归和自动化测试仍待推进。
- BE-10 已完成 Python API `no-store` 缓存边界、数据库/Alembic 部署预检脚本、MySQL 方言与 `utf8mb4` 字符集/排序规则预检门禁、部署 smoke 脚本、`04-部署指南` 中静态站 + Python API 共存拓扑、反向代理分流、上线预检和回滚路径首轮；真实 MySQL、真实反向代理和 Windows 服务注册实机验证仍待推进。
- BE-12 已新增 `BugRecord` 持久化、admin 维护入口和外部 issue provider/id/url 引用字段，`admin.bug.create/update` 审计快照会记录链接变化；`AuditLog` 已覆盖 admin、学校、班级、课程、单元、作业、提交、批改、内容草稿创建/编辑、脚本审核、登录成功/失败/锁定、退出、告警 outbox 入队/列表/复核/队列摘要/批量复核审计；审计记录已带 request_id、IP 哈希、user-agent、请求方法/路径、结果、失败原因和 `prev_hash/current_hash` 应用层链式哈希；管理端审计日志 JSON/CSV 明细导出已复用列表筛选和排序，默认剥离 `snapshot_json`，显式 `include_snapshot=true` 才返回快照，并通过 `limit/truncated` 标记导出截断；CSV 明细响应附带导出元数据头并中和表格公式前缀；审计报表摘要已按 action/resource_type/actor_role/event_result/failure_reason 聚合并支持 JSON/CSV；审计链完整性校验已复用 `app.services.audit_chain` 重算 current hash、检查相邻 prev hash、报告历史空 hash 与截断状态；审计高频候选摘要已按 action、actor/action、ip/action、resource/action 和 failure_reason 聚合，并支持默认 24 小时时间窗、阈值和 `bucket_limit` 控制；审计留存预检已支持按配置或显式 cutoff 计算归档候选、临期数量、聚合桶和哈希链边界；`scripts.archive_audit_logs` 已支持本地 JSONL/CSV 归档文件、Manifest、SHA-256/记录数复验和 hash-chain 状态摘要；成功导出或生成摘要后会写入 `admin.audit.export`、`admin.audit.report`、`admin.audit.chain_integrity`、`admin.audit.retention_plan` 或 `admin.audit.high_frequency`，只记录格式/筛选/阈值/数量摘要，不记录导出条目、报表明细、链 issue 明细、留存候选明细、高频候选明细或 outbox payload/复核备注正文；文档化风险系统迁移、正式外部 issue 自动创建/双向同步、对象存储/WORM、第三方时间戳、外部锚定、删除/清理策略和正式告警/处置策略仍待推进。

---

## 十、当前不做事项

1. 不立刻删除 C++ 服务，避免破坏现有静态部署路径。
2. 不一次性重写全部前端页面，先从工科试验室 schema 试点页面开始。
3. 不把学生敏感学习数据继续扩展到 localStorage 或普通 cookie。
4. 不让教师自定义脚本绕过审核或安全讨论直接公开执行。
5. 不在 07 文档里记录具体已完成版本，完成项仍进入 `03-发布历史.md`。

---

## 十一、已确认决策

| 编号 | 决策 | 影响 |
| ---- | ---- | ---- |
| D-01 | 第一批后端生成页面选择工科试验室实验页 | 复用既有模块化特色和页面模板，降低试点风险 |
| D-02 | 登录先采用简单本地账号注册 + 登录，并对接 MySQL | 暂不引入强身份验证；先完成基础用户体系 |
| D-03 | 需要学校/组织层级；学生 direct join、teacher join request 审批、admin/受控导入 direct join 并存 | 管理端、教师端、学生端都要围绕学校和班级建立资源边界；后续 UI 不应把审批流表现成唯一入口，也不应让普通教师绕过审批自助成为班级 teacher |
| D-04 | 教师自定义页面原则上允许脚本，但具体脚本需视情况讨论和审核 | 先搭建扩展框架，为后续更多用户群体辅助开发预留能力 |
| D-05 | 作业首阶段暂不完整接入代码空间运行记录 | 代码空间偏代码测试；后续二期规划 AI 助教和 OJ 作业模块 |
| D-06 | 学生画像先采用规则统计 | 当前已按作业频次、数据量、完成率、正确率和积分做在线聚合，后续再考虑统计物化或模型分析 |

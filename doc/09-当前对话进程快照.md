# 当前对话进程快照

> **生成时间**: 2026-07-07  
> **当前分支**: `houduan`  
> **快照用途**: 记录本轮长期后端重构对话的当前状态、未提交工作、已验证证据和下一步接续顺序，便于 review 后继续推进。  
> **边界说明**: 本文是过程快照，不替代 `02-更新规划.md`、`03-发布历史.md` 或 `07-后端优化与设计.md`；正式完成项仍需同步到对应主文档后再提交。

## 1. 长期目标

当前长期目标是在 `houduan` 分支继续推进项目后端完善与整体设计重构。开发必须以 `02-更新规划.md` 和 `07-后端优化与设计.md` 为主线，必要时辅助阅读 `01-开发者手册.md`、`04-部署指南.md`、`03-发布历史.md` 等文档。

约束与约定：

- 所有后端重构与文档更新均在 `houduan` 分支推进。
- 不覆盖、不合并 `review` 分支；review 分支只作为审查报告和候选修复来源，进入 `houduan` 前必须重新评估、选择性吸收、复测并按版本提交。
- 子智能体可用于审查和并行梳理，但主智能体必须回头审查其结论后再采用。
- 文档更新需要同步规划、历史、后端设计和开发者入口，不能只改代码。
- Git 提交需要沿用 `.codex` 全局文档中的 `V+版本号` 机制。
- 当前目标是长期目标，不能因为完成某个小切片就标记整体完成。

## 2. 当前总体进度判断

按后端 API 与核心业务闭环估算，当前 `houduan` 后端首阶段约完成 75%。按可上线生产级完整三端系统估算，约完成 55% 至 60%。

当前已经具备阶段性 review 价值，但若目标是“生产试运行级验收”，仍需继续补真实 MySQL、部署联调、脚本真实执行隔离、正式三端 UI 和调度/审计生产化。

建议的验收节奏：

- 当前 BE-03 `last_seen` 刷新节流收束、文档同步、完整测试和 `V6.5.41` 提交完成后，可以进入一轮“后端阶段性初验”。
- 初验重点适合放在 API 设计、权限边界、业务闭环、测试覆盖和文档一致性。
- 若要接近部署试运行，还需要约 3 至 5 个工作日集中补 BE-10、脚本隔离和关键前端接入。

## 3. 已落地后端能力概览

账号与会话：

- 本地注册、登录、退出、当前用户接口已完成。
- 密码强度策略、登录失败锁定、用户名大小写规范化、`normalized_username` 数据库唯一约束已完成。
- 公开注册仅允许 teacher / student，首个 admin 通过 bootstrap。
- 活动会话列表、单会话撤销、禁用用户会话撤销、会话设备摘要、登录 user-agent、`last_seen` 追踪已完成。
- 当前正在收束 `last_seen` 刷新节流，目标是降低高频鉴权写库压力。

三端后端基础：

- 管理员、教师、学生角色边界已在后端 API 层建立。
- 学校、班级、班级成员、legacy/direct join、join request 审批流、管理端加入申请队列已完成首轮。
- 教师课程、课程单元、作业、提交、批改、积分流水、学生只读复盘已完成首轮。
- 学生侧课程、单元、作业、学习事件、个人进度、个人知识状态和快照已有后端入口。
- 正式三端 UI 仍未完成，是后续大块。

内容模块化生成与治理：

- 后端内容 schema、`physics/energy-conservation` 试点、公开 render、内容页列表已完成首轮。
- 正式内容初始化、ContentDraft 草稿、编辑、提交、撤回、退回、脚本审核、脚本静态分析、sandbox 契约已完成。
- 内容发布、不可变版本、current 指针、base version/hash、previous 链、追加式回滚、版本 diff 与 semantic 摘要已完成。
- 真实 iframe/worker 执行隔离、CSP 强制、资产 allowlist/SRI 和更细 diff 脱敏仍待推进。

管理与审计：

- 管理端用户、学校、班级、内容页、内容草稿、版本、统计、待批改、缺陷清单接口已有首轮。
- `AuditLog` 已覆盖 admin、学校、班级、课程、单元、作业、提交、批改、内容草稿、脚本审核、认证事件等。
- 审计日志 JSON/CSV 明细导出、报表摘要、默认剥离快照、显式快照导出和导出行为审计已完成。
- 归档、防篡改、高频事件策略和外部 issue 流程仍待推进。

测试与部署基础：

- 后端 pytest 测试体系已经覆盖认证、内容、课程学习、学校班级、权限、知识快照、部署预检和 smoke。
- API no-store、部署预检、部署 smoke、静态站 + Python API 共存文档已完成首轮。
- 真实 MySQL、真实反向代理、Windows 服务注册和生产字符集/排序规则仍需实机验证。

## 4. 当前未提交工作区状态

快照生成前的工作区状态：

```text
## houduan...origin/houduan
 M README.md
 M backend/README.md
 M backend/app/api/deps/auth.py
 M backend/app/core/config.py
 M backend/tests/test_auth.py
```

当前本地差异统计：

```text
README.md                    |   7 +-
backend/README.md            |   5 +-
backend/app/api/deps/auth.py |  27 +++++-
backend/app/core/config.py   |   1 +
backend/tests/test_auth.py   | 196 +++++++++++++++++++++++++++++++++++++++++++
5 files changed, 228 insertions(+), 8 deletions(-)
```

本文件创建后，工作区还会新增：

```text
doc/09-当前对话进程快照.md
```

这些改动尚未提交。不要把这批工作当成已发布版本，除非后续完成文档同步、测试和 `V6.5.41` 提交。

## 5. 正在收束的 BE-03 切片

当前正在推进的代码切片是 BE-03 会话 `last_seen` 刷新节流。

已做代码改动：

- `backend/app/core/config.py` 新增 `session_last_seen_update_seconds`，默认 `300`，通过 `ASTRA_SESSION_LAST_SEEN_UPDATE_SECONDS` 配置。
- `backend/app/api/deps/auth.py` 中 `_touch_auth_session()` 改为按规则判断是否刷新 `AuthSession.last_seen_at` 和 `last_seen_ip_hash`。
- 同一 IP 哈希且仍在节流窗口内时不重复提交数据库写入。
- `last_seen_at` 缺失、IP 哈希变化、窗口过期或配置为 `0` 时仍会刷新。
- `db.refresh(user)` 行为应保持稳定，不依赖本次是否真的写入 `last_seen`。

已做测试改动：

- `test_session_last_seen_refresh_is_throttled_for_same_ip`
- `test_session_last_seen_refreshes_after_throttle_window`
- `test_session_last_seen_refreshes_when_missing`
- `test_session_last_seen_refreshes_when_ip_hash_changes_inside_window`
- `test_session_last_seen_throttle_can_be_disabled`

已有测试证据：

```text
python -m pytest backend\tests\test_auth.py -q
```

先前执行结果为认证模块 26 个测试通过。提交前仍需重新执行一次目标测试和完整后端测试。

## 6. 子智能体协作记录

本轮使用过子智能体 `Laplace` 做只读审查。其核心建议已回收：

- 使用 `session_last_seen_update_seconds` 配置，默认 300 秒。
- `0` 表示关闭节流。
- `last_seen_at` 缺失、窗口过期、IP 哈希变化时必须刷新。
- 同一 IP 哈希窗口内跳过写库，降低高频鉴权写放大。
- 不应把该机制描述成强设备绑定、设备指纹或完整风控。
- 测试需要覆盖缺失值、窗口过期、IP 变化、配置关闭节流等路径。
- 文档需要同步 README、backend README、01、02、03、04、07。

主智能体已审查并采用这些建议，其中“保持 `db.refresh(user)` 稳定”已落实到当前设计中。

## 7. 文档同步现状

已经部分同步：

- `README.md` 已开始写入 BE-03 `last_seen` 刷新节流状态和更新日志。
- `backend/README.md` 已开始写入配置项和认证服务边界说明。

仍需同步：

- `doc/01-开发者手册.md`：补配置项、会话治理说明和当前状态。
- `doc/02-更新规划.md`：把 BE-03 会话治理快照更新为“追踪/节流”，明确剩余为账号重置、强设备绑定、长期会话策略和真实 MySQL 验证。
- `doc/03-发布历史.md`：新增 `V6.5.41` 或对应小节，记录实现、测试、子智能体审查和剩余风险。
- `doc/04-部署指南.md`：补充 `ASTRA_SESSION_LAST_SEEN_UPDATE_SECONDS` 对生产写压力的影响和真实 MySQL 观察点。
- `doc/07-后端优化与设计.md`：同步会话治理快照、BE-03 路线、风险统计和剩余缺口。

## 8. 接续顺序

建议下一轮按以下顺序继续：

1. 复查当前 BE-03 `last_seen` 节流实现，确认鉴权语义、会话撤销、过期会话和禁用用户路径不受影响。
2. 补齐 `01`、`02`、`03`、`04`、`07` 文档同步，保持“规划、历史、设计、部署、开发入口”口径一致。
3. 执行验证：

```text
python -m pytest backend\tests\test_auth.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
```

4. 若验证通过，按版本机制提交：

```text
V6.5.41 会话活跃刷新节流
```

5. 提交后推送 `origin/houduan`，再汇报 commit hash、测试结果和剩余大块。

## 9. 当前不可误判事项

- 当前 `houduan` 相对主线累计接近两万行改动，这是后端化累计成果，不代表当前本地还有两万行未收束。
- 当前本地残留主要是 BE-03 小切片，不是大模块拆了一半。
- `review` 分支不可直接覆盖或合并到 `houduan`。
- `last_seen` 节流只解决写放大，不解决强设备绑定、长期会话风控或账号找回。
- 后端 API 核心闭环已具备阶段性 review 价值，但生产级验收仍需真实 MySQL、部署、脚本隔离和正式三端 UI。

## 10. 2026-07-07 接棒更新：管理员密码重置

上一轮快照中的 `last_seen` 刷新节流已完成验证、文档同步、`V6.5.41 会话活跃刷新节流` 提交并推送到 `origin/houduan`。

随后继续推进 BE-03/BE-05 账号治理小切片，当前已实现管理员协助密码重置：

- 新增 `POST /api/admin/users/{id}/password-reset`，仅 admin 可调用。
- 请求体由管理员提供新密码，复用注册/bootstrap 的密码强度策略。
- 重置成功后默认撤销目标用户未撤销会话，旧 token 会被拒绝。
- 同时清理目标用户 `LoginAttempt` 登录失败桶，避免管理员重置后仍被旧失败记录锁定。
- 写入 `admin.user.password_reset` 审计，记录目标用户摘要、撤销会话数和登录失败桶清理状态；不记录明文密码、密码 hash 或 token。
- 子智能体 `James` 做了只读复核；其“不要暴露保留旧会话开关”的建议已采纳。

本切片已同步 `README.md`、`backend/README.md`、`doc/01-开发者手册.md`、`doc/02-更新规划.md`、`doc/03-发布历史.md` 和 `doc/07-后端优化与设计.md`。

已执行验证：

```text
python -m pytest backend\tests\test_admin.py::test_admin_can_reset_user_password_and_revoke_sessions -q
python -m pytest backend\tests\test_admin.py -q
python -m pytest backend\tests\test_admin.py backend\tests\test_auth.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
```

后续仍未完成：用户自助找回/重置、邮件/短信验证、一次性重置 token、MFA、强设备绑定、长期会话策略、真实 MySQL 并发/事务验证和正式管理端 UI。

## 11. 2026-07-07 接棒更新：审计日志 CSV 导出

管理员密码重置完成后继续推进 BE-12 审计导出治理小切片，当前已实现审计日志 CSV 导出：

- 新增 `GET /api/admin/audit-logs/export.csv`，仅 admin 可调用。
- 复用 JSON 导出的筛选参数、倒序排序、`limit=1000` 默认值、`limit<=5000` 上限、`include_snapshot=false` 默认值和 `from <= to` 校验。
- CSV 响应使用下载附件，附带 `X-Audit-Export-Total`、`X-Audit-Export-Limit`、`X-Audit-Export-Truncated`、`X-Audit-Export-Include-Snapshot` 和 `X-Audit-Exported-At` 元数据头。
- 默认 `snapshot_json` 列为空；显式 `include_snapshot=true` 时写入紧凑 JSON 字符串。
- 对以 `= + - @` 以及 tab/换行开头的文本单元格做前缀中和，降低 spreadsheet formula injection 风险。
- 成功 CSV 导出后同样写入 `admin.audit.export`，snapshot 记录 `format=csv`、筛选条件、导出数量、截断状态和快照开关，不记录导出条目明细。
- 子智能体 `Ampere` 做了只读复核；其独立 `/export.csv` 入口、复用安全边界、导出元数据头和公式注入防护建议已采纳。

本切片已同步 `README.md`、`backend/README.md`、`doc/01-开发者手册.md`、`doc/02-更新规划.md`、`doc/03-发布历史.md` 和 `doc/07-后端优化与设计.md`。

已执行验证：

```text
python -m pytest backend\tests\test_admin.py::test_admin_views_user_management_stats_and_bug_records -q
python -m pytest backend\tests\test_admin.py -q
```

提交前仍需执行更宽范围回归、compileall 和 `git diff --check`。

## 12. 2026-07-07 接棒更新：审计日志报表摘要

审计日志 CSV 明细导出完成后继续推进 BE-12 报表摘要小切片，当前已实现审计日志报表：

- 新增 `GET /api/admin/audit-logs/report`，仅 admin 可调用。
- 新增 `GET /api/admin/audit-logs/report.csv`，作为同一报表摘要的 CSV 下载镜像。
- 两个入口均复用审计日志 actor/action/resource/school/class/request_id/event_result/failure_reason/from/to 筛选与时间窗校验。
- 报表响应包含 `total`、`filters`、`generated_at`、`by_action`、`by_resource_type`、`by_actor_role`、`by_event_result` 和 `by_failure_reason`；`by_action` 额外统计 success/failure/other 与 latest_at。
- `bucket_limit` 默认 20、最大 100，用于控制各聚合维度输出桶数量。
- CSV 报表输出 `section,key,total,success,failure,other,latest_at` 聚合行，并返回 `X-Audit-Report-*` 元数据头；不导出原始审计条目或快照明细。
- 成功生成 JSON/CSV 报表后写入 `admin.audit.report`，snapshot 只记录格式、筛选条件、total、bucket_limit、各类 bucket 数量和 generated_at。
- 子智能体 `Copernicus` 做了只读复核；其 canonical JSON summary + CSV mirror、复用筛选/权限/审计边界的建议已采纳。

本切片已同步 `README.md`、`backend/README.md`、`doc/01-开发者手册.md`、`doc/02-更新规划.md`、`doc/03-发布历史.md` 和 `doc/07-后端优化与设计.md`。

已执行验证：

```text
python -m pytest backend\tests\test_admin.py::test_admin_views_user_management_stats_and_bug_records -q
```

提交前仍需执行 admin 全量、更宽范围回归、compileall 和 `git diff --check`。

## 13. 2026-07-07 接棒更新：缺陷记录外部 issue 链接

审计日志报表摘要完成后继续推进 BE-12 外部 issue 流程的小切片，当前已实现缺陷记录外部 issue 链接元数据：

- `BugRecord` 新增 `external_issue_provider`、`external_issue_id` 和 `external_issue_url` 三个 nullable 字段。
- 新增 Alembic `20260707_0024_bug_record_issue_links.py`，挂在 `20260707_0023` 之后。
- `GET/POST/PATCH /api/admin/bugs` 的 schema、创建、更新、响应和搜索均已同步三个字段。
- provider 会修剪并小写；issue id 和 URL 会修剪但保留大小写与原始语义。
- 缺陷列表关键字搜索覆盖 issue provider/id/url，并复用 `%/_` 通配符转义。
- `admin.bug.create` 与 `admin.bug.update` 审计快照记录外部 issue 链接变化。
- 子智能体 `Galileo` 做了只读复核；其“只保存引用元数据，不声明自动创建或双向同步”的文档口径建议已采纳。

本切片已同步 `README.md`、`backend/README.md`、`doc/01-开发者手册.md`、`doc/02-更新规划.md`、`doc/03-发布历史.md` 和 `doc/07-后端优化与设计.md`。

当前剩余边界：

- 这不是 GitHub/Jira/Linear 等外部系统集成，不调用外部 API，不保证状态一致。
- `external_issue_url` 会进入 bug 审计快照；导出默认剥离 snapshot，但显式 `include_snapshot=true` 时仍可能暴露 URL，禁止写入 token、私密 query 或敏感凭据。
- BE-12 仍需推进正式外部 issue 自动创建/双向同步、审计归档、留存、防篡改链式哈希和高频事件采样/聚合；链式哈希已在下方第 14 节接续补齐，仍待外部锚定/WORM 与正式留存策略。

提交前仍需执行目标 admin 测试、admin/auth 回归、完整 backend 回归、compileall 和 `git diff --check`。

## 14. 2026-07-07 接棒更新：审计日志链式哈希

缺陷记录外部 issue 链接已作为 `V6.5.45 缺陷外部Issue链接` 提交并推送后，继续推进 BE-12 审计防篡改线索的小切片。当前工作区正在收束 `V6.5.46 审计日志链式哈希`：

- `AuditLog` 新增 nullable `prev_hash` 和 `current_hash` 字段。
- 新增 Alembic `20260707_0025_audit_log_hash_chain.py`，挂在 `20260707_0024` 之后；旧审计记录不回填哈希，迁移后保持 `NULL`。
- `record_audit_log()` 会显式固定时间戳，读取上一条非空 `current_hash`，把它写入新记录的 `prev_hash`，再按规范化 payload 计算 SHA-256 `current_hash`。
- 哈希 payload 包含版本、算法、上一哈希、actor/action/resource、学校/班级、事件结果、失败原因、请求元数据、`snapshot_json` 和 `created_at`；不包含数据库 id、`current_hash` 或 `updated_at`。
- 审计服务使用 `db.no_autoflush` 避免查询链尾时提前 flush 当前审计记录，并通过 `Session.info["audit_chain_tail"]` 串联同一事务内的多条审计；根事务结束后清理该缓存。
- `GET /api/admin/audit-logs`、JSON 明细导出和 CSV 明细导出均已同步 `prev_hash/current_hash`；默认剥离 `snapshot_json` 时仍保留哈希字段。
- `backend/tests/test_admin.py` 已补链路顺序、导出字段、存储记录重算一致性、同事务多条日志串联和篡改 snapshot 后哈希变化测试。
- `backend/tests/test_deploy_preflight.py` 已补从 `20260707_0024` 升级到 head 时旧审计日志哈希字段保持空值的迁移测试。
- 子智能体 `Chandrasekhar` 做了只读复核；其 nullable 字段、无历史回填、无额外索引、规范化 JSON payload、`db.no_autoflush` 与事务内链尾缓存建议已采纳。

已同步文档：

- `README.md`
- `backend/README.md`
- `doc/01-开发者手册.md`
- `doc/02-更新规划.md`
- `doc/03-发布历史.md`
- `doc/04-部署指南.md`
- `doc/07-后端优化与设计.md`
- `doc/09-当前对话进程快照.md`

已执行过的早期目标验证：

```text
python -m pytest backend\tests\test_admin.py::test_admin_views_user_management_stats_and_bug_records -q
python -m pytest backend\tests\test_admin.py::test_audit_hash_chain_links_multiple_logs_in_one_transaction -q
python -m pytest backend\tests\test_deploy_preflight.py::test_audit_log_chain_hash_migration_preserves_legacy_logs -q
```

提交前仍需执行：

```text
python -m pytest backend\tests\test_admin.py backend\tests\test_deploy_preflight.py -q
python -m pytest backend\tests\test_admin.py backend\tests\test_auth.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
cd backend
python -m alembic heads
```

当前剩余边界：

- 链式哈希是应用层篡改迹象追踪，不是外部不可篡改账本，不能替代备份、binlog、外部归档、WORM 或第三方时间戳。
- 拥有数据库写权限的操作者仍可能批量重算链路；并发事务在没有数据库级锁或外部锚定时仍可能形成分叉或重放窗口。
- BE-12 后续仍需正式外部 issue 自动创建/双向同步、审计归档/留存、外部锚定/WORM、第三方时间戳和高频事件采样/聚合策略。

## 15. 2026-07-07 接棒更新：知识快照调度数据库租约

`V6.5.46 审计日志链式哈希` 已完成验证、提交并推送到 `origin/houduan`，提交为 `83c9aca`。随后继续推进 BE-08 多 worker/多副本调度锁的小切片，当前工作区正在收束 `V6.5.47 知识快照调度租约`：

- `KnowledgeSnapshotRun` 新增 `scheduler_lease_owner`、`scheduler_lease_token`、`scheduler_lease_expires_at` 和 `scheduler_heartbeat_at` 字段。
- 新增 Alembic `20260707_0026_knowledge_snapshot_run_leases.py`，挂在 `20260707_0025` 之后；旧运行记录不回填，租约字段保持 `NULL`。
- `KnowledgeSnapshotScheduler` 启动实例会生成 `hostname:pid:uuid` 形式的 instance id，执行 job 前先按 `run_key` 抢占数据库租约。
- `scripts.rebuild_knowledge_snapshots` CLI 也走同一租约路径，避免手动/cron 脚本绕过 scheduler 锁。
- 未过期 `running` 窗口返回 skipped；过期 `running` 可被新 worker 接管；`success` 不重跑；`failed` 按 `attempt_count` 与重试上限判断。
- 完成/失败释放使用 owner + token 条件更新，旧 token 会抛出 `SnapshotRunLeaseLost`，不能覆盖新 owner 的运行状态。
- 已新增 `heartbeat_snapshot_job_lease()` helper 和 `scheduler_heartbeat_at` 字段；自动周期心跳尚未接入，生产租约 TTL 应保守大于预期重算时长。
- 子智能体 `Archimedes` 做了只读复核；其“复用 `knowledge_snapshot_runs.run_key`、新增 owner/token/expires/heartbeat、过期抢占、token guarded release、CLI 不绕过锁”的建议已采纳，暂不引入 Celery/RQ/leader election。

已同步文档：

- `README.md`
- `backend/README.md`
- `doc/01-开发者手册.md`
- `doc/02-更新规划.md`
- `doc/03-发布历史.md`
- `doc/04-部署指南.md`
- `doc/07-后端优化与设计.md`
- `doc/09-当前对话进程快照.md`

已执行过的目标验证：

```text
python -m pytest backend\tests\test_knowledge_snapshot_scheduler.py -q
python -m pytest backend\tests\test_deploy_preflight.py::test_knowledge_snapshot_run_lease_migration_preserves_legacy_runs -q
python -m pytest backend\tests\test_knowledge_snapshot_scheduler.py backend\tests\test_deploy_preflight.py backend\tests\test_course_learning_loop.py::test_teacher_course_assignment_and_student_learning_event_loop -q
python -m pytest backend\tests\test_knowledge_snapshot_scheduler.py backend\tests\test_deploy_preflight.py backend\tests\test_course_learning_loop.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
cd backend
python -m alembic heads
```

提交前仍需执行：

```text
git status --short --branch
git add -A
git commit -m "V6.5.47 知识快照调度租约"
git push origin houduan
```

当前剩余边界：

- SQLite 测试不足以证明真实 MySQL 多连接并发锁行为，仍需 BE-10/BE-12 实机或集成环境压测。
- 这不是完整任务队列，尚无任务取消、队列堆积、外部告警或运维控制台。
- 自动周期心跳尚未接入 scheduler 长任务循环；若窗口重算可能超过租约 TTL，需要先调大 `ASTRA_KNOWLEDGE_SNAPSHOT_SCHEDULER_LEASE_SECONDS`。

## 16. 2026-07-07 接棒更新：知识快照租约自动心跳

`V6.5.47 知识快照调度租约` 已完成验证、提交并推送到 `origin/houduan`，提交为 `aa1aaae`。随后继续推进 BE-08 自动周期心跳的小切片，当前工作区正在收束 `V6.5.48 知识快照租约自动心跳`：

- `rebuild_periodic_knowledge_snapshots()` 新增可选 `scheduler_lease_heartbeat` callback、`scheduler_heartbeat_seconds` 和 `clock` 参数。
- 重算开始态与 `attempt_count` 会先提交，避免主重算事务长期锁住 `knowledge_snapshot_runs` 行导致独立 heartbeat session 在 MySQL 上被自己阻塞。
- `_rebuild_window()` 在进入循环、每个 class/course snapshot 后、每个 user snapshot 后做 heartbeat checkpoint；达到间隔时调用 callback。
- FastAPI 进程内调度器抢到 `SnapshotJobLease` 后，会注入独立短 session 的 heartbeat callback，复用 `heartbeat_snapshot_job_lease()` 的 run_key + owner + token guard。
- `scripts.rebuild_knowledge_snapshots` CLI 也注入同一类 heartbeat callback，避免手动/cron 重算缺少长任务续租。
- heartbeat 返回 false 时抛出 `SnapshotRunLeaseLost`；旧 worker 会回滚当前快照重算、不标记 failed、不覆盖新 owner 状态。
- 子智能体 `Lovelace` 做了只读复核；其“注入 callback 避免循环 import、不要在主重算 session 中 commit 心跳、CLI 也要接入、心跳失租应抛 SnapshotRunLeaseLost”的建议已采纳。

已同步文档：

- `README.md`
- `backend/README.md`
- `doc/01-开发者手册.md`
- `doc/02-更新规划.md`
- `doc/03-发布历史.md`
- `doc/04-部署指南.md`
- `doc/07-后端优化与设计.md`
- `doc/09-当前对话进程快照.md`

已执行过的目标验证：

```text
python -m pytest backend\tests\test_knowledge_snapshot_scheduler.py -q
```

提交前仍需执行：

```text
python -m pytest backend\tests\test_knowledge_snapshot_scheduler.py backend\tests\test_deploy_preflight.py backend\tests\test_course_learning_loop.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
cd backend
python -m alembic heads
git status --short --branch
git add -A
git commit -m "V6.5.48 知识快照租约自动心跳"
git push origin houduan
```

当前剩余边界：

- 这仍不是完整任务队列，尚无任务取消、队列堆积、外部告警或运维控制台。
- SQLite 测试不足以证明真实 MySQL 多连接心跳续租、锁等待和长事务行为，仍需 BE-10/BE-12 实机或集成环境压测。
- 如果单个 class/course 聚合本身耗时超过租约 TTL，循环 checkpoint 无法在该单次计算中续租；生产仍需保守配置 `ASTRA_KNOWLEDGE_SNAPSHOT_SCHEDULER_LEASE_SECONDS` 并观察实际耗时。

## 17. 2026-07-07 接棒更新：审计高频事件摘要

`V6.5.48 知识快照租约自动心跳` 已完成验证、提交并推送到 `origin/houduan`，提交为 `d9f8a3d`。随后继续推进 BE-12 审计高频候选摘要的小切片，当前工作区正在收束 `V6.5.49 审计高频事件摘要`：

- 新增 `GET /api/admin/audit-logs/high-frequency`，仅 admin 可调用。
- 接口复用审计日志 actor/action/resource/school/class/request_id/event_result/failure_reason/from/to 筛选；未传时间窗时默认检查最近 24 小时，避免无界全表聚合。
- 高频专属参数包括 `window_hours`、`min_count`、`min_failure_count`、`min_failure_ratio` 和 `bucket_limit`。
- 响应返回 `total`、`generated_at`、`filters`、`window`、`thresholds` 和统一 `candidates`；候选维度包括 `action`、`actor_action`、`ip_action`、`resource_action` 和 `failure_reason`。
- 聚合统计 success/failure/other、失败比例、distinct actor/IP/request_id、first/latest 时间和命中原因；比例、命中原因和最终排序在 Python 侧完成，降低 SQLite/MySQL 差异。
- 成功生成摘要后写入 `admin.audit.high_frequency`，snapshot 只记录筛选、时间窗、阈值、total、candidate_count、dimension_counts 和 generated_at，不记录 candidates 明细、原始日志 id、完整 IP 哈希清单、user-agent 明细或原始 `snapshot_json`。
- 子智能体 `Dirac` 做了只读复核；其默认时间窗、五类聚合维度、SQLAlchemy 兼容聚合、Python 侧排序/比例计算和 snapshot 脱敏建议已采纳。

当前已修改代码：

- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/admin.py`
- `backend/tests/test_admin.py`

已同步文档：

- `README.md`
- `backend/README.md`
- `doc/01-开发者手册.md`
- `doc/02-更新规划.md`
- `doc/03-发布历史.md`
- `doc/04-部署指南.md`
- `doc/07-后端优化与设计.md`
- `doc/09-当前对话进程快照.md`

提交前仍需执行：

```text
python -m pytest backend\tests\test_admin.py backend\tests\test_deploy_preflight.py -q
python -m pytest backend\tests\test_admin.py backend\tests\test_auth.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
cd backend
python -m alembic heads
git status --short --branch
git add -A
git commit -m "V6.5.49 审计高频事件摘要"
git push origin houduan
```

当前剩余边界：

- 该接口是只读聚合视图，用于帮助管理员发现候选风险；不自动告警、不封禁、不创建外部 issue、不生成 BugRecord、不做归档或外部锚定。
- 真实 MySQL 大表上的索引命中、临时表、锁等待和查询超时仍需 BE-10/BE-12 部署联调观察。
- BE-12 后续仍需正式外部 issue 自动创建/双向同步、审计归档/留存、外部锚定/WORM、第三方时间戳和正式告警/升级/处置策略。

## 18. 2026-07-07 接棒更新：审计留存预检

`V6.5.49 审计高频事件摘要` 已完成验证、提交并推送到 `origin/houduan`，提交为 `0abbd2e`。随后继续推进 BE-12 审计归档/留存可观测性的小切片，当前工作区正在收束 `V6.5.50 审计留存预检`：

- 新增配置 `ASTRA_AUDIT_LOG_RETENTION_DAYS`，默认 365 天，用于留存预检的默认 cutoff 计算。
- 新增 `GET /api/admin/audit-logs/retention-plan`，仅 admin 可调用，复用审计日志 actor/action/resource/school/class/request_id/event_result/failure_reason/from/to 筛选。
- 接口支持 `retention_days` 覆盖配置，也支持 `before` 指定绝对 cutoff；二者互斥；`warning_days` 默认 30，用于计算即将到期但尚未进入归档候选的数量。
- 响应返回 `generated_at`、`filters`、observe-only `capabilities`、`policy`、`summary` 和按 action/resource_type/event_result 聚合的候选桶；summary 包含总量、保留量、归档候选、临期数量、首尾候选 id 和哈希链边界。
- 成功生成预检后写入 `admin.audit.retention_plan`，snapshot 只记录策略、筛选、数量、bucket 数量、首尾候选 id 与链边界，不记录候选明细、原始 `snapshot_json`、IP 哈希清单或导出内容。
- 子智能体 `Rawls` 和 `Franklin` 做了只读复核；其“只做本地可观测预检，不做删除、WORM、外部锚定、外部告警或 issue 自动化”的建议已采纳，并显式在响应中返回 observe-only capabilities。

当前已修改代码：

- `backend/app/core/config.py`
- `backend/app/api/endpoints/admin.py`
- `backend/app/schemas/admin.py`
- `backend/tests/test_admin.py`
- `backend/.env.example`

已同步文档：

- `README.md`
- `backend/README.md`
- `doc/01-开发者手册.md`
- `doc/02-更新规划.md`
- `doc/03-发布历史.md`
- `doc/04-部署指南.md`
- `doc/07-后端优化与设计.md`
- `doc/09-当前对话进程快照.md`

已执行过的目标验证：

```text
python -m pytest backend\tests\test_admin.py::test_admin_audit_retention_plan_summarizes_candidates_without_deleting -q
python -m pytest backend\tests\test_admin.py::test_admin_views_user_management_stats_and_bug_records -q
```

提交前仍需执行：

```text
python -m pytest backend\tests\test_admin.py backend\tests\test_deploy_preflight.py -q
python -m pytest backend\tests\test_admin.py backend\tests\test_auth.py -q
python -m pytest backend -q
python -m compileall backend\app backend\scripts backend\tests backend\alembic -q
git diff --check
cd backend
python -m alembic heads
git status --short --branch
git add -A
git commit -m "V6.5.50 审计留存预检"
git push origin houduan
```

当前剩余边界：

- 该接口是 observe-only 预检，不生成归档包、不移动或删除数据库记录、不写入对象存储，也不执行强制留存策略。
- 链式哈希和链边界只能帮助定位候选范围，不等同外部不可篡改账本；WORM、第三方时间戳和外部锚定仍待后续。
- 真实 MySQL 大表上的范围过滤、临时表、锁等待、归档吞吐和清理策略仍需 BE-10/BE-12 部署联调与演练。

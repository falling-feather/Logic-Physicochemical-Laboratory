# 星序 Astra — 项目规划

> **文档梗概**：作为当前设计、任务原件、认领、版本分配、项目对接和状态的唯一权威来源；已完成事实进入 [`03-开发历史.md`](03-开发历史.md)。
>
> **具体规划法则**：
>
> 1. 所有开发、配置、文档、资源和验收工作必须先建立唯一任务卡；只有“已完成”使用 `[x]`。
> 2. 主开发集中分配 `V7.4.x` 小版本并独占 `houduan` 写入；长期小组没有写锁和版本令牌时保持只读。
> 3. 项目提交标题遵守 `.codex/AGENTS.md` 的 `Vx.y.z 中文说明`；任务编号、主责和协作关系在本文与开发历史追踪。
>
> **最后一次更新时间**：2026-07-15
>
> **更新者**：PM组

---

## 0. 全局控制规则

- 当前版本轨道为 `V7.4.x`，集成分支为 `houduan`。
- 本项目采用项目规则兼容模式：项目对接已启用，但不向 Git 标题增加责任组字段；机械对接校验只验证基础兼容入口，责任与版本由主开发人工对账。
- 已完成旧编号不改写、不复用；未完成旧标签迁移为三位流水编号：`OPS-01 → OPS-001`、`ARCH-FE-01 → FE-001`、`A11Y-01 → UX-001`、`PERF-01 → PERF-001`、`BUILD-02 → TOOLS-001`、`INTEGRATION-01 → REL-001`、`PRODUCT-02 → PM-002`。
- 根工作区同一时间只有主开发写入；既有脏 worktree 不得覆盖、搬移、重置或强制清理。

---

## 目录

1. [当前周期](#1-当前周期)
2. [项目对接登记](#2-项目对接登记)
3. [正式任务原件](#3-正式任务原件)
4. [调度与依赖](#4-调度与依赖)
5. [验收与风险](#5-验收与风险)
6. [需要用户提供或确认](#6-需要用户提供或确认)

---

## 1. 当前周期

当前实现基线为 V7.4.16：后端阶段、登录前置、三角色工作台、管理数据地图、管理 API 分域、Python/Node/C++ 可复现构建、19 页面注册和 88 实验注册结构首片均已完成。当前周期目标为：

1. 在指定目标环境完成域名、TLS、secret、MySQL、备份、日志、监控、发布和回滚闭环。
2. 以可回滚切片建立前端页面注册表与唯一生命周期入口，再完成可访问性门禁。
3. 外部通道、性能深分页和 OJ/AI 只在真实数据、owner 与审批具备后启动。

## 2. 项目对接登记

- **项目对接模式**：兼容启用（项目规则覆盖）
- **主控任务标题**：星序 Astra｜主开发｜总控
- **版本分配者**：主开发
- **当前版本轨道**：V7.4.x
- **集成分支**：`houduan`
- **提交责任组字段**：禁用；遵守 `.codex/AGENTS.md`
- **长期团队策略**：主开发与 QA 固定；FE、OPS 按当前结构维护和发布主线长期保留

| 小组 | 代号 | 长期任务标题 | 责任边界 | Git 模式 | 分支/工作树 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- |
| 主开发 | PM | 星序 Astra｜主开发｜总控 | 用户接口、任务拆分、版本、Git、实现集成和最终决策 | 集成工作区单写 | `houduan` / 根工作区 | 活跃 |
| 前端组 | FE | 星序 Astra｜前端组｜长期对接 | 页面注册、Router、生命周期、Service Worker 和前端契约审查 | 只读长期任务 | 无写入 worktree | 活跃 |
| 运维组 | OPS | 星序 Astra｜运维组｜长期对接 | 目标环境输入、部署证据、监控、备份和回滚审查 | 只读长期任务 | 无写入 worktree | 活跃 |
| QA 组 | QA | 星序 Astra｜QA组｜长期对接 | 独立自动化、浏览器、响应式与发布证据验收 | 只读长期任务 | 无写入 worktree | 活跃 |

### 2.1 工作树登记

| 所有者 | 当前任务 | 基线 | 分支/工作树 | 允许写入 | 当前状态 | 清理门禁 |
| --- | --- | --- | --- | --- | --- | --- |
| 主开发 | `FE-001` | `8f5a898`（V7.4.15） | `houduan` / 根工作区 | 已登记任务范围 | 活跃 | 集成工作区不清理 |
| 遗留前端工作 | 未登记；待人工对账 | `988390a`（V7.2.0） | `qianduan` / `.codex/worktrees/qianduan` | 禁止新增写入 | 暂停：无独有提交，有 9 个修改文件和 8 个未跟踪文件 | 未验收、未提交、未集成，禁止清理 |

## 3. 正式任务原件

### 3.1 当前执行与 P0/P1

- [x] PM-001｜状态：已完成｜目标版本：V7.4｜模块：项目协作控制面｜主责：PM｜协作：DEV、FE、OPS、QA｜任务：建立 00 总纲和 01—03 规范入口，登记长期团队、版本分配、Git 写入、遗留 worktree 和独立验收规则｜前置：V7.4.11 基线与现有 worktree 对账｜关联文档：本文件、[00-项目总纲.md](00-项目总纲.md)、[01-开发者文档.md](01-开发者文档.md)、[03-开发历史.md](03-开发历史.md)｜关联版本：V7.4.12｜验证：兼容文档校验、项目对接校验、相对链接、Git/worktree 对账和 `git diff --check` 通过；长期任务按标准标题唯一创建并置顶
- [ ] OPS-001｜状态：阻塞｜目标版本：V7.4｜模块：目标环境发布闭环｜主责：OPS｜协作：DEV、QA、REL｜任务：在指定 staging/正式目标完成域名、owner、TLS、安全组、secret store、MySQL、备份、日志、监控、发布和回滚实证｜前置：目标域名、运维 owner、备份保留期、监控渠道和环境访问权｜关联文档：[04-部署指南.md](04-部署指南.md)｜关联版本：V7.3.2（仓库证据闸）｜验证：目标 manifest 46/46；preflight、smoke、topology、stage gate、端口隔离、独立库恢复和回滚均通过｜阻塞原因：缺少指定目标环境和外部资源，不能伪造上线证据｜阻塞开始：2026-07-11｜下次复查：2026-07-22
- [ ] FE-001｜状态：进行中｜目标版本：V7.4.16｜模块：前端应用壳与页面/实验注册｜主责：FE｜协作：DEV、QA、UX｜任务：按可回滚切片减少 `index.html`、ModuleSelector 和 Router 手工清单，建立显式页面/实验注册表与唯一整页离开入口；结构首片不改变模块切换、视觉或认证权威｜前置：PM-001 完成；V7.4.11 契约基线｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：V7.4.13（19 页面注册）；V7.4.16（88 实验结构注册）｜验证：88 项与 CONFIG/DOM 双射、脚本存在、87 hook + 1 兼容旁路、65/21/2 cleanup 状态、唯一 `leavePage`、Service Worker、14 项合同和三条真实深链已通过主开发验证；等待 QA-002 独立验收
- [ ] QA-002｜状态：已认领｜目标版本：V7.4.16｜模块：实验注册结构独立验收｜主责：QA｜协作：FE、DEV｜任务：对 V7.4.16 精确提交执行 88 项合同、加载顺序、深链、整页离开/重入、桌面和 390×844 独立回归｜前置：V7.4.16 提交交付｜关联文档：本文件、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：待验收后记录｜验证：至少覆盖函数图像、机械、晶体结构、搜索、排序、细胞结构和机械能；明确 21+2 未启用边界
- [ ] FE-002｜状态：已认领｜目标版本：V7.4.17｜模块：21 项候选 cleanup 逐批启用｜主责：FE｜协作：QA｜任务：按学科验证并接线 21 个已有 destroy 的候选，补足异步/共享资产边界，不改变尚未证明安全的模块切换语义｜前置：QA-002 通过｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[08-前端页面实现索引.md](08-前端页面实现索引.md)｜关联版本：待提交｜验证：每项离开/重入无重复 RAF、interval、Observer、监听器、控件或 console；验证后才允许升级状态
- [ ] FE-003｜状态：已认领｜目标版本：V7.4.18｜模块：缺失实验取消协议｜主责：FE｜协作：QA｜任务：为 `sorting` 与 `cell-structure` 建立真实 init/destroy/取消协议，修复不可取消 sleep、匿名 Observer/监听器和持续 RAF｜前置：QA-002 通过｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[05-UI规范模板.md](05-UI规范模板.md)｜关联版本：待提交｜验证：排序中离页不继续写 DOM；细胞结构离页停止 RAF、断开 Observer/监听器；重复三轮重入无累积
- [ ] FE-004｜状态：已认领｜目标版本：V7.4.19｜模块：模块切换生命周期语义｜主责：FE｜协作：QA、UX｜任务：在 88 项 cleanup 均有真实协议后，评审并启用实验切换/返回画廊的 destroy 与再次 init，形成唯一模块级生命周期入口｜前置：FE-002、FE-003 完成｜关联文档：[01-开发者文档.md](01-开发者文档.md)、[05-UI规范模板.md](05-UI规范模板.md)｜关联版本：待提交｜验证：A→B、B→画廊、画廊→A、整页离开各三轮，hash、Canvas、公共浮层、异步任务和键盘焦点均稳定
- [x] QA-001｜状态：已完成｜目标版本：V7.4｜模块：FE-001 独立验收｜主责：QA｜协作：FE、DEV、UX｜任务：对页面注册表首切片执行独立契约、浏览器、移动端和角色越权回归并给出通过或退回回执｜前置：V7.4.13 提交交付｜关联文档：本文件、[05-UI规范模板.md](05-UI规范模板.md)｜关联版本：V7.4.15（验收记录；被验提交 `6ec516d`）｜验证：QA 独立完成 158 个 JS 语法、13 个合同、学生角色裁剪、越权 hash、页面生命周期、静态资源 200、console 与真实 Edge 390×844 回归，结论通过
- [ ] UX-001｜状态：已认领｜目标版本：V7.4｜模块：可访问性与键盘回归｜主责：UX｜协作：FE、QA｜任务：覆盖主导航、搜索、实验控件、浮层、三端工作台和 iframe 状态的键盘与焦点语义｜前置：FE-001 首切片稳定｜关联文档：[05-UI规范模板.md](05-UI规范模板.md)｜关联版本：待分配｜验证：键盘可达、焦点可见、模态焦点闭环、Canvas 有替代说明；自动扫描与人工键盘验收通过
- [x] BE-001｜状态：已完成｜目标版本：V7.4｜模块：知识快照测试确定性｜主责：BE｜协作：QA、TOOLS｜任务：消除 `test_snapshot_rebuild_script_skips_when_lease_is_unavailable` 对当前墙钟日期的依赖，使脚本租约占用场景使用显式确定时钟且不改变生产租约语义｜前置：2026-07-15 全量回归稳定复现单项失败；目标发布 5 项在沙箱外重跑通过｜关联文档：[01-开发者文档.md](01-开发者文档.md)｜关联版本：V7.4.14｜验证：知识快照与目标发布 29 项通过；后端全量 381 通过、5 跳过、0 失败；未传 `now` 时仍调用真实 `utc_now()`

FE-001 当前切片进度：

- 已完成：19 页面注册表、Router 消费、核心 cache/fallback、角色隐藏门禁和离页 hash 保真；版本 V7.4.13。
- 已完成结构首片：88 项冻结实验注册表接管脚本/init/cleanup 状态，ModuleSelector 接管唯一整页离开入口，Router 删除 69 项手工清理数组；版本 V7.4.16。
- 当前边界：65 项只保持既有 `legacy-callback`，21 项为 `candidate-unwired`，`sorting` 与 `cell-structure` 为 `missing`，全部 `verified:false`；V7.4.16 不改变实验切换/返回画廊语义。
- 待完成：QA-002 后按 FE-002、FE-003、FE-004 完成候选清理、缺失取消协议和模块级生命周期；不得把结构注册误报为 88 项清理已验证。
- QA-001 已通过：V7.4.13 的页面注册首片由 QA 独立执行自动化、Browser、角色越权、生命周期和真实 Edge 390×844 回归，未发现可归因回归；验收记录版本 V7.4.15。
- 已完成 `BE-001`：`run_rebuild` 增加内部可选 `now` 注入并复用同一时刻计算默认日期与首次租约判定；测试显式使用租约有效期内时刻，生产命令行未传值时仍走真实 UTC 时钟。版本 V7.4.14。

### 3.2 条件任务

- [ ] PERF-001｜状态：暂缓｜目标版本：V7.4｜模块：深分页与搜索｜主责：PERF｜协作：BE、DATA、QA｜任务：用真实目标数据评估 keyset pagination、全文索引或聚合物化｜前置：可脱敏的目标数据规模和查询分布｜关联文档：[07-后端优化与设计.md](07-后端优化与设计.md)｜关联版本：待分配｜验证：EXPLAIN 与基准给出改动前后 p95，且不返回 SQL、参数或敏感结果｜暂缓原因：缺少真实目标数据，禁止用小样本提前选型｜暂缓开始：2026-07-15｜下次复查：2026-07-22
- [ ] TOOLS-001｜状态：暂缓｜目标版本：V7.4｜模块：Python 测试客户端兼容迁移｜主责：TOOLS｜协作：BE、QA｜任务：依据上游正式兼容路径消除 Starlette/httpx 过渡期弃用警告，同步哈希锁、CI 和文档｜前置：上游兼容路径明确｜关联文档：[01-开发者文档.md](01-开发者文档.md)｜关联版本：待分配｜验证：Python 3.12 隔离锁安装与 386 项回归通过，警告由兼容升级或迁移消除而非过滤｜暂缓原因：当前没有足够上游证据，禁止猜测包替换关系｜暂缓开始：2026-07-15｜下次复查：2026-07-29
- [ ] REL-001｜状态：暂缓｜目标版本：V7.4｜模块：外部通道独立审批｜主责：REL｜协作：SEC、OPS、BE、QA｜任务：按业务价值只选择 Webhook、GitHub issue sync 或 audit anchor 中的一个 provider 做 staging｜前置：明确 provider、owner、权限和审批结论｜关联文档：[07-后端优化与设计.md](07-后端优化与设计.md)｜关联版本：待分配｜验证：最小权限、幂等、限流、歧义、回执和人工核对证据完整，失败可关闭回本地权威｜暂缓原因：尚无 provider 价值与审批结论｜暂缓开始：2026-07-15｜下次复查：2026-08-01
- [ ] PM-002｜状态：暂缓｜目标版本：V7.4｜模块：代码空间/OJ/AI 二期决策｜主责：PM｜协作：CONTENT、SEC、AI、OPS｜任务：评估代码提交、判题记录和 AI 助教是否进入正式产品｜前置：数据、隐私、成本与教学责任评审｜关联文档：[00-项目总纲.md](00-项目总纲.md)｜关联版本：待分配｜验证：形成批准、拒绝或继续调研结论，批准前不接入生产数据｜暂缓原因：前置评审未启动，且不属于当前 P0｜暂缓开始：2026-07-15｜下次复查：2026-08-01

## 4. 调度与依赖

```text
PM-001
├── FE-001 ──> QA-001
│             └──> QA-002 ──> FE-002 + FE-003 ──> FE-004 ──> UX-001
└── OPS-001 ──> 真实目标环境发布

OPS-001/真实数据 ──> PERF-001
上游兼容证据 ──> TOOLS-001
独立审批 ──> REL-001
产品评审 ──> PM-002
```

当前顺序：V7.4.16 实验注册结构首片提交后交 QA-002 独立验收；通过后集中完成 FE-002 与 FE-003，再由 FE-004 收束模块切换生命周期，最后启动 UX-001。OPS-001 继续等待真实目标环境输入包。

`BE-001` 已作为 V7.4.14 独立小切片完成，未与 FE-001 实验注册混成同一提交。

## 5. 验收与风险

- 自动化：`python -m pytest backend`、`npm test`、依赖锁检查、C++ manifest/smoke、`git diff --check` 和文档/项目对接校验。
- 浏览器：桌面与 390×844 检查页面身份、非空首屏、错误层、console、截图和至少一条真实交互。
- 发布：MySQL 迁移、preflight、smoke、topology、stage gate、端口隔离、服务恢复、独立库恢复和回滚证据。
- 风险：认证语义、前端大文件拆分、隔离测试数据、依赖漂移、外部通道默认关闭和遗留 `qianduan` worktree 成果保护。
- 不做：一次性重写全部前端、恢复 C++ 业务 API、开放任意 SQL/表编辑、持久化敏感学习状态、未经评审上线 OJ/AI。

## 6. 需要用户提供或确认

1. OPS-001 所需的目标域名、运维 owner、备份保留期、监控渠道和目标环境访问方式。
2. 遗留 `qianduan` worktree 的未提交首页/星序入口设计是否继续验收、迁移，还是保留等待后续决定。
3. 是否存在足够业务价值的首个外部 provider；没有则继续全部关闭。

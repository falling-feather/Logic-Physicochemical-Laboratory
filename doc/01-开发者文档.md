# 星序 Astra — 开发者文档

> **文档梗概**：提供当前稳定实现的权威阅读入口、系统连接关系、运行验证和维护边界；详细技术字典继续由兼容保留的 [`01-开发者手册.md`](01-开发者手册.md) 承载。
>
> **具体规划法则**：
>
> 1. 当前实现只依据代码、配置、测试和可复核运行结果；未来任务只链接 [`02-项目规划.md`](02-项目规划.md)。
> 2. 业务 API 只进入 `backend/`；浏览器认证保持 HttpOnly Cookie-only，非浏览器显式 Bearer 仅作兼容且不得与 Cookie 混用；页面和实验保持成对 init/destroy 生命周期。
> 3. 修改架构、路径、接口、页面、命令或维护方式时，同一提交同步本文和详细实现卷。
>
> **最后一次更新时间**：2026-07-19
>
> **更新者**：PM组

---

## 目录

1. [系统全貌](#1-系统全貌)
2. [实现入口](#2-实现入口)
3. [启动与验证](#3-启动与验证)
4. [扩展与维护边界](#4-扩展与维护边界)

---

## 1. 系统全貌

星序由主站 SPA、代码空间子站、FastAPI 业务后端、数据库和静态交付服务组成。浏览器先通过 `/api/users/me` 确认服务端 Session，再启动 Router；学生、教师和管理员的导航、section 与受保护脚本按权威角色裁剪。

```text
主站 index.html / shared / pages ──┐
代码空间 codevis ──────────────────┼──> 浏览器 + HttpOnly Cookie
                                   │             │
                                   │             ▼
Node/C++ 静态服务 <────────────────┘      FastAPI backend
                                                  │
                                      SQLite 开发 / MySQL 发布
```

当前集成交付基线为 V7.5.7。V7.4.31—V7.4.36 已依次完成开放式 `#planets` 三星系总览、唯一 `#admin` 五领域全局治理、三角色统一星序落点、安全退出、9001 同源本机入口和集中验收；V7.4.37 进一步修正角色工作台的页面归属：`#student/#teacher/#admin` 现在全部登记为 `astra`，与工科试验室、代码空间、未来星系三个学习单元并列归属星序，不再继承工科试验室 navbar、页脚或课程壳。V7.5.4 与 V7.5.5 已分别把代码空间和未来星系从演示/散乱长页重绘为课程目录、独立子课和可完成互动；V7.5.6 增加题目、代码提交和隔离判题持久化契约，V7.5.7 再把学生班级/课程上下文、正式提交、未来星系直达发布裁剪和教师三星系编排接入浏览器。此前 V7.4.13—V7.5.3 的页面/实验注册、生命周期、Cookie/Bearer 契约、角色资源裁剪、组织治理、课程发布和 target-release-v2/51 项仓库门禁保持有效；用户仍将域名与公网 R6 后置，本机 9001 结果不得外推为 staging/production。

V7.5.x 已完成规划、团队启动、`BE-004/V7.5.3` 通用课程底座、`FE-009/V7.5.4` 代码空间、`GAME-001/V7.5.5` 未来星系、`BE-005/V7.5.6` 代码判题契约和 `FE-010/V7.5.7` 教师编排。Alembic 0048 用稳定键、`CourseUnitClassPlan` 与 `CourseClass.plan_version` 承载班级排序、`hidden/locked/open`、计划开放和前置分块；0049 增加 `CodeProblem/CodeProblemVersion/CodeSubmission/CodeJudgeAttempt`。问题版本不可变快照题面、测试规格、语言与资源策略；提交按学生+题目版本+班级幂等。代码空间使用内存态 Cookie Session 上下文解析单班级或显式班级/课程映射，正式提交 adapter 只消费权威题目/提交接口；未来星系的全局发布上下文在直达 `#frontier` 或子课时同样失败关闭。教师工作台第五个“课程节奏”视图按星系、班级、课程读取发布计划、进度矩阵与代码提交，PATCH 携带 `expected_version`，409 后丢弃陈旧状态并权威重载；代码源码单独读取并转义呈现。默认 `DisabledCodeRunnerAdapter` 只持久化 `runner_unavailable`，AI 助教继续后置。详细设计与验收边界见 [`21-V7.5课程体验与教师编排设计.md`](02-子文档/21-V7.5课程体验与教师编排设计.md)，任务状态只认 [`02-项目规划.md`](02-项目规划.md)。

V7.4.30 补充：Windows 服务包生成器与 `deploy.ps1` 通过受限 `--environment`/`-Environment` 显式区分 `staging` 和 `production`；默认值仅为 production 兼容旧调用，目标 staging 不得手工改 XML，也不得把 development 写入目标服务包。报告、API/worker XML 和测试必须保持同一环境语义。

V7.4.32 补充：`#planets` 使用代码原生的开放式轨道索引同时展示工科试验室、代码空间与未来星系资源，桌面使用内部身份侧栏，390×844 使用固定角色 dock；页面不展示无权威来源的进度或统计。`#admin` 仍是唯一管理员页面，治理范围覆盖全部星系；五个本地分区只切换现有用户、组织、内容、运行与审计面板，领域数据地图、白名单 PATCH、双确认、409/歧义写锁和“不开放任意 SQL”边界原样保留。其旧有 englab 运行归属已由 V7.4.37 纠正为 astra。

V7.4.33 补充：`AstraApplicationSession.roleLanding(role)` 对 student/teacher/admin 均返回 `planets`，`roleWorkspace(role)` 才分别返回 `student/teacher/admin`。空 hash 登录统一显示星序，显式且有权的角色深链仍保留；越权深链在角色脚本加载前回落星序。全局账号菜单同时提供“返回星序总览”和“进入角色工作台”，星序顶栏提供直接调用 Cookie Session 登出端点的 44px 安全退出按钮。app-session、page-registry、main、planets 与 Service Worker 缓存代际为 `20260718v7433RoleLandingP0`；student/teacher/admin CSS/JS 继续 network-only，不进入共享 CacheStorage。

V7.4.34 补充：根目录 `astra-local.ps1` 是 Windows 本机验收入口，默认从 `%LOCALAPPDATA%\Astra\local-preview` 使用 SQLite，自动创建/校验仓库内忽略的 Python 3.12+ `.venv`、按 `backend/requirements.lock` 的 SHA-256 哈希锁安装依赖并执行 Alembic head；规范化数据目录哈希同时绑定互斥量与实例响应头，阻止同库跨端口并发和错误数据库的重复启动误认，development 还显式关闭 `.env` 可能开启的外部投递、锚定和调度器。`backend/app/local_preview.py` 复用权威 `create_app()`，只额外挂载 `pages/`、`shared/`、`UI/`、`codevis/`、`index.html`、`sw.js` 与 `LICENSE.md`；本机首页注入 same-origin-only 标记并清理旧 `astra-api-base`，`AstraApiClient` 据此拒绝任何跨 origin API，`backend/`、`doc/`、`server/`、`.git/` 和其他根文件保持 404。可选 `-BootstrapAdmin` 通过隐藏输入与 stdin 调用权威 bootstrap API，不把密码写入参数或持久文件；该入口只绑定 `127.0.0.1`、只设置 development 进程环境，不修改 `deploy.ps1` 或 staging/production 语义。

V7.4.35 补充：QA-009 首轮证明确认旧 `role-workflows-proof.cjs` 对本机运行时 bootstrap 201 的假设与 9001 启动边界冲突；最终实现仅在 `--confirm-isolated-environment` 下接受成对的临时 `ASTRA_QA_ADMIN_USERNAME`/`ASTRA_QA_ADMIN_PASSWORD`，登录由 `-BootstrapAdmin` 预置的管理员并跳过运行中创建，缺一即失败、密码不写入报告，目标 staging token 路径不变。bootstrap 的权威状态语义是：全新未预置且运行时开关关闭时返回 403；启动期成功预置后重复请求返回 409 `Admin bootstrap is already complete`，两者均非 201、无新增管理员/审计写入。证明同时适配越权 hash 回落 `#planets`、五领域管理分区、用户筛选和归档/恢复色语义，公共缓存代际为 `20260718v7435QaCloseoutP0`。QA-009 对精确提交 `ef17c7bcc354b4b2a9403c357d23c4f209eb5f35` 的独立复验已通过。

V7.4.36 补充：R5c 与 PM-005 已完成，FE/UI/OPS/QA 长期任务已取消置顶并归档；两项视觉 P3（星系轨道实装比概念更紧凑、角色工作台信息密度偏高）只作为后续可选优化，不阻断当前本机交付。R6/OPS-001 保持暂缓和未完成。

V7.4.37 补充：`AstraPageRegistry` 把 student、teacher、admin 的 galaxy 统一设为 `astra`，首页预判不再把角色 hash 放入 `englabPages`；Router 以 galaxy 而非具体页名隐藏工科 navbar，并只对真实 englab 非首页显示工科页脚。三个角色 section 共享星序左侧层级导航、移动端三学习单元条和角色 dock。教师工作台只在当前分区渲染对应内容；组织、课程、单元和作业操作使用原生 disclosure 展开，保留既有 API、权限、归档只读和未知写入对账边界。移动端覆盖 `.page` 的 `will-change: transform`，确保固定 dock 相对视口而不是文档底部定位。

V7.5.3 补充：`GET /api/courses/{course_id}/units?class_id=...` 对学生返回当前班级的非隐藏活动及 `effective_release_state/lock_reasons`；学生在同一课程属于多个班级时必须显式提供 `class_id`。教师或管理员通过 `GET/PATCH /api/courses/{course_id}/classes/{class_id}/release-plan` 读取和更新发布计划，PATCH 要求 `expected_version`，冲突返回 409，无变化不消耗版本；`GET /api/progress/courses/{course_id}/classes/{class_id}/students` 返回分页学生×分块矩阵。读取路径不补写缺失计划，数据不一致失败关闭；创建挂班/单元时才建立默认开放计划。0048 已验证 SQLite 0047→0048→0047→0048 往返和 MySQL DDL 编译，未提供隔离 MySQL，因此没有新增真实 MySQL 或目标环境通过结论。

V7.5.4 补充：`/codevis/#catalog` 是代码空间默认入口，`#lesson?activity=<activity_key>` 与 `#challenge?activity=<activity_key>` 分别承载可刷新恢复的单一学习目标和“预测—运行—追踪—修正”闭环，旧 `#trace` 仅保留兼容追踪工具；activity 参数只用于选择稳定清单项，不是授权来源。`CvCourseManifest` 冻结 6 组、18 活动和四语言元数据；`CvCourseStateAdapter.configureHttp()` 只接收明确的课程 ID 映射、`class_id` 与 same-origin fetch。JS-Interpreter、Skulpt、JSCPP 以固定版本保存在 `codevis/vendor/`，来源、许可证和 SHA-256 记录在 `vendor/manifest.json` 与 `THIRD_PARTY_NOTICES.md`；加载器不使用 CDN，C/C++ 运行固定在 Worker 且主线程 4.2 秒硬终止。学生页面不显示这些实现说明。

V7.5.5 补充：`#frontier` 只渲染六方向课程目录，`#cosmos/#engineering/#datascience/#infotech/#materials/#humanities` 解析各自的 `route_slug` 并进入单一子课；稳定 `activity_key` 不进入 hash，也不作为授权来源。`FrontierCourseManifest` 冻结 `future-galaxy` 的 6 组/18 活动，并通过 same-origin BE-004 adapter 把缺失活动解释为 hidden、异常响应解释为 unavailable。普通活动按需创建各自 Canvas owner；`cosmos.orbital-scale` 才动态导入本地 `three@0.185.1`，使用太阳点光源、轨道位置与观察方位控件解释昼夜关系，无 WebGL、低性能或 `prefers-reduced-motion` 时切换为仍可完成判断的 Canvas。离页会中止异步导入、取消 RAF、断开 Observer/监听器并释放 geometry/material/texture/renderer/WebGL context。未来星系页脚和代码空间一样保留工科试验室的品牌列、课程/关于列与版权行结构。

V7.5.6 补充：教师/管理员通过 `POST /api/code-problems` 和 `POST /api/code-problems/{id}/versions` 建立题目与不可变版本；学生或前端可用 `GET /api/code-problems/by-activity?course_id=...&activity_key=...&class_id=...` 从稳定课程身份发现题目。`POST /api/code-problems/{id}/submissions` 只允许学生在 open 分块提交 canonical JavaScript/Python/C/C++，按 UTF-8 字节限制源码与输入，并以学生+版本+班级保证幂等；相同内容重放返回原记录，不同内容冲突 409。`GET /api/code-submissions` 按身份、班级、课程、活动和数据库分页裁剪，学生只能读取本人且课程/单元撤回发布或分块 hidden 后不再返回当前资源，班级教师/管理员仍可读取授权历史与源码。提交与判题尝试状态区分 queued、runner_unavailable、running、accepted、wrong/partial、编译/运行/时间/内存/输出失败和取消；当前没有已批准 runner，默认只诚实保存不可用状态。

V7.5.7 补充：`codevis/shared/js/student-context.js` 从 Cookie Session 和本人课程读取单班级或显式班级/课程映射，只在内存中配置 `CvCourseStateAdapter`；多班级歧义、认证丢失和接口异常失败关闭。`submission-adapter.js` 将公开预检与正式提交分离，按稳定活动解析题目并提交源码，runner 未启用时显示 `runner_unavailable` 而不是成功。主站 `shared/js/frontier-publication-context.js` 在 Router 之前监听权威会话，使 `#frontier`、直达子课和学生工作台共享同一发布裁剪，hidden 不泄漏标题，locked 使用教师节奏说明。`#teacher` 新增第五个 `curriculum` 视图：教师按星系/班级/课程读取并修改发布计划、查看学生进度，代码空间另显示提交列表、独立源码与判题尝试；写入不重试，409 使用权威版本重载。

## 2. 实现入口

| 范围 | 位置 | 当前职责 | 详细说明 |
| --- | --- | --- | --- |
| 主站外壳 | `index.html`、`shared/js/page-registry.js`、`shared/js/experiment-registry.js`、`shared/js/main.js`、`shared/js/router.js` | 19 页面与 88 实验运行注册、页面壳、启动、hash 路由和生命周期编排 | [`01-开发者手册.md`](01-开发者手册.md) 第 4、7 节 |
| 页面与实验 | `pages/`、`shared/js/config.js`、`shared/js/module-selector.js` | 三个星系、88 个实验、画廊顺序、按需加载、精确模块级 cleanup 与整页离开入口 | [`08-前端页面实现索引.md`](08-前端页面实现索引.md) |
| 代码空间课程 | `codevis/index.html`、`codevis/pages/course-*`、`codevis/shared/js/course-manifest.js`、`codevis/shared/js/student-context.js`、`codevis/shared/js/submission-adapter.js` | 6 组/18 活动目录、子课、互动挑战、Cookie 学生上下文、正式提交 adapter 与本地审计运行时 | [`codevis/README.md`](../codevis/README.md)、[`08-前端页面实现索引.md`](08-前端页面实现索引.md) |
| 未来星系课程 | `pages/frontier/`、`shared/js/frontier-publication-context.js`、`shared/js/frontier-learning.js`、六方向 Canvas owner、`shared/vendor/three-r185/` | 6 组/18 活动目录、单目标子课、直达发布上下文、按需 Canvas/Three 与完整降级/清理 | [`21-V7.5课程体验与教师编排设计.md`](02-子文档/21-V7.5课程体验与教师编排设计.md)、[`08-前端页面实现索引.md`](08-前端页面实现索引.md) |
| 星序角色工作区 | `index.html`、`shared/css/app-session.css`、`pages/student/`、`pages/teacher/`、`pages/admin/` | 星序层级导航、角色入口、教师五分区课程编排与管理员全局治理；不得继承任一学习单元后台壳 | [`05-UI规范模板.md`](05-UI规范模板.md)、[`08-前端页面实现索引.md`](08-前端页面实现索引.md) |
| 会话与 API client | `shared/js/app-session.js`、`shared/js/api-client.js` | Router 前会话确认、cookie-only 请求和失败对账 | [`01-开发者手册.md`](01-开发者手册.md) 第 4、5 节 |
| 业务后端 | `backend/app/` | 认证、组织、教学、内容、任务、审计、课程发布计划、进度聚合、代码题目/提交与隔离 runner 契约 API | [`07-后端优化与设计.md`](07-后端优化与设计.md) |
| 数据与迁移 | `backend/alembic/`、`backend/app/models/` | SQLite/MySQL 模型与连续至 0049 的 Alembic 演进 | [`04-部署指南.md`](04-部署指南.md) |
| 静态交付 | `server/` | Node 开发静态服务和 C++ Release 静态服务 | `README.md`、`server/README.md` |
| 本机同源验收 | `astra-local.ps1`、`backend/app/local_preview.py`、`backend/scripts/local_preview_bootstrap_admin.py` | 9001 单端口依赖准备、迁移、静态白名单、现有 API 与可选首管理员引导 | [`04-部署指南.md`](04-部署指南.md)“本机验收入口” |
| 自动化 | `backend/tests/`、`tools/tests/`、`tools/browser/` | 后端、契约、浏览器和发布证明 | `README.md` 质量门禁 |
| 目标发布证据 | `backend/scripts/target_release_evidence.py`、`backend/scripts/target_release_gate.py`、`tools/browser/target-browser-evidence.cjs`、`backend/target-release-manifest.example.json`、`backend/release-artifact-manifest.example.json` | 制品清单、fail-closed 恢复/回滚模板、浏览器运行断言到 revision-bound 固定 raw、七类来源/时间/语义校验、同源 envelope 封装与 51/51 总闸 | [`04-部署指南.md`](04-部署指南.md) 第〇节 |
| 目标 staging 密钥与验收 | `backend/scripts/windows_dpapi_secret_store.py`、`backend/scripts/windows_service_drill_bundle.py`、`deploy.ps1`、`tools/browser/role-workflows-proof.cjs` | 受保护 DPAPI 密钥文件、服务账号绑定、正式 origin 服务包和本地/目标两种浏览器证明模式 | [`04-部署指南.md`](04-部署指南.md) 第三节 |
| 后端阶段追踪 | `doc/09-后端阶段收束小版本开发安排.md` | V6.6 历史锚点、V7.4 R1—R6 顺序、固定退出门禁和目标环境证据边界 | 任务状态仍以 `doc/02-项目规划.md` 为准 |

## 3. 启动与验证

从仓库根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\astra-local.ps1
```

访问 `http://127.0.0.1:9001/`，用 `Ctrl+C` 停止。首次需要管理员时在尚无管理员的本机数据目录使用 `-BootstrapAdmin`，密码只经隐藏交互输入。该命令是默认验收方式；若需分别调试静态与 API，再使用下面的双终端方式：

```powershell
npm ci --ignore-scripts
node server/dev-static-server.mjs --port 8766
```

另一个终端执行：

```powershell
Set-Location backend
python -m pip install --require-hashes -r requirements.lock
python -m alembic upgrade head
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

联调入口为 `http://127.0.0.1:8766/?backendSchema=1&apiBase=http%3A%2F%2F127.0.0.1%3A8000#home`。完整环境、正常结果和失败排查见 [`README.md`](../README.md) 与 [`04-部署指南.md`](04-部署指南.md)。

提交前最低验证：

```powershell
python -m pytest backend
npm test
git diff --check
```

## 4. 扩展与维护边界

- 新增主站页面前先在 [`02-项目规划.md`](02-项目规划.md) 建立任务，再同步 `AstraPageRegistry`、DOM 导航身份、脚本/钩子、HTTP fallback、Service Worker 和契约；角色权限继续只由 `app-session.js` 掌管。
- 新增实验需同步 `CONFIG.experiments`、`AstraExperimentRegistry`、`data-module` DOM 和对应实现文件；脚本/init/cleanup 状态只在注册表登记，`ModuleSelector` 是运行入口，Router 不再维护实验清理表。
- cleanup 边界保持 65 项 `legacy-callback / verified:false`、23 项 `validated-callback / verified:true`、0 项 `missing`。`AstraExperimentRegistry.cleanupModule(subject, id)` 只执行精确 `verified:true` owner，并返回冻结报告 `unknown`、`unverified`、`owner-unavailable`、`cleaned` 或 `failed`；65 项 legacy 绝不执行模块级 callback。`openModule()` 的 A→B 与 `closeModule()` 的 B→画廊会先关闭学科 Zoom，再清理当前已验证 owner；owner/Zoom 失败在 DOM、hash 和 active state 变化前中止。成功 cleanup 删除初始化标记以允许重新 init；legacy 保留初始化标记。transition generation 会取消或失效旧资源加载、init 重试、resize、焦点和相关推荐 timer，dirty legacy 不得在同页重复 init；整页离开继续由 `cleanupPage()` 兼容清理，并通过 `skipExperimentCleanup` 防止双执行。
- V7.4.19 将 DNA/遗传的模式 wrapper 查找从错误的 `.canvas-container` 子树改为真实 `.demo-section` 范围；`bio-dna-modes`、`bio-photo-modes`、`bio-genetics-modes` 在桌面和 390×844 各三轮进入/切换/离页/重入后均为单例。候选 cleanup 合同也使用与 `index.html` 一致的 controls/Canvas 兄弟关系，不得再用失真 fake DOM 掩盖挂载失败。
- V7.4.20 要求活动首页使用 `.page.home-page.active` 恢复 `position: fixed`、动态视口高度与 `overflow: hidden`，子级 `.home-container` 使用 `height: 100%` 跟随父级 `100dvh`；这是对加载顺序中 `.page.active` 更高选择器权重的显式覆盖。首页的星空、极光和轨道允许在页面内部超出几何边界，但不得再进入 HTML/body 的根滚动宽度；`home-particle-lifecycle-contract` 同时冻结该 CSS 与版本化加载契约。
- V7.4.23 要求 `index.html`、`APP_SHELL` 与星系 HTTP fallback 均不得静态包含三角色 CSS/JS。`AstraPageRegistry` 维护页面角色与样式/脚本矩阵，`app-session.js` 仅在 `/api/users/me` 成功后加载允许样式；Router 只在角色守卫通过后加载页面脚本。三端资源由 Service Worker 使用 `cache: no-store` 的 network-only 分支处理，退出、运行中 401 与跨角色重新认证必须清理角色样式/CacheStorage 并整页重载，避免旧脚本继续留在同一 JavaScript 执行域。
- V7.4.24 的组织写入口固定为 `GET/PATCH /api/admin/schools/{id}` 与 `GET/PATCH /api/admin/classes/{id}`。PATCH 只接受声明式白名单字段、非空原因和 `expected_version`；成功响应是提交后的权威资源，409 后调用方必须重新 GET 对账，不得自动重放。学校/班级归档会拒绝后续教学写入并从无显式范围的当前学情/进度中排除，但管理读取、作业历史、提交和事件历史继续保留。
- V7.4.25 的管理组织编辑器先精确 GET，再把身份、状态和版本作为只读权威字段展示；元数据白名单与归档/恢复分开确认，用户修改输入后必须重新双确认，no-op 不写入。每次操作最多一次 PATCH；2xx 后以精确 GET 证明版本递增和字段一致，409 保留草稿并读取新版本，发送后取消/超时/网络错误保留跨路由写锁，只有显式对账确认未应用后才解锁。页面 owner/generation 保护旧请求不得清除新生命周期 busy；归档学校/班级使教师成员、成绩和作业策略入口只读，学生提交原因显示 `class_archived`。FastAPI CORS 允许凭据型 `PUT` 预检。
- V7.5.7 的 page registry、Router、main、teacher 与 Service Worker 壳层代际使用 `20260719v757TeacherCurriculumP0`，未来星系发布上下文使用 `20260719v757FuturePublicationP0`；未改动的 app-session、planets 和 admin 专属资源继续保持各自既有代际。`astra-workspace-hierarchy-contract.cjs` 与教师/学生发布合同冻结角色归属、工科壳显隐、教师五分区、直达课程裁剪和移动固定 dock；角色资源仍为 network-only，不得进入共享 CacheStorage。
- 前端认证显示不得替代后端授权；浏览器不得读取 HttpOnly Cookie、兼容 token 或持久化业务 token，也不得缓存 `/api/*`。非浏览器 Bearer 调用必须显式排除 Cookie；混合、重复或畸形凭据由后端 fail closed。
- C++/Node 静态服务不得恢复业务 API，也不得挂载仓库根目录。
- 数据库结构变化必须带 Alembic、回滚考虑、SQLite 回归和真实 MySQL 门禁。
- 目标发布证据只接受 `target-release-v2`：manifest 先冻结 target instance/origin/public probe host 与 release version/revision/同包五类制品清单路径/SHA/bundle ID；seal、51 项总闸和 Windows 服务包共用公开 DNS/标准 443 的精确 HTTPS origin 校验，IP、单标签、保留/示例域、非法 DNS label、路径或非标准端口均失败关闭。原始 evidence 数组必须精确七项且 run ID 唯一，自动 raw 必须携带执行时间，再经 `target_release_evidence seal` 固定语义，并同时接受封装时刻与总闸当前时刻的新鲜度复核。目标 topology 必须绑定外部探测引用和可解析公网 host、强制原始 API 端口不可达并逐项回读四服务 SCM；deploy smoke 必须通过单一 0047 组织治理列与 `version >= 1`；浏览器报告必须含三角色、资源隔离、陈旧版本 409 及组织归档/恢复。正式 CLI 不接受时间覆盖，不得提交真实 evidence bundle。
- Windows 目标服务不得把 DSN、审计盐或 bootstrap token 放入 XML、命令参数、系统级普通环境或报告。`windows_dpapi_secret_store.py seal` 从 stdin 接收 `ASTRA_*` JSON，使用 DPAPI LocalMachine，并在创建时给专用父目录与文件写入受保护 DACL；只有冻结的操作者 SID、SYSTEM、Administrators 和所选 LocalService/NetworkService 可访问，操作者 SID/服务账号同时绑定加密载荷与外层元数据，服务运行时不得改用“当前 SID”误判 ACL。bootstrap token 只进入 API，不进入 worker。目标浏览器 proof 只接受公开 DNS、标准 443 的同源 HTTPS、明确 `staging/production` health、干净 Git、仓库外空输出目录和 `ASTRA_ADMIN_BOOTSTRAP_TOKEN` 环境注入；成功后直接生成绑定当前 `release_revision` 的固定 `target-browser-smoke.json`，完成后仍须独立做 MySQL 权威总账及 target-release-v2 封装。
- 外部投递、问题同步和审计锚定默认关闭，启用需要独立审批与 staging 证据。
- 详细模块、接口、页面、命令和反例继续维护在 [`01-开发者手册.md`](01-开发者手册.md)；本文只提供规范入口和当前连接关系。

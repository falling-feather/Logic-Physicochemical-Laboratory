[pytest]
pythonpath = .
testpaths = tests
addopts = -q -p no:cacheprovider
norecursedirs = .pytest_cache pytest-cache-files-*
markers =
    mysql_release_evidence: explicit real-MySQL release drill; skipped unless a confirmed isolated database is supplied
